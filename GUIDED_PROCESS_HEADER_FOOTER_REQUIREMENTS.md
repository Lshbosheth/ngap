# 引导式 Header / Footer 第一优先级需求文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 需求名称 | 引导式顶部信息区与底部操作区 |
| 优先级 | P0，当前第一优先级 |
| 目标版本 | 引导式改造第一期 MVP |
| 需求范围 | Header、Content、Footer 的流程位置约束、编辑配置、运行时分区渲染和 Footer 业务操作 |
| 不包含 | `control` 正式能力、复杂条件语言、大流程性能专项、旧流程批量迁移 |
| 适用运行时 | 主 `src` 预览和独立 `page` 运行页 |

---

## 2. 背景

当前引导式运行页把已执行节点的业务元素持续追加到同一个正文区域，不能稳定表达：

- 哪个业务节点是整个场景最先展示的顶部核心信息；
- 哪些业务节点属于正常诊断或办理环节；
- 哪个业务节点是流程最后的场景按钮操作区；
- Footer 中不同按钮如何根据前面 Content 的结果控制显隐并执行操作。

现有 `BottomBanner` 主要依靠物料自身绝对定位实现底部展示，容易覆盖正文、重复叠加，也没有明确表达它在流程中的结束位置。

为了先开发简单引导式流程，第一期优先完成 Header、Content、Footer 三段式区域及其首尾拓扑规则。

---

## 3. 核心产品模型

Header、Content、Footer 都是流程图中的业务节点，都必须通过连线参与流程执行；三者的区别是页面展示区域和在流程中的位置约束。

| 区域 | 产品名称 | 流程位置 | 是否进入智能导航 | 数量限制 |
| --- | --- | --- | ---: | ---: |
| `header` | 顶部核心信息 | 如果存在，必须是开始节点后的第一个业务节点 | 否 | 最多 1 个 |
| `content` | 正文环节 | Header 与 Footer 之间按连线执行 | 是 | 多个 |
| `footer` | 底部操作区 | 如果存在，必须是结束节点前的最后一个业务节点 | 否 | 最多 1 个 |

标准拓扑：

```text
有 Header 和 Footer：
开始 → Header → Content... → Footer → 结束

只有 Header：
开始 → Header → Content... → 结束

只有 Footer：
开始 → Content... → Footer → 结束

都没有：
开始 → Content... → 结束
```

其中：

- 开始节点存在 Header 时，只能直接连接 Header；
- Footer 存在时，只能直接连接结束节点；
- Footer 是页面布局上独立的底部按钮区域，但不是脱离流程图的场景配置；
- Footer 不允许后面继续连接普通业务节点或分支节点。

---

## 4. 页面结构

```text
┌────────────────────────────────────┐
│ Header：顶部核心信息（可选）       │
├────────────────────────────────────┤
│ 智能导航（按现有开关决定是否显示） │
├────────────────────────────────────┤
│ Content：按活动路径追加正文环节    │
│                                    │
│                                    │
├────────────────────────────────────┤
│ Footer：场景按钮操作区（可选）     │
└────────────────────────────────────┘
```

- Header 按流程进入后显示在页面顶部；
- Content 按实际命中的活动路径持续追加；
- Footer 按流程进入后显示在页面底部操作区；
- 没有配置或流程尚未进入 Header/Footer 时，对应区域不产生空白占位；
- Footer 视觉上独立于 Content，不能混在正文元素列表中，也不能覆盖正文。

---

## 5. 业务目标

第一期完成后，流程设计人员可以：

1. 把一个业务节点设置为 Header；
2. 把一个业务节点设置为 Footer；
3. 让其他业务节点默认作为 Content；
4. 在画布中清晰看到 Header 是首个业务节点、Footer 是末尾业务节点；
5. 在 Footer 中组装一个或多个场景操作按钮；
6. 使用前面 Content 的选择结果控制 Footer 整体或内部按钮显隐；
7. 点击 Footer 按钮执行对应的接口、事件、变量赋值或其他业务操作；
8. 在主预览和独立运行页获得一致结果。

---

## 6. Header 业务规则

### 6.1 数量和位置

- 一个流程可以没有 Header，最多只能有一个 Header；
- 存在 Header 时，开始节点必须且只能直接连接 Header；
- Header 不能拥有其他父业务节点；
- Header 不能位于某条中间分支中；
- Header 后面进入 Content；如果流程没有 Content，也可以按产品允许的最简流程直接进入 Footer 或结束节点；
- Header 不进入智能导航，也不计入诊断总数和异常数。

### 6.2 唯一性替换

当用户把第二个节点设置为 Header 时，必须弹出替换确认：

```text
当前已有顶部节点“诊断核心信息”。
是否将顶部区域替换为“客户基础资料”？
```

确认后：

- 原 Header 恢复为 Content；
- 新节点设置为 Header；
- 系统提示用户检查开始节点和原节点的连线；
- 不得静默删除业务节点或业务组件内容。

取消后不修改任何配置。

### 6.3 Header 运行方式（待评审确认）

建议第一版：

- Header 不配置人工/自动，不作为流程停顿点；
- Header 组件加载完成后自动进入唯一后续节点；
- Header 不承担多条件分支；
- Header 只能有一条无条件出线；
- 重新开始流程时重新初始化 Header；
- 页面卸载时释放 Header 内部组件、订阅和未完成异步任务。

---

## 7. Content 业务规则

- 普通业务节点默认属于 Content；
- 旧流程节点缺少展示区域字段时按 Content 兼容；
- Content 可以有多个，按流程连线和分支规则执行；
- 已进入活动路径且有内容的 Content 自动进入智能导航；
- Content 的人工/自动配置继续决定流程是否等待用户操作；
- Content 的点选、表单、变量或接口结果可以作为 Footer 显隐和按钮操作的输入；
- Content 内部数据不能由 Footer 直接读取 DOM 或 React state，必须进入平台可绑定的数据范围。

---

## 8. Footer 业务规则

### 8.1 数量和位置

- 一个流程可以没有 Footer，最多只能有一个 Footer；
- 存在 Footer 时，它必须是结束节点前最后一个业务节点；
- Footer 的唯一后续节点必须是结束节点；
- Footer 只能有一条出线，且该出线不能配置条件；
- Footer 后面禁止继续连接 Content、Header、其他 Footer 或分支节点；
- Footer 可以有一个或多个父节点，用于让不同互斥业务路径最终进入同一个 Footer；
- 所有需要展示 Footer 的有效路径都必须在结束前汇合到该 Footer；
- Footer 不进入智能导航，也不计入诊断总数和异常数。

### 8.2 场景级按钮区域

Footer 进入活动路径后，作为整个场景的独立按钮区显示在页面底部：

- Footer 可以包含多个按钮和必要的辅助提示内容；
- Footer 不按普通 Content 卡片样式展示；
- Footer 不加入正文滚动元素列表；
- Footer 固定在页面壳底部，正文保留独立滚动空间；
- Footer 高度变化时，正文可用高度必须自动调整；
- Footer 内容为空时不显示空白底栏，并给流程配置人员提示。

### 8.3 根据 Content 结果控制显隐

Footer 整体及内部按钮可以根据前面 Content 的结果控制显隐。例如：

```text
前面 Content 选择“需要办理”
→ Footer 显示“确认办理”按钮

前面 Content 选择“只需答复”
→ Footer 隐藏“确认办理”，显示“直接答复”按钮
```

联动规则：

- Footer 显隐条件使用平台统一的数据绑定和表达式能力；
- 第一版可以优先复用当前已有变量赋值、表单数据和元素显隐能力；
- 后续再统一切换到节点正式输出；
- Content 值变化时，Footer 整体、按钮显隐和禁用状态立即重新计算；
- 前序选择修改或分支回滚后，失效路径的旧值不能继续控制 Footer；
- Footer 不能直接读取 Content DOM 或组件内部 React state；
- Footer 只能读取允许公开给后续节点的数据，不能无约束遍历其他节点私有状态。

### 8.4 Footer 按钮操作

Footer 按钮点击后执行各自配置的业务操作，包括但不限于：

- 调用业务接口；
- 设置或更新变量；
- 触发组件事件；
- 打开弹窗或提示；
- 执行提交、确认、办理成功、直接答复等场景动作。

操作边界：

- 每个按钮可以有独立的显隐、禁用条件和点击动作；
- 按钮加载中、成功和失败反馈沿用现有事件/接口能力；
- Footer 按钮不能写死或输出 `nextNodeId`；
- Footer 后续只有结束节点，因此需要结束流程的按钮应触发“Footer 完成”语义，再由流程引擎沿唯一出线进入结束节点；
- 只打开弹窗、刷新数据等辅助按钮可以执行操作但不完成 Footer；
- 哪些按钮完成后进入结束节点，必须有明确配置，不能默认任意按钮点击都会结束流程；
- Footer 未完成时保持在当前 Footer 节点等待用户继续操作。

### 8.5 Footer 生命周期

- 流程进入 Footer 前，不显示 Footer 区域；
- 进入 Footer 后，初始化 Footer 组件及按钮状态；
- Footer 完成并进入结束节点后，成功页是否继续保留 Footer 由后续完成页规则决定；第一版建议流程结束后隐藏 Footer；
- 重新开始流程时清空 Footer 临时状态和上次操作结果；
- 分支回滚使 Footer 退出活动路径时，清空 Footer DOM、订阅和临时数据；
- 页面卸载时取消 Footer 未完成异步任务。

---

## 9. 编辑器需求

### 9.1 配置入口

在流程业务节点编辑入口增加“展示区域”：

```text
展示区域
  ○ 正文环节（默认）
  ○ 顶部核心信息
  ○ 底部操作区
```

展示区域配置属于当前流程中的节点实例，不能修改通用业务组件模板。

### 9.2 配置交互

- 新增和旧业务节点默认显示“正文环节”；
- 设置 Header/Footer 时立即进行唯一性和拓扑校验；
- 存在第二个 Header/Footer 时必须完成替换确认；
- 节点切回 Content 时保留其组件内容，不自动删除节点；
- Footer 配置面板允许编辑整体显隐、按钮显隐、按钮动作以及“是否完成 Footer”；
- 数据选择器显示 Footer 当前允许使用的 Content 数据和流程共享数据；
- 保存应用前执行 Header/Footer 首尾拓扑校验；
- 校验失败时高亮节点和问题连线，并给出明确修复说明。

### 9.3 画布展示

节点卡片增加区域标签：

```text
[顶部] 诊断核心信息
[环节] 套餐使用情况
[底部] 办理操作
```

建议：

- Header 使用蓝色标签；
- Content 沿用当前普通节点样式；
- Footer 使用紫色标签；
- Footer 与结束节点相连时仍保留正常连线展示；
- 区域标签不能改变节点 ID、分支数据或业务组件内容。

### 9.4 连线实时限制

- 存在 Header 时，开始节点连接其他业务节点应被阻止并提示先连接 Header；
- Header 不能再连接第二条出线；
- Footer 创建出线时只能选择结束节点；
- 其他节点不能从 Footer 之后继续接入；
- 多条互斥路径可以进入同一个 Footer；
- 删除 Header/Footer 或修改区域后，重新执行拓扑校验，不能留下不可达节点。

### 9.5 保存校验文案

至少包含：

- “一个流程最多只能配置一个顶部核心信息节点”；
- “顶部核心信息必须是开始节点后的第一个业务节点”；
- “一个流程最多只能配置一个底部操作区节点”；
- “底部操作区必须是结束节点前的最后一个业务节点”；
- “底部操作区之后只能连接结束节点”；
- “底部操作区只能配置一条无条件出线”；
- “存在底部操作区时，目标业务路径必须先进入底部操作区再结束”。

### 9.6 画布连线方向调整

为适配横向流程和旧流程常见的纵向流程，编辑器增加“连线方向”调整功能：

```text
连线方向
  ○ 左右：节点左侧进入、右侧流出
  ○ 上下：节点上方进入、下方流出
```

第一版按整张流程图统一设置，不要求用户逐条连线或逐个节点配置方向。

交互和数据规则：

- 新流程默认值沿用当前左右模式，避免改变现有使用习惯；
- 左右模式下，普通入线连接节点左侧，普通出线连接节点右侧；
- 上下模式下，普通入线连接节点上方，普通出线连接节点下方；
- 多个条件分支的出口在右侧或下方按分支顺序排列，分支文字和连接点需要保持一一对应；
- 切换方向后立即重新计算全部连线路径和箭头位置；
- 切换方向不能修改节点 ID、父子关系、分支索引、条件配置和流程执行顺序；
- 保存并重新打开流程后，继续使用上次选择的方向；
- 旧流程缺少该配置时按左右模式兼容；
- 开始、结束、Header 和 Footer 节点遵守同一方向：上下模式形成“开始 → Header → Content → Footer → 结束”的纵向视觉结构；
- 第一版切换方向只调整连接点和连线路径，不自动重排用户已经摆放的节点位置；自动布局作为后续独立能力；
- 方向切换后如果现有节点位置导致线路交叉，用户可以手动移动节点调整；
- 连线方向属于编辑展示配置，不影响运行时页面的 Header/Content/Footer 布局。

性能要求：

- 拖动节点时只更新与当前节点相关的连线，不允许每次鼠标移动刷新全部连线；
- 方向切换属于一次明确的整图操作，可以全量重算，但不能在普通拖动中反复触发；
- 必须使用包含多分支、长连线和交叉线的真实复杂流程验证上下模式，不能只测试串行节点。

---

## 10. 运行时需求

### 10.1 按节点和区域保存渲染结果

运行时不能只使用一个扁平元素数组表达整页内容，至少需要保留节点边界并得到：

- 活动路径中已进入的唯一 Header；
- 活动路径中已进入的 Content 列表；
- 活动路径中已进入的唯一 Footer。

Footer 虽然属于流程节点，但渲染到页面壳的 Footer 区域，不加入 Content 元素列表。

### 10.2 页面布局

- Header 位于智能导航和正文之前；
- Content 是唯一主滚动区域；
- Footer 位于页面壳底部，不覆盖正文；
- Footer 高度变化时正文滚动区自动调整；
- Header/Footer 未进入或不可见时不显示空容器；
- Header 与导航的固定/滚动方式沿用引导式布局配置；
- Footer 第一版固定在页面壳底部，通过 Grid/Flex 实现，禁止由物料自身使用绝对定位覆盖正文。

### 10.3 BottomBanner 兼容

- Footer 的正式来源是节点 `presentation.region = footer`；
- 不能永久通过节点是否包含 `BottomBanner` 推断 Footer；
- `BottomBanner` 在 Footer 节点中只作为普通横向容器，定位由页面壳负责；
- 普通组装式页面与旧数据继续保留原布局行为；
- 旧 BottomBanner 数据若需迁移，只作为 Footer 候选提示，确认后写入显式区域配置。

### 10.4 异常处理

- Header/Footer 组件加载失败时，只在对应区域展示错误，不允许整个页面白屏；
- Footer 按钮动作失败时恢复按钮可操作状态并展示明确反馈；
- 操作失败时不能错误进入结束节点；
- Footer 加载失败不能覆盖正文；
- 技术异常不进入智能导航业务失败统计。

---

## 11. 数据要求

流程节点实例增加展示区域字段：

```text
presentation.region = header | content | footer
```

要求：

- 字段缺失或非法时按 Content 兼容；
- 区域字段属于流程节点实例，不能写回业务组件模板；
- Footer 仍保存在流程节点列表中，并保留正常 `nodeId/componentId`；
- Footer 按钮显隐和操作继续保存在 Footer 业务组件实例配置中；
- Footer 完成动作使用稳定语义标识，不能保存目标节点 ID；
- 保存接口返回后再次读取，区域和动作配置不能丢失；
- 主 `src` 与独立 `page` 使用相同默认值、拓扑校验和绑定语义。

本需求只冻结字段语义，不冻结具体接口 JSON 层级。

---

## 12. 兼容性要求

- 不配置 Header/Footer 的旧引导式流程保持原运行结果；
- 普通组装式页面不受影响；
- 旧节点缺少展示区域字段时按 Content 处理；
- 复制 Header/Footer 节点时，复制结果默认降级为 Content，避免唯一性冲突；
- 删除 Header/Footer 后对应区域自然清空；
- 节点新增、删除、复制、移动、连线、撤销重做和保存必须保留正确区域信息；
- 主 `src` 和独立 `page` 必须同步实现。

---

## 13. 验收标准

### 13.1 编辑器验收

- 新建节点默认是 Content；
- 任意业务节点可以设置为 Header 或 Footer；
- 同一流程不能保存两个 Header 或两个 Footer；
- 存在 Header 时，开始节点只能连接 Header；
- Footer 之后只能连接结束节点；
- 多条互斥路径可以汇合到同一个 Footer；
- Header/Footer 卡片区域标签正确；
- 画布可以在左右连线和上下连线之间切换；
- 切换连线方向后节点关系、分支条件和分支顺序保持不变；
- 保存并重新打开后配置正确恢复；
- Footer 内多个按钮可以分别配置显隐、动作和是否完成 Footer。

### 13.2 运行时验收

- 无 Header/Footer 的旧流程表现不变；
- Header 按流程进入后显示在顶部；
- Footer 按流程进入后显示在底部且不覆盖正文；
- Header/Footer 不进入智能导航及其统计；
- 修改前面 Content 选择后，Footer 整体或指定按钮显隐正确；
- 点击不同 Footer 按钮分别执行配置动作；
- 辅助按钮不会意外进入结束节点；
- 配置为完成 Footer 的按钮成功后沿唯一出线进入结束节点；
- 按钮操作失败时停留在 Footer，不错误结束；
- Footer 高度变化后正文仍能滚动到最后一个 Content；
- 重新开始流程时旧 Header/Footer 运行状态被清理；
- 主预览和独立运行页表现一致。

### 13.3 回归验收

- 普通组装式页面中的 BottomBanner 布局不变；
- 旧引导式数据可以打开、编辑、预览和保存；
- 流程节点新增、删除、复制、移动、连线、撤销和重做没有回归；
- Footer 内现有按钮、接口、变量赋值、事件和显隐能力正常工作。

---

## 14. 第一版测试场景

1. `开始 → Header → 自动 Content → 人工 Content → Footer → 结束`；
2. `开始 → Header → Content → 结束`；
3. `开始 → Content → Footer → 结束`；
4. `开始 → Content → 结束`，验证旧流程兼容；
5. 设置第二个 Header/Footer，验证替换与取消；
6. 尝试从开始节点绕过 Header，校验必须失败；
7. 尝试从 Footer 连接普通节点或配置条件出线，校验必须失败；
8. 两条互斥分支汇合到 Footer，再进入唯一结束节点；
9. Content 单选、多选或下拉值控制 Footer 整体和不同按钮显隐；
10. Footer 辅助按钮执行操作但不结束；完成按钮成功后进入结束节点；
11. Footer 操作失败、重复点击、内容高度变化和重新开始；
12. 同一份复杂流程分别切换左右、上下连线，检查连接点、箭头、分支文字和保存恢复；
13. 主 `src` 预览和独立 `page` 使用同一份流程数据对比结果。

---

## 15. 当前已确认与待确认

### 已确认

1. Header、Content、Footer 都是流程业务节点；
2. 普通业务节点默认是 Content；
3. 一个流程最多一个 Header 和一个 Footer；
4. 有 Header 时，开始节点直接连接 Header，Header 是首个业务节点；
5. 有 Footer 时，Footer 是最后一个业务节点，其后只能连接唯一结束节点；
6. Footer 是整个场景独立展示的按钮区域；
7. Footer 可以根据前面 Content 的结果控制整体或按钮显隐；
8. Footer 按钮点击后执行对应业务操作；
9. Header/Footer 不进入智能导航；
10. Footer 由页面布局容器定位，不能依赖 BottomBanner 绝对定位覆盖正文；
11. 主 `src` 与独立 `page` 同步交付。
12. 编辑画布支持整图左右/上下连线方向切换，方向不影响流程业务语义。

### 待评审确认

1. Header 第一版是否固定为自动加载并自动进入下一节点；
2. Footer 按钮通过什么配置标识“操作成功后完成 Footer 并进入结束节点”；
3. Footer 第一版读取 Content 数据时，先复用页面级兼容变量，还是同步引入节点正式输出；
4. 流程进入结束节点后，Footer 立即隐藏还是保留在完成页面；
5. Header/Footer 组件加载失败后的重试入口样式。
