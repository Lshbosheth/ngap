# 引导式流程分期实施总览与执行规划

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 引导式流程分期实施总览与执行规划 |
| 文档状态 | 当前开发执行总览基线 |
| 规划范围 | 引导式流程改造第一期至第六期 |
| 排期粒度 | 产品阶段 + 逐步任务 + 阶段关口；不代表具体自然日期 |
| 关联需求 | `GUIDED_PROCESS_HEADER_FOOTER_REQUIREMENTS(引导式Header与Footer第一期需求).md` |
| 一期开发方案 | `GUIDED_PROCESS_PHASE1_TECHNICAL_PLAN(引导式一期Header与Footer开发方案).md` |
| 二期开发方案 | `GUIDED_PROCESS_PHASE2_TECHNICAL_PLAN(引导式二期组件作用域与引用体系开发方案).md` |
| 三期开发方案 | `GUIDED_PROCESS_PHASE3_TECHNICAL_PLAN(引导式三期正式输出诊断与动态分支开发方案).md` |
| 四期开发方案 | `GUIDED_PROCESS_PHASE4_TECHNICAL_PLAN(引导式四期路径回滚与运行时稳定性开发方案).md` |
| 五期开发方案 | `GUIDED_PROCESS_PHASE5_TECHNICAL_PLAN(引导式五期画布体验与容量开发方案).md` |
| 六期开发方案 | `GUIDED_PROCESS_PHASE6_TECHNICAL_PLAN(引导式六期整体验收与推广准备开发方案).md` |
| 开发契约总表 | `GUIDED_PROCESS_DEVELOPMENT_CONTRACTS(引导式开发契约总表).md` |
| 开发前核查 | `GUIDED_PROCESS_PRE_DEVELOPMENT_CHECKLIST(引导式开发前技术核查清单).md` |
| 前后端契约 | `GUIDED_PROCESS_FRONTEND_BACKEND_CONTRACT(引导式前后端接口与数据契约).md` |
| 关联设计 | `GUIDED_PROCESS_REDESIGN(引导式流程展示编排升级设计).md` |
| 决策记录 | `GUIDED_PROCESS_PENDING_ITEMS(引导式待完善事项).md` |

本文既统一定义后续所说的“第一期、第二期……”分别代表什么，也规定从现在开始应该按什么顺序核查、评审、开发、联调和验收。详细设计文档中原有的技术阶段划分，可用于一期内部或后续各期的开发任务拆分，但不再作为产品版本顺序。

---

## 2. 总体顺序

```text
第一期：Header / Footer 页面骨架
→ 第二期：组件作用域与引用体系
→ 第三期：正式输出、诊断结果与动态分支
→ 第四期：后续路径回滚与运行时稳定性
→ 第五期：画布体验与容量
→ 第六期：整体验收与推广准备
```

排序原则：

1. 先让页面具备可使用的场景首尾区域；
2. 再解决多组件共同运行时的数据隔离和稳定引用；
3. 在稳定数据基础上让组件结果驱动诊断和分支；
4. 再完整解决前序结果变化后的换路和异步失效；
5. 业务运行模型稳定后优化大型流程编辑体验；
6. 最后进行双运行时、试点流程和复杂流程的整体验收。

### 2.1 最终执行链

实际执行不是直接从“一期写代码”开始，而是：

```text
开发准备
  → 真实代码与后端链路核查
  → 当前阶段契约冻结
  → 修正当前阶段任务与工时
  → 共享纯逻辑和后端数据往返
  → 编辑器 + 主 src + 独立 page 纵向实现
  → 自动化测试和双入口联调
  → 阶段验收门
  → 下一期
```

每一期都重复“核查—冻结—实现—联调—验收”，不能把六份方案当成一次性全部编码清单。

### 2.2 项目角色

当前排期按一名前端、一名后端估算。正式启动前还必须明确以下参与者：

| 角色 | 必须承担的工作 |
| --- | --- |
| 产品/需求 | 确认冻结规则、验收结果、范围变化和已知限制 |
| 前端开发 | 编辑器、共享 Runtime、主 src、独立 page、测试和文档回填 |
| 后端开发 | sceneData/组件定义往返、发布校验、引用索引、历史/复制和联调 |
| 组件/配置开发 | 提供试点组件、真实接口、事件脚本、输出映射和业务验收数据 |
| 业务验收人 | 验证真实场景的结论、按钮动作和错误提示 |
| 项目负责人 | 决定阶段关口是否通过、阻断问题是否允许进入下一期 |

如果暂时没有独立测试人员，前后端承担技术测试，但产品和业务验收不能由开发自行替代。

### 2.3 每一期固定五道关口

| 关口 | 要做什么 | 通过证据 | 未通过时 |
| --- | --- | --- | --- |
| G-A 开发准入 | 按开发前核查清单检查真实代码、接口、环境和测试数据 | 启动记录、调用点清单、接口样例、阻断项负责人 | 不开始大范围编码 |
| G-B 契约冻结 | 冻结当前 Schema、稳定身份、职责边界、错误和迁移方式 | 契约评审记录、字段样例、修正后的工时 | 先更新方案和契约总表 |
| G-C 功能完成 | 按本期 T1～Tn 实现编辑器、共享逻辑和必要后端能力 | 单元测试、代码审查、保存回读证据 | 留在本期修复 |
| G-D 双入口联调 | 同一流程验证主 src、独立 page 和后端发布数据 | 双入口对比、接口往返、普通页面回归 | 不得以主预览完成代替本期完成 |
| G-E 阶段验收 | 关闭本期完成门槛和阻断缺陷 | 阶段验收记录、已知限制、下一期输入清单 | 不进入下一期主干开发 |

每个阶段结束时必须更新：

- 当前阶段详细方案中的真实核查结论；
- `GUIDED_PROCESS_DEVELOPMENT_CONTRACTS(引导式开发契约总表).md`；
- `GUIDED_PROCESS_PRE_DEVELOPMENT_CHECKLIST(引导式开发前技术核查清单).md` 的状态和证据；
- `GUIDED_PROCESS_FRONTEND_BACKEND_CONTRACT(引导式前后端接口与数据契约).md` 的真实后端落点；
- 本总览的当前执行位置、实际工时和下一阶段输入；
- `CODEX_CONTEXT.md`。

### 2.4 从现在开始的逐步执行表

以下步骤就是推荐的实际实施顺序。任务名称引用各期详细方案，不另建一套重复任务编号。

| 步骤 | 阶段/任务 | 主要动作 | 主要交付物 | 进入下一步的条件 |
| ---: | --- | --- | --- | --- |
| 0 | 项目启动 | 明确前端、后端、产品、组件配置和验收人；准备主 src/page/后端环境 | 人员表、环境表、沟通和缺陷记录入口 | 负责人和联调环境明确 |
| 1 | 文档基线 | 阅读契约总表、一期需求、一期方案和开发前核查清单 | 阅读/评审问题清单 | 没有把旧原型当成完成代码 |
| 2 | 一期 T1 / G-A | 核查 `ProcessCanvas`、`CanvasTop.saveData()`、主 `ProcessPage`、独立 page、BottomBanner 和真实后端链 | 调用点清单、后端链路图、接口样例 | 保存/查询/历史/发布事实明确 |
| 3 | 一期 T2 / G-B | 冻结 Schema v1、稳定 nodeId、region、拓扑错误和共享纯逻辑边界 | v1 字段样例、迁移规则、测试骨架 | 前后端确认同一语义 |
| 4 | 一期 T3/T4/T5 | 部分并行实现编辑器区域配置、Header 等待通道、后端透传与发布校验 | 编辑器功能、Header barrier、完整字段往返 | 三条链分别有专项测试 |
| 5 | 一期 T6 | 主 src 实现 Header 前置、Footer 固定、Content 启动和错误处理 | 主预览可运行简单流程 | 主入口场景通过 |
| 6 | 一期 T7 | 同步接入独立 page；不复制第二套算法 | 独立运行页同场景结果 | 与主 src 关键快照一致 |
| 7 | 一期 T8 | 处理 Footer 布局和 BottomBanner 兼容 | 引导式/普通页面回归记录 | 普通组装式页面无回归 |
| 8 | 一期 T9 / G-D/G-E | 保存、关闭、重开、历史、发布、双入口、真实简单试点验收 | 一期验收记录、试点流程、已知限制 | G1：可开发简单流程 |
| 9 | 二期 P2-T1/T2 / G-A/G-B | 核查两套 Store、变量/脚本/引用；冻结 v2、variableId/globalVariableId | 数据读写清单、v2 契约、后端索引方案 | 作用域和引用身份冻结 |
| 10 | 二期 P2-T3 | 实现节点 Runtime Store、引用解析、精确订阅和 dispose 基础 | 无 UI Runtime contract tests | 节点域纯逻辑通过 |
| 11 | 二期 P2-T4 | 主 src 和 materials/page 接入节点上下文与旧物料 adapter | 同模板多实例隔离 | 双入口不串值 |
| 12 | 二期 P2-T5/T6 | 实现结构化选择器、依赖图、影响面板和发布强校验 | 引用配置与影响查询 | 删除/改类型能准确阻断 |
| 13 | 二期 P2-T7 | 把一期试点流程 v1→v2，人工确认歧义 | 迁移报告、升级草稿 | Header/Footer 和引用均正常 |
| 14 | 二期 P2-T8 / G-D/G-E | 双入口、普通页面和一期回归 | 二期验收记录 | G2：稳定作用域和引用可供三期使用 |
| 15 | 三期 P3-T1/T2 / G-A/G-B | 核查 MT/AT/VA，冻结 v3、outputId、结果和迁移矩阵 | v3 契约、输出定义保存回读 | 输出身份和类型冻结 |
| 16 | 三期 P3-T3/T4 | 实现输出映射、value/label、完成策略和原子结果发布 | 人工/自动结果链路 | 结果可稳定计算且去重 |
| 17 | 三期 P3-T5/T6 | 实现 transitionId、唯一/默认分支和智能导航诊断 | 分支试算、诊断展示、发布校验 | 结果正确、分支唯一 |
| 18 | 三期 P3-T7/T8 / G-D/G-E | v2→v3、双入口和降档首次选择验收 | 三期验收记录 | G3：结果算对、分支选对；尚不承诺已加载旧路释放 |
| 19 | 四期 P4-T1/T2 / G-A/G-B | 核查旧数组截断、异步和页签；冻结 v4 执行身份与动作锁 | 运行协议、竞态用例 | process/node/action 身份冻结 |
| 20 | 四期 P4-T3 | 实现 activeExecutionPath、activeContentPath 和失效后缀计算 | 路径纯逻辑测试 | 分歧和释放范围正确 |
| 21 | 四期 P4-T4/T5 | 实现逆序 dispose、Abort/旧写拒绝、共享值撤销和原子换路 | 路径协调器、资源测试 | 快速改选只保留最后路径 |
| 22 | 四期 P4-T6/T7 | 接入 onNodeLoaded、刷新、手机号变化、普通页签和双入口 | 生命周期测试、有效轨迹上下文 | 三类生命周期符合冻结规则 |
| 23 | 四期 P4-T8 / G-D/G-E | 做 8→28→49以上、竞态、泄漏和普通页面回归 | 四期验收记录 | G4：正常可改选业务试点条件具备 |
| 24 | 五期 P5-T1 / G-A/G-B | 准备真实110、模拟150、重组件/轻组件四基准；采集当前性能 | 基线报告、正式指标 | 数据和操作脚本可复现 |
| 25 | 五期 P5-T2 | 拆分轻量拓扑摘要和完整组件详情 | CanvasNode/Edge 摘要、详情缓存 | 画布不携带完整 componentData |
| 26 | 五期 P5-T3/T4 | 局部渲染/连线、rAF 拖动、历史和序列化优化 | 优化后性能数据 | 核心操作达到或接近门槛 |
| 27 | 五期 P5-T5 | 实现左右/上下连接点和线路由 | 方向保存回读和视觉回归 | 业务拓扑 JSON 不变 |
| 28 | 五期 P5-T6 条件步骤 | 只有现有画布仍未达标才做视口降级或候选库原型 | 同数据对比报告 | 决定保留现有画布或另立迁移计划 |
| 29 | 五期 P5-T7 / G-D/G-E | 完整复测110/150、重组件、内存、保存和普通页 | 性能和选型结论 | G5：已验证容量范围明确 |
| 30 | 六期 P6-T1 / G-A | 冻结最终版本、环境、账号、组件和 S1～S8 数据 | 验收基线 | 前五期完成证据齐全 |
| 31 | 六期 P6-T2/T3 | 全链路组合回归和双入口快照对比 | 问题清单、一致性报告 | 跨期阻断问题定位完成 |
| 32 | 六期 P6-T4/T5 | 试点升级发布、110/150和普通页最终回归 | 试点发布报告、容量复核 | 正式场景可重复运行 |
| 33 | 六期 P6-T6 | 完成配置手册、组件接入、错误、限制和支持说明 | 完整交付文档 | 配置人员独立演练通过 |
| 34 | 六期 P6-T7 / G-D/G-E | 关闭阻断缺陷、全量冒烟、确认首批场景和责任人 | 最终验收结论 | G6：进入正式使用 |

### 2.5 六个里程碑的可用程度

| 里程碑 | 达成后能做什么 | 仍不能承诺什么 |
| --- | --- | --- |
| G1 一期完成 | 搭建 Header/Footer 和简单流程试点 | 完整节点隔离、正式输出、任意改选 |
| G2 二期完成 | 通用组件多实例隔离、稳定引用、删除保护 | 正式诊断和完整动态分支 |
| G3 三期完成 | 组件结果、导航诊断和首次分支选择正确 | 已加载后再改选的旧路径完整释放 |
| G4 四期完成 | 前序改选后安全换路，可做正常业务试点 | 110/150 节点编辑器容量承诺 |
| G5 五期完成 | 明确可支持的大流程容量和两种方向 | 全项目正式推广验收结论 |
| G6 六期完成 | 第一批场景正式使用条件具备 | 本轮明确排除的 P2 和平台级能力 |

### 2.6 各阶段累计周期

以下为单前端顺序开发的累计日历范围，不是承诺日期：

| 完成位置 | 本期周期 | 从项目启动累计 |
| --- | ---: | ---: |
| 一期完成 | 6～8 周 | 6～8 周 |
| 二期完成 | 7～9 周 | 13～17 周 |
| 三期完成 | 6～8 周 | 19～25 周 |
| 四期完成 | 6～9 周 | 25～34 周 |
| 五期完成 | 5～7 周 | 30～41 周 |
| 六期完成 | 3～5 周 | 33～46 周 |

一期前两周的开发前核查已经包含在一期 6～8 周内；不要再次在总周期外重复增加。若后端开发同样不熟悉项目或无法及时取得真实后端工程，则按第 10 章另加缓冲。

### 2.7 开发过程中的固定节奏

- 每周开始：确认本周对应详细方案任务、验收点和阻断依赖；
- 每个字段落地：先验证保存—查询—历史—发布往返，再继续堆 UI；
- 每个共享 Runtime 语义完成：同周接主 src 和独立 page 冒烟；
- 每周结束：更新核查清单证据、实际工时、风险和下周入口；
- 每期中段：用贯穿试点流程演示，不等全部代码结束才首次联调；
- 每期结束：执行 G-D/G-E，不通过不得把问题推到第六期；
- 发现新增需求：先判断属于当前冻结范围、后续既定阶段还是 P2/新立项，再决定是否调整排期。

### 2.8 执行责任速查

| 工作段 | 主责 | 必须配合 | 说明 |
| --- | --- | --- | --- |
| 步骤0～3：启动、核查、契约 | 前端 + 后端 | 产品、组件配置 | 前端不能独自猜后端；产品不重新发散冻结范围 |
| 步骤4～8：一期实现验收 | 前端 | 后端、组件配置、产品验收 | 后端负责字段往返和发布校验；双入口同一期完成 |
| 步骤9～14：二期 | 前端 | 后端、试点配置 | 后端负责跨流程引用索引；配置人员确认迁移歧义 |
| 步骤15～18：三期 | 前端 | 后端、组件开发、产品验收 | 组件开发负责内部计算和业务状态映射 |
| 步骤19～23：四期 | 前端 | 后端、组件开发 | 后端确认幂等边界；组件接入 dispose/异步规范 |
| 步骤24～29：五期 | 前端 | 后端、旧流程资料提供者 | 后端提供数据和体积/耗时；是否换库由基准决定 |
| 步骤30～34：六期 | 前端 + 后端 | 产品、配置开发、业务验收、负责人 | 不能由开发自行宣布正式推广 |

### 2.9 范围或进度变化时怎么处理

| 情况 | 处理方式 |
| --- | --- |
| 实际代码与方案假设不同 | 当前步骤暂停，更新核查证据、详细方案和工时后继续 |
| 当前阶段内的普通缺陷 | 留在本期修复，不改变总体阶段 |
| 属于后续既定阶段的需求 | 记录到对应阶段，不提前混入当前实现 |
| 属于待完善事项 P2 | 本轮不做，除非产品明确重新立项 |
| 新增跨期数据字段 | 先更新契约总表和前后端契约，再编码 |
| 后端暂时拿不到 | 可继续只读前端核查和技术验证，但 G-B 前必须取得后端结论 |
| 第五期现有画布不达标 | 先形成同基准报告，再新增画布迁移计划和工时 |
| 阶段工时超出原估算 | 说明原因、剩余范围和新预测，不通过削减测试来维持旧数字 |

---

## 3. 第一期：Header / Footer 页面骨架

### 3.1 目标

先建立引导式页面的场景级顶部信息区和底部操作区，使配置人员能够搭建并运行简单业务流程。

### 3.2 范围

- Header、Content、Footer 三类展示区域；
- 普通业务节点默认归一化为 Content；
- Header/Footer 各最多一个；
- `开始 → Header` 和 `Footer → 结束` 的首尾拓扑约束；
- Header 页面启动时先完成接口和组件初始化，再启动 Content；
- Header 可以通过当前已有能力写入显式全局变量，供后续 Content 使用；
- Footer 页面启动时固定挂载，根据流程进行中的数据控制内部元素显隐或禁用；
- Footer 按钮执行各自业务动作，不推进流程；
- 活动路径到达 Footer 后自动进入唯一结束节点；
- 流程结束后 Header/Footer 保留至页面卸载或整体重启；
- Header/Footer 不进入智能导航；
- 编辑器区域配置、唯一性检查、连线限制和发布校验；
- `BottomBanner` 在引导式 Footer 中由页面壳负责定位，同时回归普通组装式页面；
- 主 `src` 预览与独立 `page` 运行页保持一致；
- 流程定义提前保存稳定 `nodeId` 和 `guidedSchemaVersion`。

### 3.3 过渡方案

第一期尚未实现完整组件作用域，简单试点流程暂时复用当前平台已有的：

- 页面级变量；
- 事件与脚本赋值；
- 数据绑定；
- 元素显隐、禁用和按钮动作。

第一期不得再引入新的按名称猜测引用方式。试点流程数量应保持可控，并确保后续可以依靠稳定 `nodeId + guidedSchemaVersion` 进行轻量升级。

### 3.4 本期不包含

- 完整组件节点作用域；
- 正式组件输出体系；
- 输出驱动的完整动态分支；
- 通用分支回滚引擎；
- 大流程性能和连线方向优化；
- P2 项目。

### 3.5 完成门槛

- `GUIDED_PROCESS_HEADER_FOOTER_REQUIREMENTS(引导式Header与Footer第一期需求).md` 的编辑器、运行时及回归验收全部通过；
- 至少用一个简单真实流程跑通 Header 公共数据、自动/人工 Content、Footer 显隐和按钮业务动作；
- 保存、关闭、重新打开后区域、节点身份、流程版本和组件配置不丢失；
- 主 `src` 和独立 `page` 对同一流程定义表现一致；
- 普通组装式页面中的 `BottomBanner` 无回归。

---

## 4. 第二期：组件作用域与引用体系

### 4.1 目标

解决多个组件同时存在时的变量污染、同名覆盖、重复模板实例串值，以及组件删除或修改后引用失效的问题，为正式输出和分支回滚建立稳定数据基础。

### 4.2 范围

- 每个流程节点实例拥有独立组件变量作用域；
- 同一通用组件模板多次使用时，以不同 `nodeId` 隔离数据；
- 当前组件可以读取和修改自身组件变量；
- 组件可以读取已定义的显式全局变量，并通过明确动作写入允许的全局变量；
- 跨组件引用使用稳定结构化身份，底层至少包含 `nodeId + variableId`；
- 配置界面可以按组件/环节名称选择数据，但保存时不得只保存易变名称；
- 分支、Footer 显隐、接口参数、输入绑定、变量赋值和导航字段统一登记结构化引用；
- 维护反向依赖关系，能够显示某变量被哪些配置使用；
- 删除节点、删除变量、改变变量类型或修改关键定义时立即校验受影响引用；
- 保存时执行全量引用检查，发布时阻断未解决错误；
- 校验变量在每条可达路径上的赋值可达性；
- Footer 响应式显隐允许依赖值初始未赋值，但必须配置默认行为；
- 事件脚本声明变量读取和写入信息，纳入依赖校验；
- 清理节点时能够释放该节点作用域中的表单、接口、变量和订阅数据。

### 4.3 第一期试点流程升级

- 保留原节点、连线、Header/Footer/Content 区域和组件实例配置；
- 将能明确判断归属的组件变量迁移到对应节点作用域；
- 明确的跨组件引用转换为结构化引用；
- 显式全局变量继续保留为全局变量；
- 来源不明确或存在同名冲突时列出清单，由配置人员确认；
- 不静默按名称猜测或重新绑定；
- 升级后必须通过引用完整性校验才能重新发布；
- 因试点流程较少，允许人工重建个别组件或连线，不建设重型迁移系统。

### 4.4 本期不包含

- 完整正式输出和诊断协议；
- 输出变化后的通用后续路径回滚；
- 大流程画布专项。

### 4.5 完成门槛

- 两个节点分别使用同名组件变量时互不影响；
- 同一通用组件模板多次使用时数据互不串联；
- 组件删除、变量删除和类型变化可以准确列出受影响引用；
- 有阻断引用错误的流程不能发布；
- 第一期试点流程完成轻量升级并保持原有 Header/Footer 行为；
- 主 `src` 与独立 `page` 的作用域读取和写入语义一致。

---

## 5. 第三期：正式输出、诊断结果与动态分支

### 5.1 目标

让每个业务环节产生稳定、可展示、可判断的正式结果，并由该结果驱动智能导航和后续分支。

### 5.2 范围

- 组件对外声明正式输出字段；
- 人工环节提供用于流程判断的 `value` 和用于智能导航展示的 `label`；
- `label` 可以比 `value` 包含更多用户可读信息；
- 自动环节从接口返回数据或独立状态字段形成业务成功/失败诊断；
- 业务成功/失败暂按绿色/红色展示，与接口技术异常区分；
- 人工环节根据用户点选或输入结果展示诊断结论；
- 组件内部通过 `change + 事件脚本 + 组件变量赋值` 动态计算结果；
- 任一相关字段变化时可以重新计算正式输出；
- 分支条件直接读取稳定正式输出，不依赖组件内部 DOM 或临时 React state；
- 多条条件出线必须唯一命中；
- 支持明确的默认分支和未完成时不推进；
- 自动环节完成后静默判断并连续执行后续自动环节；
- 人工环节在其定义的有效交互结果产生后继续；
- 智能导航在环节卡片下展示最新有效诊断结果。

### 5.3 代表性验收例子

“意向降档金额 + 意向降档套餐”组件：

- 两项都为 8 元时输出 `PLAN_8`；
- 业务金额大于 8 且小于 49 时输出 `PLAN_8_49`；
- 业务金额大于等于 49 时输出 `PLAN_49_PLUS`；
- 任一必需字段不完整或校验失败时不产生可推进结果；
- 智能导航展示用户可理解的 `label`，分支只匹配稳定 `value`。

### 5.4 与第四期的边界

本期负责“算出最新结果并选出应该走的分支”。如果用户已经走入后续路径后再修改前序结果，对旧路径组件、数据和异步请求进行完整释放，属于第四期。

在第四期完成前，第三期不能作为支持复杂可反复改选流程的最终版本推广；可以先在不会回改前序结果或由业务限制修改的试点流程中验证输出和分支。

### 5.5 完成门槛

- 自动和人工环节都能产生统一格式的正式结果；
- 智能导航正确展示业务成功/失败及 `label`；
- 分支稳定读取 `value`，并保证一次只命中一个出口；
- 自动节点连续执行过程中不会无故停顿；
- 代表性降档例子在首次选择时正确进入三条分支；
- 技术异常直接停止并报错，不被误显示为业务失败。

---

## 6. 第四期：后续路径回滚与运行时稳定性

### 6.1 目标

支持用户在已经加载后续环节后修改前序选择，并安全地清理旧路径、加载新路径，保证页面最终只保留当前有效轨迹。

### 6.2 范围

- 前序正式输出变化后重新评估分支；
- 结果不变时不重复加载后续路径；
- 结果变化时从分歧点清除旧后续路径；
- 卸载旧路径的 Content/Control 组件、节点变量、表单数据、接口结果和订阅；
- 取消或逻辑废弃旧路径尚未完成的异步请求；
- 为有效动作生成执行 ID，拒绝旧异步结果回写；
- 加载新分支并继续自动执行，直到人工环节、结束节点或技术异常；
- Header/Footer 不随分支回滚卸载；
- Footer 根据回滚后的最新有效数据重新计算；
- 维护只读 `activeContentPath`，表示当前已经加载且最终仍成立的 Content 轨迹；
- `activeContentPath` 不包含 Header、Footer、Control 和已经回滚的历史路径；
- 增加节点实例级 `onNodeLoaded` 事件；
- `onNodeLoaded` 在组件及必需初始化/接口完成后触发，并可读取当前节点数据和当前有效轨迹；
- 平台不保存业务日志，业务模块通过自身接口记录环节加载、人工点选或按钮动作；
- 人工操作和 Footer 按钮使用实例级动作锁，防止重复提交。

### 6.3 代表性验收例子

用户先选择 8 元并进入 `PLAN_8` 后续路径，随后把金额改成 28 元：

```text
PLAN_8 后续路径整体失效并释放
→ 当前组件输出变为 PLAN_8_49
→ 重新匹配第二条分支
→ 加载 PLAN_8_49 后续路径
→ activeContentPath 只保留新路径
```

### 6.4 完成门槛

- 前序值改变后页面不残留旧分支组件和数据；
- 旧异步结果不能覆盖新路径状态；
- 输出未变化时不会重复加载或重复请求；
- Header/Footer 在换路过程中保持挂载且状态正确；
- `activeContentPath` 始终反映当前最终态；
- `onNodeLoaded` 的触发时机和事件上下文稳定；
- 降档例子完成 `8 → 28 → 49以上` 的连续改选回归。

---

## 7. 第五期：画布体验与容量

### 7.1 目标

保证包含大量节点、节点内部元素和复杂连线的流程仍可正常打开、编辑和保存，并改善横向、纵向流程的可读性。

### 7.2 容量与性能范围

- 单流程业务节点上限暂定 150 个；
- 容量评估同时统计节点数、节点内部元素数、组件配置体积、连线数和分支复杂度；
- 画布节点使用轻量摘要，不在编辑画布中完整渲染业务组件；
- 节点状态隔离，避免单个节点变化导致整图刷新；
- 拖动节点时只更新相关连线；
- 优化大对象深拷贝、序列化、撤销重做和保存过程；
- 节点和连线按可视区域或合理策略降低非必要渲染成本；
- 使用真实约 110 节点流程和模拟 150 节点流程建立统一基准；
- 先优化当前画布实现，不预先锁定 React Flow、X6 或 LogicFlow；
- 若当前实现优化后仍未达到验收目标，再使用同一数据对候选开源库做原型对比。

### 7.3 布局与连线方向范围

- 流程级支持“左右”和“上下”两种连线方向；
- 切换方向只改变连接点和线路径，不改变节点、分支或执行语义；
- 分支出口、箭头和文字在两种方向下保持对应；
- 保存并重新打开后恢复所选方向；
- 旧流程缺少配置时沿用当前左右模式；
- 第一版不因切换方向自动重排节点，自动布局作为未来独立能力。

### 7.4 完成门槛

- 真实约 110 节点和模拟 150 节点数据能够完成打开、缩放、拖动、连线、配置、保存和重开；
- 节点内部元素较多时仍按照相同基准单独验证；
- 普通拖动不触发不必要的整图连线重算；
- 左右/上下切换不改变流程业务语义；
- 是否保留当前画布或替换开源库，有同数据、同操作、同指标的测试结论。

---

## 8. 第六期：整体验收与推广准备

### 8.1 目标

把前五期能力形成一套可交付、可配置、可回归的完整引导式流程方案。

### 8.2 范围

- Header/Footer、组件作用域、正式输出、诊断、分支和回滚全链路联调；
- 主 `src` 预览与独立 `page` 运行页对比；
- 第一期试点流程升级后的完整回归；
- 简单流程、复杂分支流程和大节点流程分别验收；
- 浏览器刷新、手机号变化和普通页签切换生命周期验证；
- 技术异常、配置错误、引用失效和发布阻断验证；
- 普通组装式页面及 `BottomBanner` 回归；
- 配置说明、错误文案、试点使用说明和验收记录收口；
- 确认第一批正式使用场景及配置人员。

### 8.3 完成门槛

- 各期完成门槛全部关闭；
- 同一流程在两套运行入口中的关键结果一致；
- 试点流程升级后没有变量串值、旧路径残留或 Footer 状态错误；
- 150 节点容量目标有可复现的测试结论；
- 配置人员能够依靠文档独立完成简单流程搭建和错误修复；
- 正式使用范围和已知限制明确。

---

## 9. 阶段依赖与并行关系

| 阶段 | 必须依赖 | 可以提前准备 | 不建议提前合并 |
| --- | --- | --- | --- |
| 第一期 | 已冻结的 Header/Footer 需求 | 双运行时代码核查、测试数据 | 完整作用域和回滚引擎 |
| 第二期 | 稳定 `nodeId + guidedSchemaVersion` | 第一期试点迁移清单 | 正式分支逻辑 |
| 第三期 | 第二期稳定作用域与引用 | 输出字段 UI 原型、真实接口样例 | 完整回滚实现 |
| 第四期 | 第三期正式输出与分支 | 异步取消、动作锁技术验证 | 画布性能重构 |
| 第五期 | 流程数据模型基本稳定 | 110/150 节点测试数据准备 | 运行时业务规则调整 |
| 第六期 | 前五期完成 | 验收文档持续维护 | 新增大范围产品能力 |

第三期和第四期可以作为一个大版本连续开发，但应分开验收：第三期确认“结果是否算对、分支是否选对”，第四期确认“改选后旧路径是否清干净、新路径是否稳定”。

第五期的基准数据可以提前准备，但画布底层重构不宜与第二至第四期的数据模型和运行时重构同时大范围开展，以减少定位问题的复杂度。

---

## 10. 预计开发时间

### 10.1 估算口径

本节按照以下实际团队条件估算：

- 1 名前端开发，刚进入项目，对现有代码结构和两套运行入口均不熟悉；
- 1 名后端开发，能够访问实际后端工程、数据库和联调环境；
- 暂无独立测试人员，开发人员需要承担单元测试、联调验证和主要回归工作；
- 1 人日按 8 小时计算，1 周按 5 个工作日计算；
- 各期需求在开始开发前已经达到本文所述的冻结程度，不包含等待产品确认的时间；
- 以 Repomix 导出的原始项目代码为开发基线；当前工作区中已经出现的 Header/Footer 原型代码只作为技术参考，不按“已经完成”抵扣正式开发工作量；
- 后端代码当前不在本工作区，因此后端工时属于方案级预估。完成实际接口、DTO、历史版本和发布链路核查后，允许在当前范围内上下调整约 30%；
- 日历周期已经包含代码熟悉、自测、双运行入口回归、联调和一般问题修复，不包含排队发布、跨部门等待及生产变更窗口；
- P2 项目、移动端、可访问性、重型旧流程自动迁移和新增业务组件不在本次估算内。

如果后端开发同样是刚进入项目且不熟悉保存、发布、历史版本等链路，建议在总计划前额外增加 2～3 周后端核查和联调缓冲。

### 10.2 六期汇总估算

| 阶段 | 前端工作量 | 后端工作量 | 预计日历周期 | 估算说明 |
| --- | ---: | ---: | ---: | --- |
| 第一期：Header / Footer | 26～34 人日 | 6～10 人日 | 6～8 周 | 前端工作量集中在编辑器、固定区域运行时、双入口同步和回归；后端主要确认并补齐流程定义透传、校验和历史版本兼容 |
| 第二期：组件作用域与引用 | 27～36 人日 | 8～12 人日 | 7～9 周 | 涉及变量存储模型、结构化引用、反向依赖、删除/修改校验和一期试点流程升级 |
| 第三期：正式输出与动态分支 | 22～30 人日 | 10～15 人日 | 6～8 周 | 涉及输出配置、事件计算、诊断展示、条件判断，以及输出和业务接口数据契约 |
| 第四期：路径回滚与稳定性 | 24～33 人日 | 6～10 人日 | 6～9 周 | 涉及运行状态机、旧路径释放、异步失效、动作锁、有效轨迹和节点加载事件 |
| 第五期：画布体验与容量 | 20～30 人日 | 2～5 人日 | 5～7 周 | 先优化现有画布并完成 110/150 节点基准；不包含确定更换开源画布库后的整体迁移 |
| 第六期：整体验收与推广 | 12～18 人日 | 6～10 人日 | 3～5 周 | 包含全链路联调、双入口对比、试点升级、容量回归、配置说明和验收问题收口 |
| **合计** | **131～181 人日** | **38～62 人日** | **约 33～46 周** | 以前端单人顺序开发为主要关键路径，约为 **8～11 个月** |

说明：日历周期不能简单用前后端人日相加。后端契约确认、接口调整和测试数据准备可以与部分前端工作并行，但单名前端需要连续处理编辑器、主 `src` 预览和独立 `page` 运行页，因此前端仍是整体排期的主要关键路径。

### 10.3 各期工作量构成

#### 第一期

前端 26～34 人日主要包括：

- 原始代码、保存链路和两套运行入口核查：3～4 人日；
- `guidedSchemaVersion`、稳定 `nodeId`、区域数据契约及兼容读取：2～3 人日；
- 编辑器区域配置、唯一性、替换确认、连线限制及发布前完整校验：6～8 人日；
- 主 `src` 预览中的 Header 前置初始化、Footer 固定挂载和 Content 启动顺序：5～6 人日；
- 独立 `page` 运行页的等价实现及公共纯逻辑抽取：4～5 人日；
- Footer 页面布局、`BottomBanner` 作用域样式和普通组装式页面回归：2～3 人日；
- 单元测试、联调、自测、问题修复和文档补充：4～5 人日。

后端 6～10 人日主要包括：

- 核查保存、查询、复制、历史版本和发布链路是否完整保留新增字段：2～3 人日；
- 调整 DTO/序列化、版本兼容和必要的发布校验：2～4 人日；
- 准备联调数据、接口联调和缺陷修复：2～3 人日。

#### 第二期

前端 27～36 人日主要包括组件实例作用域、变量读写 API、结构化引用选择器、反向依赖展示、删除/修改校验、保存/发布检查、双运行时适配及一期流程升级。后端 8～12 人日主要包括作用域数据持久化、引用完整性校验、流程版本升级、历史数据兼容和联调支持。

#### 第三期

前端 22～30 人日主要包括正式输出配置、`value/label` 计算和更新、自动/人工环节诊断展示、分支条件配置、自动连续执行及双入口回归。后端 10～15 人日主要包括输出数据契约、自动环节状态字段、配置校验、业务接口样例和联调支持。

#### 第四期

前端 24～33 人日主要包括分支重算、分歧点识别、旧路径卸载、组件作用域释放、异步执行 ID、动作锁、`activeContentPath` 和 `onNodeLoaded`。后端 6～10 人日主要包括异步请求配合、幂等/防重支持、业务记录事件所需上下文和联调缺陷处理。

#### 第五期

前端 20～30 人日主要包括性能基准、渲染隔离、连线局部更新、序列化与撤销重做优化、可视区域策略、左右/上下方向切换和 110/150 节点验证。后端 2～5 人日主要用于大流程数据准备、保存体积和接口耗时核查。

如果同数据基准证明当前画布无法达到目标，并最终决定整体替换为 React Flow、X6、LogicFlow 或其他开源库，应单独增加一次迁移评估；初步建议额外预留前端 15～25 人日、日历 3～5 周，不应直接包含在当前第五期承诺中。

#### 第六期

前端 12～18 人日和后端 6～10 人日用于前五期能力的组合回归，不再安排新的大范围产品能力。重点是修复跨期组合问题、完成双入口一致性验证、试点流程升级、150 节点结论、配置说明和正式验收记录。

### 10.4 排期使用原则

1. 每一期开始前，应把该期详细方案拆成可验收任务，再用实际代码核查结果修正本节估算；
2. 每一期至少保留约 20% 的问题修复和回归时间，不能把全部工期都排成功能编码；
3. 主 `src` 预览完成不代表该功能完成，独立 `page` 运行页必须在同一期同步通过；
4. 第一期原型代码只有在逐项代码审查、数据往返验证和双入口测试通过后，才可以复用到正式实现；
5. 第二至第四期属于连续的数据和运行时改造，不建议为了压缩日历时间由同一名前端交叉并行开发；
6. 第五期是否更换画布库必须以统一性能基准决定，不能在未测试当前实现前直接把迁移工时压入原计划。

---

## 11. 当前执行位置

当前状态：

- P0/P1 产品规则已基本完成；
- P2 当前整体暂不考虑；
- Header/Footer 第一期需求已经冻结；
- 第一至第六期详细开发方案、开发契约总表、开发前技术核查清单和前后端契约已经完成；
- 总路线已经拆成步骤0～34及G-A～G-E关口；
- 尚未开始主干代码实现；
- 当前位于“步骤0 项目启动 / 步骤1 文档基线”位置，下一项实质工作是“步骤2 一期T1真实技术核查”。

### 11.1 下一次启动时严格执行

1. 明确一名前端、一名后端、产品确认人、试点组件/配置人员和验收人；
2. 确认前端主工程、独立 `page`、实际后端工程和联调环境均可访问；
3. 在 `GUIDED_PROCESS_PRE_DEVELOPMENT_CHECKLIST(引导式开发前技术核查清单).md` 新建一期启动记录；
4. 按一期 T1 核查 `ProcessCanvas`、`CanvasTop.saveData()`、主 `ProcessPage`、独立 page、BottomBanner 和保存/查询/历史/发布链；
5. 取得 `/app/saveAppInfo`、`/app/queryAppAndNodeInfo`、`/app/queryAppAndNodeInfo2` 等真实请求响应，并回填前后端契约；
6. 输出“符合/部分符合/不符合/阻断”清单和修正后一期工时；
7. 进入 G-B，评审 Schema v1、稳定 nodeId、presentation.region、Header 可等待初始化和后端透传；
8. 只有 G-A/G-B 都通过后，才按步骤4开始一期 T3/T4/T5 编码。

### 11.2 一期启动所需最小输入

- 当前前端仓库和可运行方式；
- 独立 page 的运行/构建方式；
- 实际后端工程或后端开发支持；
- 已知接口请求响应样例；
- 一个简单真实试点流程；
- 一个可执行初始化并写公共数据的 Header 组件；
- 一个自动 Content 和一个人工 Content；
- 一个包含显隐/禁用与业务按钮动作的 Footer；
- 普通组装式 BottomBanner 回归页面；
- 测试账号、手机号变化触发条件和发布权限。

输入不齐时可以继续核查和制作最小模拟数据，但不能把模拟验证写成正式联调通过。

### 11.3 一期结束时必须交给二期的内容

- 稳定 nodeId 和 guidedSchemaVersion v1；
- 保存、查询、历史、复制和发布完整往返证据；
- Header/Footer/Content 节点和拓扑定义；
- 可复用的共享图、归一化和校验逻辑；
- 主 src 与独立 page 的宿主适配边界；
- 第一期试点流程原定义、已发布定义和验收记录；
- 试点流程使用到的页面变量、事件、接口和引用清单；
- 一期已知限制和二期迁移歧义候选清单。

二期不得重新生成一期节点身份，也不得要求把试点流程从零重画。
