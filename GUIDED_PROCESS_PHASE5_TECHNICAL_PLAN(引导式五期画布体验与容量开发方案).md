# 引导式五期画布体验与容量开发方案

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 引导式五期画布体验与容量开发方案 |
| 文档性质 | 第五期开发实施方案，不代表已经修改业务代码 |
| 对应阶段 | 第五期：画布体验与容量 |
| 当前状态 | 待技术评审，尚未开始正式代码开发 |
| 前置阶段 | 引导式流程数据协议和运行语义已经基本稳定，第二至第四期不再大幅改结构 |
| 目标容量 | 单流程最多 150 个业务节点；真实约 110 节点和模拟 150 节点双基准 |
| 前端工程 | 当前 `ngap` 工作区，重点为应用编排 ProcessCanvas 及相关 Store/序列化链路 |
| 后端工程 | 当前工作区未提供，主要配合大流程数据、保存体积和接口耗时核查 |
| 分期规划 | `GUIDED_PROCESS_PHASE_PLAN(引导式流程分期实施规划).md` |
| 四期方案 | `GUIDED_PROCESS_PHASE4_TECHNICAL_PLAN(引导式四期路径回滚与运行时稳定性开发方案).md` |
| 决策记录 | `GUIDED_PROCESS_PENDING_ITEMS(引导式待完善事项).md` |

第五期不是仅调 CSS，也不先决定必须使用某个开源库。本期先把画布数据从完整业务组件中瘦身、把高频更新局部化，再用同一数据和同一操作基准决定现有画布是否足够。

---

## 2. 本期目标

- 真实约 110 节点流程能够稳定打开、编辑、保存和重开；
- 模拟 150 节点流程通过同一操作基准；
- 节点少但内部元素很多的流程单独通过容量验证；
- 拖动一个节点时只更新相关节点和连线；
- 画布节点不携带或挂载完整业务组件树；
- 撤销重做不深拷贝大体积组件定义和接口数据；
- 节点编辑详情按需加载并可释放；
- 支持流程级“左右”和“上下”两种连线方向；
- 切换方向不改变拓扑、分支、条件或运行含义；
- 形成可复现的“保留现有画布/更换开源库”结论。

---

## 3. 已冻结规则

### 3.1 容量不能只看节点数

每份基准必须同时记录：

- 业务节点数、开始/结束节点、Header/Footer/Control 数量；
- 连线数、分支出口数、交叉线和长连线数量；
- 单节点原子元素数、全部节点元素总数和峰值；
- 组件完整配置体积；
- 变量、接口、事件、输出、诊断和分支配置体积；
- DOM/SVG 数量、Store 订阅数量；
- 历史记录条数和每条记录大小；
- JS 堆内存、长任务和关键交互耗时。

### 3.2 先优化现有画布

第一轮必须完成：

- 拓扑摘要与业务组件详情分离；
- 节点/连线局部更新；
- 历史记录轻量化；
- 细粒度订阅；
- 初始化、布局测量和保存序列化优化。

只有优化后仍未达到验收目标，才以相同基准对 React Flow、X6、LogicFlow 或其他候选库做原型。

### 3.3 方向切换只改变呈现

- 左右：左侧进入、右侧流出；
- 上下：上方进入、下方流出；
- 节点坐标默认不自动重排；
- 不修改 transitionId、parentTransitionId、分支顺序和条件；
- 旧流程缺少方向配置时使用当前左右模式；
- 自动布局不属于本期。

---

## 4. 本期范围边界

### 4.1 包含

- 性能基准工具和数据集；
- 画布轻量拓扑数据模型；
- 组件详情按需加载和缓存；
- 节点卡片渲染隔离；
- 邻接索引和连线局部更新；
- 拖动 requestAnimationFrame 合帧；
- 撤销重做轻量化；
- 保存序列化和初始化优化；
- 必要时的视口降级策略；
- 左右/上下连接点和线路径；
- 候选开源库的有条件原型对比；
- 主编辑器、预览入口和普通页面回归。

### 4.2 不包含

- 自动布局和一键整理；
- 小地图、分组、泳道等新画布产品能力；
- 移动端和无障碍；
- 重做前三期业务配置协议；
- 重做第四期运行状态机；
- 未经基准直接整体替换画布库；
- 候选库正式迁移的额外 15～25 前端人日；
- 把运行页已完成复杂节点转为摘要的产品能力，该事项仍按当前 P2 不考虑。

---

## 5. 基准数据设计

### 5.1 基准 A：真实复杂流程

使用可获得的约 110 节点真实旧流程结构，脱离敏感业务数据后保留：

- 节点和连线分布；
- 多分支、汇合、长线和交叉线；
- 节点组件元素数量分布；
- 配置体积和组件复用情况。

### 5.2 基准 B：150 节点上限流程

在真实分布基础上扩充到 150 个业务节点，不用 150 个空壳节点冒充容量验证。至少包含：

- 线性节点；
- 多出口节点；
- 多个共享通用组件实例；
- Header/Footer/Control；
- 不同规模的组件配置。

### 5.3 基准 C：节点少、元素多

构造 20～30 个节点，但单节点含大量表单、表格列、变量、事件和接口配置，用于验证当前观察到“约 20 节点已经卡顿”是否主要由 componentData 和深拷贝引起。

### 5.4 基准 D：节点多、元素少

构造 150 个轻节点，用于单独观察拓扑、连线、DOM/SVG 和订阅成本。

### 5.5 操作脚本

四份数据均执行相同操作：

1. 打开应用编辑器；
2. 等待画布可操作；
3. 缩放和平移；
4. 连续拖动指定节点；
5. 新增节点和连线；
6. 打开一个轻节点和一个重节点配置；
7. 修改名称、区域和分支；
8. 撤销、重做；
9. 切换左右/上下；
10. 保存；
11. 关闭并重新打开；
12. 启动预览，验证业务语义未变。

---

## 6. 性能指标与预算

开发开始第一周先采集当前基线，再冻结正式门槛。若暂无组织统一标准，可采用以下工程目标作为初始评审值：

| 指标 | 建议目标 |
| --- | --- |
| 150 节点画布可操作时间 | 常规开发机 p95 ≤ 5 秒 |
| 拖动可见流畅度 | p95 帧耗时 ≤ 32ms，无连续明显冻结 |
| 拖动关联连线更新 | 单帧只处理当前节点邻接边 |
| 普通节点配置打开 | p95 ≤ 500ms（不含首次远程接口延迟） |
| 重节点配置打开 | p95 ≤ 1.5 秒 |
| 撤销/重做 | p95 ≤ 500ms |
| 方向切换 | 150 节点 p95 ≤ 1 秒，不重建业务配置 |
| 前端保存准备/序列化 | p95 ≤ 1 秒 |
| 单次普通拖动提交 | 只生成一条历史记录 |
| 连续操作内存 | 完成并关闭详情后可回落，无持续阶梯增长 |

正式数字必须记录机器配置、浏览器版本、数据集版本和采样方法；不能只写“感觉不卡”。

---

## 7. 画布轻量数据模型

### 7.1 拆分拓扑摘要和组件详情

```ts
interface CanvasNodeSummaryV5 {
  nodeId: string;
  componentId?: string;
  name: string;
  nodeType: string;
  position: { x: number; y: number };
  presentation: { region: string };
  interactionMode?: 'manual' | 'automatic';
  branchSummary?: {
    transitionCount: number;
    hasDefault: boolean;
  };
  validationSummary?: {
    errorCount: number;
    warningCount: number;
  };
}

interface CanvasEdgeSummaryV5 {
  transitionId: string;
  sourceNodeId: string;
  targetNodeId: string;
  order: number;
  title?: string;
}
```

画布 Store 只持有摘要、选中态、视口和必要索引，不持有完整元素树、接口返回或运行数据。

### 7.2 组件详情仓库

完整组件详情通过 componentId/nodeId 在打开配置时加载：

- 请求去重；
- 按 definitionVersion 缓存；
- 同一通用组件多实例共享只读模板定义；
- 节点实例配置单独保存；
- 关闭编辑器后按 LRU/容量策略释放；
- 未修改详情不进入画布高频状态。

### 7.3 保存组装

保存时由序列化层组合：

```text
轻量拓扑摘要
+ 节点实例配置变更
+ 组件定义引用/版本
+ 流程级配置
→ 后端所需 sceneData/DTO
```

不为保存重新把所有远程组件定义深拷贝进每个节点。

---

## 8. 渲染与状态隔离

### 8.1 节点卡片

- React.memo 或等价细粒度订阅；
- props 只传当前摘要；
- 选中一个节点只更新旧选中和新选中节点；
- 校验数量变化只更新该节点；
- 不在卡片中渲染真实业务组件原子元素。

### 8.2 连线

建立邻接索引：

```text
outgoingEdgeIdsByNodeId
incomingEdgeIdsByNodeId
edgeByTransitionId
```

拖动节点只重算该节点的入线/出线，不遍历全部 lineArr。

### 8.3 拖动

- pointermove 只更新临时坐标；
- requestAnimationFrame 合并高频更新；
- 连线读取临时坐标；
- pointerup 时一次性提交 Store；
- 一次拖动只写一条历史记录；
- 不在每个 mousemove 进行全图深拷贝和序列化。

### 8.4 视口策略

先完成数据和订阅瘦身。若 150 节点仍存在明显 DOM/SVG 成本，再增加：

- 视口外节点轻量占位或不渲染；
- 视口外连线降级；
- 选中、拖动和连线中的节点强制保留；
- 扩展 overscan，避免平移时频繁闪烁。

视口优化不能导致连线目标消失或保存拓扑丢失。

---

## 9. 撤销重做

### 9.1 历史内容

历史只记录编辑定义变化：

- 节点增删；
- 节点位置；
- 连线增删；
- 摘要字段；
- 节点实例配置 patch；
- 流程级方向和展示设置。

不记录：

- 完整通用组件定义副本；
- 接口运行结果；
- 预览 Runtime；
- DOM 测量结果；
- 派生邻接索引。

### 9.2 实现策略

优先采用可逆命令/patch，若当前架构改造成本过高，可采用轻量拓扑快照。无论哪种都必须：

- 限制历史容量；
- 合并一次拖动；
- 派生索引不入历史；
- 还原后重新进行引用和拓扑校验；
- 不破坏稳定 nodeId/transitionId。

---

## 10. 初始化与保存优化

### 10.1 初始化

- 第一次只请求流程拓扑、节点摘要和必要统计；
- 批量接口避免 N+1，但不批量返回每个节点完整组件树；
- 邻接索引在内存一次构建；
- 避免初始化阶段反复 querySelector 和同步布局测量；
- 节点尺寸相同或可预测时使用配置尺寸；
- 必须测量的节点集中在一次布局阶段。

### 10.2 保存

- dirty tracking 识别真正变化；
- 派生字段不重复保存；
- 大对象序列化只执行一次；
- 保存过程不阻塞画布长时间无反馈；
- 后端返回版本/摘要后增量更新；
- 保存后重新打开验证方向、位置、transitionId 和业务配置不丢失。

### 10.3 后端配合

后端需核查：

- 是否能拆分流程拓扑摘要与组件详情接口；
- 大流程查询响应体积和耗时；
- sceneData/DTO 是否重复嵌入组件定义；
- 保存体积上限、网关限制和数据库字段限制；
- 复制、历史版本和发布读取新 layoutDirection；
- 生成真实 110/模拟 150 节点测试数据。

---

## 11. 左右/上下方向

### 11.1 配置协议

```ts
type GuidedLayoutDirectionV5 = 'horizontal' | 'vertical';

interface GuidedCanvasPresentationV5 {
  layoutDirection: GuidedLayoutDirectionV5;
}
```

缺省归一化为 horizontal。

### 11.2 连接点

| 模式 | 输入点 | 输出点 |
| --- | --- | --- |
| horizontal | 左侧中心 | 右侧中心 |
| vertical | 上方中心 | 下方中心 |

多分支出口在对应输出边按稳定 transition order 排列。

### 11.3 线路径

- 折线路由器接收方向参数；
- 箭头方向跟随最后一段；
- 分支文字贴近首段或出口，避免遮挡节点；
- 长线、回线、反向线和交叉线分别回归；
- 切换方向后重新计算全部线的呈现路径，但不修改边数据；
- 节点坐标保持，用户可手工重新排布。

### 11.4 编辑器入口

在流程级“页面布局/画布设置”提供方向选择，并明确提示“切换方向不会自动重新排列节点”。修改进入撤销重做和 dirty tracking。

---

## 12. 是否更换开源库的决策门

### 12.1 进入原型对比的条件

现有画布完成本期数据瘦身、局部更新和历史优化后，仍出现以下任一情况才进入候选原型：

- 150 节点核心操作无法达到冻结指标；
- 基础缩放、拖动、连线仍需大量维护且问题不可控；
- 视口渲染和多方向连接点在现有结构上无法可靠实现；
- 修复成本已经接近迁移成本。

### 12.2 同数据、同操作、同指标

候选库原型必须使用相同的 CanvasNodeSummary/EdgeSummary 适配层、四份数据集和操作脚本。比较：

- 性能指标；
- 与现有节点/分支编辑器集成成本；
- transitionId 和自定义连线能力；
- 受控状态、撤销重做和保存适配；
- 包体、许可证、维护活跃度；
- 主项目 React 版本和构建兼容；
- 双入口影响；
- 迁移和回归成本。

### 12.3 决策输出

形成一份可复现报告：

- 保留当前画布及原因；或
- 推荐某候选库、缺口和额外迁移计划；或
- 当前候选均不合适。

若决定替换，另立迁移阶段，初步增加前端 15～25 人日、3～5 周，不计入第五期原承诺。

---

## 13. 开发任务与预计时间

| 任务 | 主要内容 | 前端人日 | 后端人日 |
| --- | --- | ---: | ---: |
| P5-T1 基准和性能采集 | 四类数据集、操作脚本、当前基线、正式预算 | 3～4 | 1～2 |
| P5-T2 拓扑摘要与详情拆分 | 轻量节点/边、详情仓库、按需加载、缓存释放 | 4～6 | 1～2 |
| P5-T3 渲染和拖动局部化 | 节点隔离、邻接索引、rAF、局部连线更新 | 4～6 | 0 |
| P5-T4 历史与序列化优化 | patch/轻快照、dirty tracking、初始化和保存 | 3～5 | 0～1 |
| P5-T5 左右/上下方向 | 配置、连接点、线路由、文字箭头、回读 | 3～4 | 0～1 |
| P5-T6 视口策略或候选原型 | 仅在基准需要时做视口降级或候选库对比 | 1～3 | 0 |
| P5-T7 110/150 回归与报告 | 四数据集复测、内存、双入口/普通页回归、决策报告 | 2 | 0～1 |
| **合计** |  | **20～30** | **2～5** |

建议日历周期 **5～7 周**。若进入正式画布库替换，不再沿用该周期，需新增迁移计划。

---

## 14. 测试矩阵

### 14.1 功能正确性

- 新增、删除、复制、拖动节点；
- 新增、删除、重排分支连线；
- undo/redo 保持稳定 ID；
- 保存重开位置和方向一致；
- 左右/上下切换业务拓扑 JSON 不变；
- Header/Footer/Control 和唯一结束约束不受影响；
- 节点详情按需加载后配置完整。

### 14.2 性能

- A/B/C/D 四基准全部执行操作脚本；
- 记录 p50/p95，不能只记录最好一次；
- React Profiler 检查单节点变化渲染范围；
- Performance 检查长任务和拖动帧；
- Heap Snapshot 检查详情关闭和多次编辑后的回落；
- 统计 DOM/SVG、订阅、历史记录大小；
- 保存前端序列化和后端接口分别计时。

### 14.3 回归

- 普通小流程操作不因优化变复杂；
- 主 src 预览可读取保存后的流程；
- 独立 page 运行语义一致；
- 第四期路径回滚不受画布摘要模型影响；
- 普通组装式页面和 BottomBanner 不受影响。

---

## 15. 验收标准

- [ ] 真实 110 和模拟 150 节点均完成全操作脚本；
- [ ] 节点少元素多、节点多元素少均单独有结果；
- [ ] 画布节点不携带完整 componentData；
- [ ] 拖动仅重算邻接连线；
- [ ] 一次拖动只产生一条历史；
- [ ] 单节点配置变化不引起全图节点刷新；
- [ ] 保存和重开不丢失稳定身份和配置；
- [ ] 左右/上下切换不改变执行语义；
- [ ] 旧流程缺省方向仍为左右；
- [ ] 性能报告可复现并记录环境；
- [ ] 是否换库有同数据、同操作、同指标结论；
- [ ] 若需换库，已明确不包含在原第五期工时中。

---

## 16. 开发完成门槛

1. 基准数据和指标经产品、前端、后端确认；
2. 当前实现优化前后对比数据完整；
3. 150 节点编辑核心链路满足冻结门槛，或形成正式迁移决策；
4. 方向配置完成保存、历史、复制和发布回读；
5. 复杂分支两种方向视觉可读且语义一致；
6. 小流程和普通页面回归通过；
7. 性能测试脚本、数据版本和报告归档；
8. 已知限制明确进入第六期验收材料。
