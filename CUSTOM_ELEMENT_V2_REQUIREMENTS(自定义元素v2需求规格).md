# 自定义元素 v2 需求规格

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 自定义元素 v2 需求规格 |
| 文档标识 | `CUSTOM_ELEMENT_V2_REQUIREMENTS` |
| 需求主题 | React 组件 ZIP 包上传、分析、构建、审核、发布与运行 |
| 适用范围 | NGAP 元素管理、应用编辑器、业务组件编辑器、主 `src` 预览、独立 `page/materials` 运行时 |
| 目标协议 | `elementProtocolVersion = 2` |
| 当前状态 | 需求基线，供产品、前端、后端、测试、安全和运维联合评审 |
| 上位背景 | `CODEX_CONTEXT.md` |
| 目标设计参考 | `CUSTOM_ELEMENT_REDESIGN(完整设计-理想方案).md` |
| 后续文档 | `CUSTOM_ELEMENT_V2_PHASE_PLAN(自定义元素v2分期实施规划).md` 及各期开发方案 |

本文是自定义元素 v2 后续排期、开发方案和验收的统一需求依据。旧的“务实落地版”和“领导版技术方案”仅保留为历史讨论材料；若其中的 `component.json`、浏览器直接构建发布、按 URL 猜协议等内容与本文冲突，以本文为准。

---

## 2. 背景与问题

### 2.1 当前能力

当前系统采用“三文件自定义元素”模式：

```text
TSX 组件逻辑
+ Schema TS/JS 属性配置
+ Less 样式
```

平台分别读取三份源码，在浏览器中通过 `@babel/standalone` 编译，生成 Blob URL，动态导入组件，并注册到组件表后交由 `NgapRender` 渲染。

### 2.2 当前主要问题

| 编号 | 问题 | 影响 |
|---|---|---|
| CE-BG-01 | 组件逻辑、属性定义和样式需要分别上传 | 操作繁琐，文件容易错配 |
| CE-BG-02 | 只适合单 TSX 文件，不能正常拆分包内模块 | 复杂组件难维护，偏离 React 开发习惯 |
| CE-BG-03 | 样式和图片等静态资源缺少统一依赖处理 | 组件能力受限，资源路径不稳定 |
| CE-BG-04 | 开发者必须理解平台旧 Schema 结构 | 学习成本高，属性面板配置重复 |
| CE-BG-05 | 主项目、预览弹窗和独立运行时存在重复编译逻辑 | 行为漂移，修复无法自动覆盖所有入口 |
| CE-BG-06 | 预览使用 `window.MyComponent` 等全局对象 | 会话互相污染，资源和状态难清理 |
| CE-BG-07 | 组件可能直接依赖 Store、请求工具和宿主全局对象 | 平台无法稳定升级，也无法治理能力使用 |
| CE-BG-08 | 生产运行时编译原始源码 | 性能、可复现性、审核一致性和安全性不足 |
| CE-BG-09 | 元素实例缺少明确版本锁定 | 新发布可能影响存量页面，回滚不精确 |
| CE-BG-10 | 编译错误缺少统一阶段、错误码和定位 | 用户只能看到笼统失败，排障成本高 |

### 2.3 改造原则

本次改造不是把三个上传控件合并成一个上传控件，而是建立完整、稳定、可审核的自定义元素 v2 生命周期：

```text
组件源包
  → 安全解包与静态分析
  → 平台组件清单
  → 服务端构建与扫描
  → 不可变运行产物
  → 审核与发布
  → 编辑器和独立运行页统一加载
```

---

## 3. 产品目标与成功标准

### 3.1 产品目标

- 用户只需上传一个受约束的 React 组件 ZIP 包；
- ZIP 包可以包含多个包内 TSX/TS/JSX/JS 模块、CSS/Less 和静态图片资源；
- 包根目录使用 `ngap.json` 声明入口、元数据、SDK 版本、权限和必要的显式配置；
- 平台从源码类型、JSDoc 和 `ngap.json` 生成属性、默认值、事件、方法和描述；
- 推导不完整时提供可理解的诊断和人工补充入口，不能只生成空白属性面板；
- 组件通过版本化平台 SDK 使用变量、接口、事件、消息、文件、跳转和宿主集成能力；
- 正式审核、发布和生产运行只使用服务端构建、扫描并固化的不可变产物；
- 主 `src` 与独立 `page/materials` 对同一元素保持一致的协议、加载、Props、SDK 和错误语义；
- v1 与 v2 可以长期并存，v2 发布不破坏现有 v1 元素和存量页面；
- 新 v2 元素实例从创建时锁定元素版本与产物标识，不静默跟随新版本。

### 3.2 业务成功标准

| 编号 | 指标 | 目标 |
|---|---|---|
| CE-SUC-01 | 标准 ZIP 上传闭环 | 可完成上传、分析、配置、预览、保存草稿、审核、发布和下线 |
| CE-SUC-02 | 典型 Props 推导 | 基础字符串、数字、布尔、枚举和 `onXxx` 事件可正确推导 |
| CE-SUC-03 | 多文件能力 | 包内相对模块、Less/CSS 和图片资源可构建并运行 |
| CE-SUC-04 | 双运行时一致性 | 同一冻结产物在主编辑器预览与独立运行页行为一致 |
| CE-SUC-05 | 版本稳定性 | 发布新版本不自动改变已经保存的 v2 页面实例 |
| CE-SUC-06 | 故障隔离 | 单个自定义元素加载或渲染失败不导致整页白屏 |
| CE-SUC-07 | 生产链路 | 生产 v2 页面不下载、解压或实时编译原始 ZIP |
| CE-SUC-08 | v1 回归 | v1 元素的新建策略按阶段控制，但现有 v1 页面始终能够继续运行 |

---

## 4. 范围与边界

### 4.1 本轮正式范围

1. v2 元素的新增、编辑、复制草稿、预览、保存、提交审核、审核、发布、驳回、下线；
2. 单个 ZIP 组件源包上传；
3. `ngap.json` 源清单协议；
4. 包内模块、样式和静态图片资源；
5. 默认导出 React 函数组件、`forwardRef`、`memo` 及二者组合；
6. Props、默认值、事件和简单方法候选的静态分析；
7. 标准平台组件清单与现有 Schema 的兼容映射；
8. 平台 SDK 类型、权限声明、宿主实现和错误模型；
9. 外部依赖白名单；
10. 服务端异步构建、扫描、不可变产物和构建状态；
11. 统一注册中心、Props 适配器、事件和 ref 方法接入；
12. 元素菜单、属性面板、事件面板、方法调用和页面保存；
13. 主 `src` 和独立 `page/materials` 双运行时；
14. 页面实例版本锁定、显式升级和精确回滚；
15. v1/v2 并存、v1 转 v2 辅助评估和迁移；
16. 错误诊断、审计、运行监控和资源清理。

### 4.2 首次正式推广范围

- 只允许可信内部角色上传；
- 使用 `trusted-main-window` 执行模式；
- 只开放叶子元素，不开放容器元素；
- 只允许平台白名单中的外部依赖；
- 不允许用户上传 `node_modules` 或执行包内安装脚本；
- 组件审核通过前不得进入正式元素菜单；
- 所有生产运行产物必须来自服务端构建；
- 页面新建的 v2 元素实例必须锁定版本和产物标识。

### 4.3 本轮不包含

- 任意 npm 工程上传；
- 自动安装用户指定依赖；
- 执行 `package.json scripts`、安装脚本或二进制程序；
- 开放 Node.js 内置模块；
- 允许动态 `import()`、`require()` 或任意远程模块 URL；
- 对所有 TypeScript 高级类型进行完整推断；
- 保证 AST 静态扫描能够阻止所有恶意代码；
- 外部团队、租户或第三方供应商直接上传；
- 首期容器元素、递归子元素拖拽和 children 编排；
- 首期 `sandbox-iframe` 强隔离运行；
- 首期字体、音视频、WebAssembly 和可执行资源；
- 删除 v1 运行兼容；
- 建设完整公共组件市场。

---

## 5. 术语与命名规范

### 5.1 统一术语

| 中文术语 | 固定英文/字段 | 含义 |
|---|---|---|
| v1 元素 | `protocolVersion = 1` | 现有 TSX + Schema + Less 三文件元素 |
| v2 元素 | `protocolVersion = 2` | 新的 React ZIP 包元素 |
| 组件源包 | source package | 用户上传的原始 ZIP 文件 |
| 源清单 | `ngap.json` | 组件作者随 ZIP 提供的静态 JSON 清单 |
| 平台组件清单 | component manifest | 平台分析并规范化后的可序列化组件描述 |
| 运行清单 | `runtime-manifest.json` | 服务端生成、描述运行产物的加载入口 |
| 运行产物 | runtime artifact | 已构建的 ESM、CSS、资源和运行清单 |
| 构建任务 | build | 对一个源包执行分析、扫描、打包和产物发布的异步任务 |
| 产物键 | `artifactKey` | 运行注册和缓存使用的完整不可变键 |
| 元素定义 | element definition | 元素名称、分类、图标、归属和状态等管理对象 |
| 元素版本 | element version | 一个元素发布后不可变的版本对象 |
| 元素实例 | element instance | 页面或业务组件画布上使用的一次元素实例 |
| 平台 SDK | `context` / `@ngap/component-sdk` | 自定义元素使用平台能力的稳定协议 |
| 宿主适配器 | host adapter | 把主 `src` 或独立运行时内部能力适配成统一 SDK 的实现 |
| 注册中心 | registry | 统一加载、缓存、解析和释放自定义元素的运行服务 |
| 诊断 | diagnostic | 分析、构建、扫描、加载或运行阶段的结构化问题 |

### 5.2 禁止混用的名称

- 正式源清单统一叫 `ngap.json`，不再使用 `component.json`；
- `ngap.json` 不等于平台组件清单，也不等于 `runtime-manifest.json`；
- `elementProtocolVersion` 表示协议版本，不能用元素业务版本 `2.0.0` 代替；
- 元素的 `elementId`、发布版本 `elementVersion` 和产物 `artifactHash` 是三个不同标识；
- 平台 SDK 统一通过 `context` 注入，类型开发包使用 `@ngap/component-sdk`，不把 `usePlatform`、Store 或内部 request 作为公共协议；
- 正式文档中使用“元素实例”，不把页面实例 ID 与元素定义 ID 统称为“组件 ID”。

### 5.3 需求编号规范

| 前缀 | 含义 |
|---|---|
| `CE-PKG-*` | ZIP 源包与源清单 |
| `CE-ANA-*` | 静态分析与配置生成 |
| `CE-SDK-*` | 平台 SDK 与权限 |
| `CE-BLD-*` | 服务端构建与运行产物 |
| `CE-UX-*` | 元素管理和预览交互 |
| `CE-LFC-*` | 草稿、审核、发布生命周期 |
| `CE-RT-*` | 编辑器与运行时 |
| `CE-VER-*` | 版本锁定与升级 |
| `CE-CMP-*` | v1/v2 兼容与迁移 |
| `CE-SEC-*` | 安全和审计 |
| `CE-NFR-*` | 非功能要求 |
| `CE-ACC-*` | 验收场景 |

---

## 6. 用户角色与权限

| 角色 | 主要权限 | 限制 |
|---|---|---|
| 元素开发者 | 新建 v2 草稿、上传源包、查看分析结果、补充配置、发起构建、预览、提交审核 | 不能直接发布；不能批准自身新增的高风险权限 |
| 元素维护者 | 编辑已有元素并创建新草稿版本、查看使用关系、发起升级 | 不能原地覆盖已发布版本 |
| 业务审核人 | 审核业务信息、属性、事件、方法和预览效果 | 不能忽略安全扫描硬错误 |
| 安全审核人 | 审核源码风险、SDK 权限、依赖、扫描报告和签名信息 | 审核对象必须是冻结构建产物 |
| 元素管理员 | 发布、下线、恢复、配置白名单和角色范围 | 不能修改已发布版本的运行产物 |
| 页面搭建者 | 把已发布元素拖入画布、配置属性与事件、选择是否升级 | 不能使用未发布或无权访问的版本 |
| 运行用户 | 使用已发布页面中的元素 | 只看到安全错误摘要，不看到源码和内部堆栈 |
| 运维人员 | 查看构建、加载、错误和资源指标，执行合规的产物保留与回收 | 不能改变版本绑定关系 |

角色权限必须由平台权限系统控制，不仅依赖前端隐藏按钮。

---

## 7. 总体业务流程

### 7.1 新建 v2 元素

```text
新建元素
  → 填写基础信息
  → 上传一个 ZIP 源包
  → 浏览器预检并展示文件树
  → 服务端分析生成平台组件清单草稿
  → 用户确认或补充属性、事件、方法、依赖和权限
  → 发起服务端预览构建
  → 使用真实注册中心与 NgapRender 预览
  → 保存草稿或提交审核
  → 审核冻结产物
  → 发布
  → 出现在元素菜单
```

### 7.2 编辑已发布元素

```text
已发布 1.0.0
  → 创建新草稿版本
  → 上传新源包或修改清单覆盖项
  → 重新分析、构建、扫描和预览
  → 对比 Props、事件、方法、权限和依赖变化
  → 审核
  → 发布 1.1.0 或 2.0.0
```

已发布版本不得原地覆盖。构建失败、扫描失败或审核驳回不能影响当前已发布版本。

### 7.3 页面使用与升级

```text
页面搭建者拖入元素
  → 锁定当前 elementVersion + artifactHash
  → 配置并保存页面
  → 新元素版本发布
  → 编辑器提示有新版本
  → 查看差异和迁移预览
  → 用户明确确认升级
  → 保存新的版本绑定
```

---

## 8. 组件源包需求

### 8.1 标准结构

标准 ZIP 应支持：

```text
customer-info-card.zip
├─ ngap.json
├─ package.json              # 可选，只读取受限字段
├─ README.md                 # 可选，只用于说明和审核
├─ src/
│  ├─ index.tsx
│  ├─ types.ts
│  ├─ components/
│  ├─ hooks/
│  └─ styles/
└─ assets/
   ├─ empty.svg
   └─ default.png
```

### 8.2 源包规则

| 编号 | 需求 |
|---|---|
| CE-PKG-001 | 一次只允许上传一个 `.zip` 文件 |
| CE-PKG-002 | ZIP 根目录必须存在唯一 `ngap.json`；允许外包一层普通目录后自动提升一次 |
| CE-PKG-003 | 多个候选 `ngap.json`、多层模糊嵌套或缺少可识别包根时必须报错 |
| CE-PKG-004 | 缺少 `ngap.json` 时平台可生成草稿模板，但该源包只能预览和保存草稿，不能提交审核 |
| CE-PKG-005 | 入口必须是包内 `.tsx/.ts/.jsx/.js` 文件，并默认导出可静态确认的 React 组件 |
| CE-PKG-006 | 支持包内相对模块、类型文件、JSON、CSS/Less 和允许的静态图片资源 |
| CE-PKG-007 | 路径统一使用 `/`，规范化后不能包含 `..`、绝对路径、盘符或包外引用 |
| CE-PKG-008 | 禁止符号链接、特殊设备、加密 ZIP、嵌套压缩包和 CRC 错误 |
| CE-PKG-009 | 禁止 `node_modules`、`.git`、`dist`、`.env*`、私钥、证书和构建缓存 |
| CE-PKG-010 | `package.json` 只读取 name、version、dependencies、peerDependencies，永不执行 scripts |
| CE-PKG-011 | 所有文本使用 UTF-8；非法编码阻止提交审核 |
| CE-PKG-012 | 文件扩展名、MIME 和 magic bytes 必须一致 |

### 8.3 默认容量限制

| 项目 | 默认值 |
|---|---:|
| ZIP 压缩大小 | 不超过 10 MB |
| 解压后总大小 | 不超过 30 MB |
| 文件数量 | 不超过 300 |
| 单文件大小 | 不超过 2 MB |
| 目录深度 | 不超过 12 层 |
| 外部 import 数量 | 不超过 30 |
| 属性数量 | 不超过 100 |
| 事件数量 | 不超过 50 |
| 方法数量 | 不超过 50 |
| CSS 文本总量 | 不超过 200 KB |

前端预检与服务端必须执行同类校验，服务端结果是最终可信结论。

### 8.4 `ngap.json` 必要能力

源清单必须至少表达：

- `protocolVersion`：固定为 2；
- `name`：稳定包名，使用小写英文、数字和短横线；
- `version`：语义化版本；
- `entry`：入口模块；
- `component.title`：默认元素名称；
- `sdk.version`：兼容 SDK 版本范围；
- `sdk.permissions`：权限数组，无权限时为空数组。

可选表达：

- 样式入口；
- 组件描述、分类提示和标签；
- 属性分组、属性、事件和方法补充定义；
- 外部依赖；
- 静态资源规则；
- 叶子/容器、数据源、表单项和接口模式能力；
- 平台和浏览器兼容范围；
- 未来安全执行策略。

### 8.5 元数据优先级

最终平台组件清单按以下优先级合并：

```text
上传页面中用户最终确认的覆盖项
  > ngap.json
  > 入口源码、包内类型和 JSDoc 推导
  > 组件函数名、文件名和平台默认值
```

平台对覆盖过程保留审计快照，不要求把页面修改静默写回用户原 ZIP。

---

## 9. 静态分析与配置生成需求

### 9.1 分析范围

| 编号 | 需求 |
|---|---|
| CE-ANA-001 | 分析过程只能读取文件、解析 JSON、AST、样式和资源引用，不能执行用户源码 |
| CE-ANA-002 | 从 entry 建立完整包内模块图并检测缺失模块、大小写不一致和循环依赖 |
| CE-ANA-003 | 识别函数组件、箭头函数、`forwardRef`、`memo` 和 `memo(forwardRef())` 的默认导出 |
| CE-ANA-004 | 支持 interface、type、内联对象和包内类型 import 的基础 Props 推导 |
| CE-ANA-005 | 推导字符串、数字、布尔、字面量联合、基础数组和普通对象属性 |
| CE-ANA-006 | 推导静态字面量默认值，不执行函数或表达式取得默认值 |
| CE-ANA-007 | `onXxx` 函数属性作为事件候选，保留用户确认和改名入口 |
| CE-ANA-008 | 简单 `useImperativeHandle` 键名作为方法候选，最终以清单声明和预览验证为准 |
| CE-ANA-009 | 保留 Props 不进入普通属性面板，发生冲突时给出硬错误 |
| CE-ANA-010 | 自动结果必须转换为现有属性面板、事件面板和方法面板可消费的兼容 Schema |

### 9.2 保留 Props

以下名称由平台保留：

```text
key
ref
children
context
config
id
type
elements
loopVariable
className
style
```

`ngap.json.props` 声明保留名称时必须报错，不能静默忽略。

### 9.3 推导不完整时的交互

| 编号 | 需求 |
|---|---|
| CE-ANA-011 | 页面显示“已识别、需补充、不支持”三类字段数量和明细 |
| CE-ANA-012 | 每个未识别字段显示原因、源码位置和修复建议 |
| CE-ANA-013 | 平台生成可编辑的 `ngap.json` 补充模板，并定位到待补充字段 |
| CE-ANA-014 | 未识别字段不得悄悄丢失后直接允许提交审核 |
| CE-ANA-015 | 复杂属性补充完成且验证通过后可正常进入属性面板 |
| CE-ANA-016 | 不支持的运行时代码型 Schema 能力必须说明替代方案或标记无法迁移 |

### 9.4 诊断等级

- `error`：阻止构建、预览或提交，具体门槛由错误阶段决定；
- `warning`：允许保存草稿，默认阻止提交审核，只有平台明确配置的例外可以豁免；
- `info`：展示推导结果、自动修正建议或非阻塞信息。

所有诊断必须包含稳定错误码和可读信息；能够定位到源码时应包含文件、行、列和修复建议。

---

## 10. 平台 SDK 与能力权限需求

### 10.1 接入原则

| 编号 | 需求 |
|---|---|
| CE-SDK-001 | v2 组件接收扁平业务 Props 和平台注入的 `context` |
| CE-SDK-002 | 新组件不得直接 import 项目 Store、内部 request、内部页面模块或 CrossAPI 原始对象 |
| CE-SDK-003 | 平台提供 `@ngap/component-sdk` 类型、错误模型、测试 helper 和本地 mock |
| CE-SDK-004 | SDK 主版本显式管理，构建和运行时均检查兼容性 |
| CE-SDK-005 | 主 `src` 与独立运行时实现不同 host adapter，但公共类型、权限和错误语义必须一致 |
| CE-SDK-006 | SDK 订阅必须返回清理函数，元素卸载时宿主强制清理未释放订阅 |
| CE-SDK-007 | SDK 不向组件暴露 Token、Cookie、任意 baseURL、Axios 实例、完整用户对象或 Store |

### 10.2 首次正式推广能力

- 页面变量读取、写入、订阅和公式计算；
- 当前元素实例已配置接口的执行；
- 平台登记的业务接口能力调用；
- 元素事件触发；
- 消息、确认框和通知；
- 图片/附件上传和受控下载；
- 页面路由、微应用和受控外链跳转；
- 基础用户上下文；
- 组件实例隔离存储；
- 组件日志；
- 经逐项登记的宿主集成能力。

容器 children 渲染能力不在首次正式推广范围。

### 10.3 权限规则

| 编号 | 需求 |
|---|---|
| CE-SDK-008 | `ngap.json` 必须声明组件需要的最小权限 |
| CE-SDK-009 | 权限必须来自平台能力目录，不能使用任意字符串 |
| CE-SDK-010 | 源码中可静态确认的能力调用必须与声明权限一致 |
| CE-SDK-011 | 动态拼接 interfaceId、capability 或事件编码默认阻止发布 |
| CE-SDK-012 | 上传分析页按风险等级展示权限、使用位置、用途和运行模式 |
| CE-SDK-013 | 升级新增权限必须重新审核，不能静默扩权 |
| CE-SDK-014 | 运行时根据冻结权限集合生成裁剪后的 context |
| CE-SDK-015 | 未声明权限调用返回统一 `SDK_PERMISSION_DENIED` |

### 10.4 安全边界说明

首期 `trusted-main-window` 模式下，SDK 权限是公共 API 治理和审计边界，不是浏览器级安全沙箱。

SDK 权限能够：

- 规范正常组件访问平台能力的方式；
- 记录和审核能力使用；
- 控制 SDK 方法是否提供；
- 让新增权限触发重新审核；
- 支撑 SDK 版本升级。

SDK 权限不能单独阻止恶意源码访问 DOM、window、同源存储或尝试直接发起网络请求。首期真正的安全前提是可信上传角色、人工审核、服务端扫描、不可变产物、签名和审计。未来开放外部上传前必须实施 iframe 或独立 origin 强隔离。

---

## 11. 构建与运行产物需求

### 11.1 正式构建链路

| 编号 | 需求 |
|---|---|
| CE-BLD-001 | 浏览器上传成功只表示源包已保存，不表示源包有效或可发布 |
| CE-BLD-002 | 正式构建必须在服务端隔离环境重新解包、校验、分析、扫描和打包 |
| CE-BLD-003 | 构建为异步任务，返回 `buildId`，并提供状态、阶段、进度和取消能力 |
| CE-BLD-004 | 构建阶段至少包含下载、解包、清单校验、模块分析、源码扫描、打包、产物扫描、产物发布和完成 |
| CE-BLD-005 | 相同源包、覆盖项、构建器和策略版本可复用成功构建；策略变化必须生成新产物 |
| CE-BLD-006 | 构建失败不得污染当前已发布版本 |
| CE-BLD-007 | 预览、审核和发布必须能够绑定到同一个冻结构建产物 |
| CE-BLD-008 | 发布时不得重新构建，必须提升审核通过的冻结产物 |
| CE-BLD-009 | 生产运行页只加载运行清单和 ESM/CSS/Assets，不加载原始 ZIP |
| CE-BLD-010 | 运行产物使用内容寻址、不可变 URL，并记录 hash、构建器版本和扫描版本 |

### 11.2 外部依赖白名单

首次基线依赖与当前项目版本对齐：

| 依赖 | 基线版本 | 策略 |
|---|---:|---|
| `react` | 18.3.1 | 宿主提供，组件不能携带另一份 React |
| `react-dom` | 18.3.1 | 默认不鼓励组件直接使用 |
| `antd` | 5.21.0 | 宿主提供 |
| `@ant-design/icons` | 5.3.7 | 宿主提供 |
| `dayjs` | 1.11.21 | 宿主提供 |

`lodash-es` 和 `@ant-design/plots` 可在容量和双运行时验证后按需加入正式白名单。白名单必须同时应用于分析器、服务端构建器、审核规则、主宿主和独立运行宿主。

### 11.3 构建等待体验

- 浏览器选择 ZIP 后立即执行预检并展示可用的静态分析反馈；
- 服务端构建期间显示当前阶段、进度、排队状态和已等待时间；
- 用户离开页面不自动取消构建，重新进入后可凭 `buildId` 恢复状态；
- 构建失败不要求重新上传未变化的 ZIP，允许修正清单覆盖项后重新构建；
- 预览必须以服务端构建产物为正式结论；本地或浏览器快速预览只能作为开发辅助，不能提交审核或发布；
- 构建排队或超时时给出明确提示和可恢复操作，不显示无限加载状态。

### 11.4 运行清单

`runtime-manifest.json` 至少包含：

- `elementId`、`elementVersion`、`protocolVersion`；
- `packageHash`、`manifestHash`、`artifactHash`；
- 入口 ESM、chunk、CSS、assets 的不可变 URL 和完整性信息；
- 外部依赖及宿主映射；
- SDK 版本和冻结权限；
- 平台组件清单；
- 扫描状态、构建器版本、签发时间和签名。

---

## 12. 元素管理与预览交互需求

### 12.1 五步向导

#### 第一步：基础信息

保留现有元素名称、分类、图标、页面布局、归属范围和说明。源码分析得到的名称、描述和分类提示只用于预填，最终以用户确认值为准。

#### 第二步：上传组件源包

展示：

- ZIP 文件名、压缩大小和 SHA-256；
- 包根目录和 `ngap.json` 状态；
- 文件树、文件数量、目录深度和解压后大小；
- entry、默认导出、模块、样式和资源；
- 外部依赖；
- SDK 版本和申请权限；
- 安全诊断。

#### 第三步：分析与配置

展示并允许确认：

- 组件名称和描述；
- 属性中文名、类型、编辑器、默认值、分组、排序和显隐；
- 事件名称、标题和载荷说明；
- 方法名称、标题和参数；
- 外部依赖及版本；
- SDK 权限和风险；
- 未识别字段及补充模板；
- diagnostics 分类结果。

#### 第四步：预览与校验

预览必须走真实 v2 registry、Props adapter 和 `NgapRender`，包含：

- 组件实际效果；
- 自动生成的属性面板；
- 属性调整后的实时渲染；
- 事件触发日志；
- 方法调用测试；
- SDK mock 成功、失败、拒绝和超时场景；
- 组件加载、渲染和清理诊断。

关闭预览后必须释放预览 session 的组件、样式、Blob、ref 和订阅，不得写入 `window.MyComponent` 或 `window.MyComponentJsData`。

#### 第五步：保存或提交

页面显示当前源包、清单、构建、扫描、权限和预览是否满足对应动作门槛。

### 12.2 操作门槛

| 操作 | 门槛 |
|---|---|
| 保存草稿 | 基础信息可保存；允许存在 warning；源包和诊断必须可回读 |
| 发起预览构建 | 无阻止构建的 ZIP、清单、依赖或语法 error |
| 打开正式预览 | 服务端预览构建成功，运行清单可加载 |
| 提交审核 | 无 error；必要 warning 已处理；构建和扫描成功；权限已填写用途；正式预览成功 |
| 审核通过 | 审核人预览冻结产物并确认业务、安全、依赖和权限 |
| 发布 | 只能发布审核通过的冻结版本；产物 hash 与签名一致 |
| 下线 | 新页面不能再选择；存量页面行为按平台既有下线规则明确提示，不自动换版本 |

### 12.3 交互需求编号

| 编号 | 需求 |
|---|---|
| CE-UX-001 | v2 元素创建和编辑统一使用“基础信息 → 上传组件源包 → 分析与配置 → 预览与校验 → 保存或提交”五步向导 |
| CE-UX-002 | 五步向导必须显示当前步骤、已完成步骤、阻塞步骤和可执行操作，刷新或重新进入后能够恢复 |
| CE-UX-003 | 基础信息中的名称、分类、图标、布局、归属和说明支持分析结果预填，但最终值必须由用户确认 |
| CE-UX-004 | 上传步骤必须展示 ZIP 文件名、压缩大小、SHA-256、包根目录、文件数、目录深度和解压后大小 |
| CE-UX-005 | 上传步骤必须展示 `ngap.json`、入口、模块、样式、资源、外部依赖、SDK 版本、权限和安全诊断状态 |
| CE-UX-006 | ZIP 缺少 `ngap.json` 时可生成规范草稿模板并保存草稿，但在清单有效前不得提交审核 |
| CE-UX-007 | 分析步骤必须分区展示 Props、events、methods、dependencies、permissions、未识别项和 diagnostics |
| CE-UX-008 | Analyzer 推导值、`ngap.json` 声明值和用户覆盖值必须标记来源；源包变化后受影响覆盖项标记 stale 并要求重新确认 |
| CE-UX-009 | diagnostics 必须区分 error、warning 和 info，并包含稳定错误码、文件位置、安全摘要和可执行修复建议 |
| CE-UX-010 | 正式预览只能打开服务端构建成功的冻结预览产物；本地或浏览器快速预览必须明确标记为开发辅助 |
| CE-UX-011 | 预览必须使用真实 v2 Registry、Props adapter 和 `NgapRender`，不得使用独立的简化渲染语义替代 |
| CE-UX-012 | 预览支持属性实时调整、事件日志、方法调用、ref 状态和 SDK mock 的成功、失败、拒绝、超时场景 |
| CE-UX-013 | 预览错误必须区分 manifest、CSS、module、render、method、SDK 等阶段，并允许查看安全诊断和重试可恢复步骤 |
| CE-UX-014 | 关闭或切换预览必须释放 session、组件、样式、Blob、ref、订阅和未完成调用，不得污染 `window` 全局变量 |
| CE-UX-015 | 保存草稿、构建预览、提交审核、审核通过、发布和下线按钮必须按各自门槛独立启用或禁用，并解释阻塞原因 |
| CE-UX-016 | 服务端构建期间必须显示 buildId、阶段、排队状态和等待时间；离开页面不自动取消，重新进入可恢复查询 |
| CE-UX-017 | 构建失败后必须保留源包、用户覆盖项和 diagnostics；输入未变化时无需重新上传即可修正并重新构建 |
| CE-UX-018 | 权限新增、依赖扩大、公开 API 变化和高风险能力必须在提交与审核页面突出显示并要求明确确认 |
| CE-UX-019 | v1 与 v2 元素的协议、编辑入口、构建方式和发布状态必须有清晰标识，不能让用户误以为两者使用同一运行链路 |
| CE-UX-020 | 所有长任务必须提供成功、失败、取消、超时和恢复状态，不得出现无法判断原因的无限加载 |

---

## 13. 元素生命周期需求

### 13.1 状态兼容

继续兼容当前元素状态：

```text
1 草稿
2 已发布
3 待审核
4 审核驳回
5 已下线
6 原生元素
```

v2 还需单独保存构建状态和扫描状态，不能用元素状态代替构建状态。

### 13.2 冻结对象

提交审核时冻结：

- `packageHash`；
- 规范化平台组件清单及 `manifestHash`；
- `artifactHash`；
- 构建器版本；
- 依赖策略版本；
- SDK 策略版本和权限集合；
- 扫描器版本与扫描报告；
- 运行清单；
- 正式预览结果标识。

### 13.3 审核一致性

| 编号 | 需求 |
|---|---|
| CE-LFC-001 | 审核人看到的预览必须来自冻结产物 |
| CE-LFC-002 | 发布必须使用同一个冻结产物，不能重新构建 |
| CE-LFC-003 | 审核中源包或配置发生变化时，原审核失效并重新提交 |
| CE-LFC-004 | 权限增加、依赖扩大、公开 API 变化必须在差异中突出展示 |
| CE-LFC-005 | 驳回意见绑定到具体 elementVersion、buildId 和 artifactHash |
| CE-LFC-006 | 发布后产物字段只读；修复只能创建新版本 |

---

## 14. 编辑器与运行时需求

### 14.1 注册与加载

| 编号 | 需求 |
|---|---|
| CE-RT-001 | 注册中心按 `elementId + elementVersion + artifactHash + sdkAdapterMajor` 缓存 |
| CE-RT-002 | 相同产物的并发加载共用一个 Promise |
| CE-RT-003 | 不同版本可以同页共存，不能只按 elementId 覆盖 |
| CE-RT-004 | 加载状态明确区分 loading、ready、error、missing |
| CE-RT-005 | 更新或失效必须同时清理组件、schema、样式、Blob、ref 和订阅 |
| CE-RT-006 | 不得通过 `typeof function` 猜测组件还是 loader |
| CE-RT-007 | v1 和 v2 使用显式协议版本分流 |

### 14.2 Props 与事件

- v2 业务属性从 `config.props` 扁平展开；
- 平台事件回调覆盖用户同名配置，用户不能伪造平台事件函数；
- 平台保留 Props 最后注入，用户不能覆盖 `context`、`id` 等字段；
- 兼容提供 `config/id/type/elements/loopVariable`，但新组件不应依赖内部数据结构；
- 事件回调 Props 与 `context.events.emit()` 进入同一平台事件流；
- 方法只有在清单声明且运行时 ref 实际暴露时才允许调用。

### 14.3 故障隔离

- 每个 v2 元素单独包裹错误边界；
- 编辑器显示元素名、错误摘要、错误编号和重新加载入口；
- 最终运行用户只看到安全占位和错误编号；
- 一个元素加载失败不阻止其他元素和页面主流程继续初始化；
- 运行异常必须带 elementId、version、artifactHash、instanceId 和页面上下文记录。

### 14.4 双运行时一致性

| 编号 | 需求 |
|---|---|
| CE-RT-008 | 主 `src` 和独立 `page/materials` 共用协议、清单校验、registry、Props adapter 和 SDK contract |
| CE-RT-009 | 两端仅允许请求、Store、路由、消息和宿主集成等 adapter 实现不同 |
| CE-RT-010 | 两端必须执行同一套 host adapter 契约测试 |
| CE-RT-011 | 独立运行页只加载页面实际使用的元素，并按版本去重批量查询 |
| CE-RT-012 | 引导式后续节点出现新元素时支持增量加载 |
| CE-RT-013 | 生产 v2 加载路径不得默认加载 Babel、JSZip 或 Analyzer |

---

## 15. 版本锁定与升级需求

### 15.1 标识规则

| 标识 | 含义 | 是否可变 |
|---|---|---|
| `elementId` | 元素定义稳定 ID | 不可变 |
| `elementVersion` | 元素发布版本，使用 semver | 发布后不可变 |
| `packageHash` | 原始源包内容 hash | 不可变 |
| `manifestHash` | 规范化平台组件清单 hash | 不可变 |
| `artifactHash` | 构建产物集合 hash | 不可变 |
| `builderVersion` | 产生该产物的构建器版本 | 不可变记录 |

### 15.2 页面实例锁定

| 编号 | 需求 |
|---|---|
| CE-VER-001 | 新拖入的 v2 元素实例必须保存 `elementProtocolVersion`、`elementVersion` 和 `elementArtifactHash` |
| CE-VER-002 | 页面复制、模板生成、历史版本和分享链路必须保留版本字段 |
| CE-VER-003 | 发布新版本不得自动改变已保存页面实例 |
| CE-VER-004 | 旧页面缺少版本时兼容 latest published，并在编辑器标记为未锁定风险 |
| CE-VER-005 | 编辑器显示当前版本、最新版本、更新日志和公开契约差异 |
| CE-VER-006 | 升级必须由页面搭建者明确确认并重新保存页面 |
| CE-VER-007 | 升级前必须预览属性迁移、删除字段、默认值变化、事件/方法变化和权限变化 |
| CE-VER-008 | 支持声明式 rename/remove/defaults 配置迁移；复杂迁移要求人工处理 |
| CE-VER-009 | 回滚按精确 elementVersion + artifactHash 加载旧产物 |

### 15.3 构建器升级

- 旧元素版本继续使用原不可变产物，不因构建器升级失效；
- 新构建默认使用当前受支持构建器；
- 重新构建必须生成新 artifactHash，并留下原产物关系；
- 构建器主版本停止支持前必须提供迁移窗口、批量重建评估和失败清单；
- 不允许因构建器升级静默覆盖同一版本产物。

---

## 16. v1/v2 兼容与迁移需求

### 16.1 协议识别

`elementProtocolVersion` 是 v2 正式上线的硬性字段。生产加载顺序：

1. 读取 `elementProtocolVersion`；
2. 值为 2 时加载 v2 运行信息；
3. 缺省或值为 1 时走 v1；
4. 不允许长期通过 URL、文件后缀或源码内容猜协议。

### 16.2 v1 运行兼容

| 编号 | 需求 |
|---|---|
| CE-CMP-001 | v1 元素继续读取现有 TSX、Schema 和 Less 字段 |
| CE-CMP-002 | v1 可进入统一 registry 获得缓存、错误状态和资源清理，但 Props 语义不改变 |
| CE-CMP-003 | v1/v2 可以在同一页面和同一业务组件中混合运行 |
| CE-CMP-004 | v2 改造不能要求先迁移全部 v1 元素 |
| CE-CMP-005 | 停止新建 v1 必须在 v2 稳定并通过推广门槛后单独执行 |

### 16.3 v1 转 v2

迁移辅助器输出兼容性等级：

- 可自动转换：标准 Schema、字面量默认值、常规事件/方法声明、Less；
- 需人工处理：Schema 函数、内部 request、Store、window 全局对象、内部工具引用；
- 无法自动转换：运行时生成 Schema、动态 import、非白名单依赖、无法替代的私有宿主能力。

迁移必须创建新 v2 版本，不覆盖旧发布版本；转换报告需列出已转换项、待处理项、不支持项和建议修改方式。

---

## 17. 安全、审核与审计需求

### 17.1 首期安全模型

| 编号 | 需求 |
|---|---|
| CE-SEC-001 | 只有可信内部授权角色可以上传组件源包 |
| CE-SEC-002 | 所有提交审核的源包必须经过服务端重新解包、静态分析和扫描 |
| CE-SEC-003 | 默认阻止 eval、new Function、WebAssembly、动态 import、require 和直接网络请求 |
| CE-SEC-004 | 默认阻止访问 Cookie、浏览器存储、父窗口、脚本/iframe 注入和修改 document 根节点 |
| CE-SEC-005 | CSS 阻止危险 URL、外部 import 和明显全局污染规则 |
| CE-SEC-006 | SVG 需要扫描脚本、事件属性、外部资源和危险 URL |
| CE-SEC-007 | 运行产物必须记录 hash 并由平台签发；校验失败不得执行 |
| CE-SEC-008 | 签名失败时不能降级为加载原始源码 |
| CE-SEC-009 | 审计记录上传者、审核者、源包 hash、清单 hash、产物 hash、权限和扫描结果 |
| CE-SEC-010 | 安全错误对最终用户只显示安全摘要，不暴露源码、内网 URL、Token 或堆栈 |

### 17.2 未来外部开放门槛

如果上传者范围扩大到外部团队、租户或第三方供应商，必须重新评审并实现至少一种强隔离方案：

- sandbox iframe；
- 独立 origin；
- RPC 型 Props、事件、方法和 SDK；
- 独立 CSP 和网络策略。

在强隔离完成前不得仅依靠增加 AST 规则扩大上传者范围。

---

## 18. 错误、提示与可观测性需求

### 18.1 错误阶段

```text
read-package
unzip
parse
analyze
validate
compile
scan
publish-artifact
load-manifest
load-module
register
render
event
method
```

### 18.2 错误码规范

| 范围 | 含义 |
|---|---|
| `CE10xx` | 入口与默认导出 |
| `CE11xx` | ZIP 与源清单 |
| `CE12xx` | 模块、依赖、样式和资源 |
| `CE13xx` | SDK、版本和权限 |
| `CE2xxx` | Props、默认值、事件和方法推导 |
| `CE3xxx` | 安全扫描与构建 |
| `CE4xxx` | 运行清单、产物和加载 |
| `CE5xxx` | 渲染、事件和方法运行时 |

同一错误码语义必须稳定；新增细分问题使用新错误码。

### 18.3 用户提示

| 场景 | 展示内容 |
|---|---|
| 上传/分析 | 文件、行列、错误码、原因、修复建议、阻塞动作 |
| 构建 | buildId、阶段、进度、等待时间、失败诊断、重新构建入口 |
| 编辑器加载 | 元素名称、版本、错误摘要、错误编号、重试入口 |
| 运行页加载 | 友好占位和错误编号，不显示技术内部信息 |
| SDK 调用 | 统一 safeMessage、错误码、requestId 和 retryable |

### 18.4 指标

- ZIP 预检、分析、扫描和构建各阶段成功率与 P95；
- 构建排队时间、执行时间、缓存复用率、取消率和超时率；
- 运行清单、ESM 和 CSS 加载成功率及耗时；
- registry 缓存命中率；
- 首次渲染耗时和元素级运行错误率；
- v1/v2 使用比例；
- 各 SDK capability 调用量、失败率、P95 和权限拒绝次数；
- 签名或 hash 校验失败次数；
- 预览 session、style、Blob 和订阅清理异常次数。

---

## 19. 非功能需求

| 编号 | 类别 | 要求 |
|---|---|---|
| CE-NFR-001 | 兼容性 | 支持项目当前 Chrome 80 构建目标，实际浏览器基线实施前复核 |
| CE-NFR-002 | 性能 | 独立运行页不加载 Analyzer、JSZip、Babel 或浏览器构建器作为 v2 生产首屏依赖 |
| CE-NFR-003 | 隔离 | 单个元素的组件、样式、ref、SDK 订阅和错误必须按实例或产物隔离 |
| CE-NFR-004 | 可复现 | 同一源包、覆盖项、构建器和策略输入应产生稳定构建结果 |
| CE-NFR-005 | 可恢复 | 构建任务可恢复查询；失败任务可修正后重新发起，不影响已发布版本 |
| CE-NFR-006 | 可维护 | 主 `src` 与 `page/materials` 共用核心协议和运行代码，禁止复制第三套实现 |
| CE-NFR-007 | 可测试 | package reader、analyzer、manifest、SDK、registry 和 adapter 均有自动化测试 |
| CE-NFR-008 | 可观测 | 构建、加载、渲染和 SDK 调用均带稳定 identity 和错误阶段 |
| CE-NFR-009 | 资源管理 | 同产物样式只挂载一次，最后引用释放后可清理；预览关闭必须清理全部临时资源 |
| CE-NFR-010 | 数据安全 | 日志和错误不得记录 Token、Cookie、文件内容、完整用户信息和敏感业务字段 |

---

## 20. 数据与接口产品要求

### 20.1 后端最小硬门槛

- 明确保存和返回 `elementProtocolVersion`；
- 保存组件源包不可变引用和 `packageHash`；
- 提供异步构建任务及状态查询；
- 保存平台组件清单、运行清单和 `artifactHash`；
- 元素版本记录构建器、扫描器、SDK、依赖和权限；
- 审核记录绑定冻结 build 和 hash；
- 批量返回页面运行所需的元素版本与运行清单地址；
- 已发布版本的产物字段不可更新。

### 20.2 建议数据对象

| 对象 | 主要职责 |
|---|---|
| `element_definition` | 元素名称、分类、图标、归属和当前状态 |
| `element_source_package` | 源包 URL、hash、上传者、大小和审计信息 |
| `element_build` | 构建状态、阶段、输入、策略版本、诊断和产物 |
| `element_version` | 不可变版本、平台组件清单、运行清单、权限和发布状态 |
| `element_permission_review` | 权限变化、风险、申请与审核记录 |

### 20.3 必要接口能力

- 上传源包；
- 发起分析/构建；
- 查询构建状态；
- 取消构建；
- 保存 v2 草稿；
- 提交审核、驳回、发布和下线；
- 查询元素版本列表和版本差异；
- 批量查询运行信息；
- 查询 SDK 能力目录；
- 查询元素使用关系；
- 发起显式升级或迁移评估。

接口名称和具体 DTO 在各期开发方案中冻结；本需求不要求直接沿用理想设计中的示例 URL。

---

## 21. 验收场景

### 21.1 源包与分析

| 编号 | 场景 | 预期 |
|---|---|---|
| CE-ACC-001 | 上传最小合法 ZIP | 正确识别清单、入口、组件、属性和事件 |
| CE-ACC-002 | ZIP 外包一层同名目录 | 自动提升一层并通过校验 |
| CE-ACC-003 | ZIP 存在两个 `ngap.json` | 阻止构建并指出冲突位置 |
| CE-ACC-004 | 缺少 `ngap.json` | 生成草稿模板，可保存草稿但不能提交审核 |
| CE-ACC-005 | `../`、绝对路径或大小写冲突 | 双端校验拒绝 |
| CE-ACC-006 | 多 TSX/TS 模块和包内类型 | 模块图、类型和构建正确 |
| CE-ACC-007 | Less/CSS 和 PNG/SVG | 编译、作用域和资源 URL 正确 |
| CE-ACC-008 | Props 使用复杂无法推导类型 | 明确列出未识别项并提供补充模板 |
| CE-ACC-009 | 默认值调用函数 | 不执行函数，给出 warning 和补充入口 |
| CE-ACC-010 | import 非白名单依赖 | 阻止构建并建议替代能力 |

### 21.2 SDK 与安全

| 编号 | 场景 | 预期 |
|---|---|---|
| CE-ACC-011 | 使用已声明变量读取权限 | 主、独立宿主行为一致 |
| CE-ACC-012 | 调用未声明接口能力 | 返回 `SDK_PERMISSION_DENIED` 并记录审计 |
| CE-ACC-013 | 升级版本新增文件上传权限 | 必须重新安全审核 |
| CE-ACC-014 | 直接使用 fetch/XHR | 静态扫描阻止提交审核 |
| CE-ACC-015 | 构建产物签名被篡改 | 运行时拒绝执行且不降级加载源码 |
| CE-ACC-016 | 元素卸载后仍保留订阅 | 宿主强制清理并记录异常 |

### 21.3 编辑器与运行时

| 编号 | 场景 | 预期 |
|---|---|---|
| CE-ACC-017 | v2 元素发布后拖入应用编辑器 | 默认属性、事件和方法完整 |
| CE-ACC-018 | 在业务组件编辑器使用 v2 | 行为与应用编辑器一致 |
| CE-ACC-019 | 同一元素两个版本同页 | 独立加载和渲染，不互相覆盖 |
| CE-ACC-020 | v1/v2 混合页面 | 均正常运行，Props 语义不串线 |
| CE-ACC-021 | 某个 v2 元素渲染抛错 | 该元素显示 fallback，其他元素继续运行 |
| CE-ACC-022 | 重复打开关闭预览 | 无 window 污染、重复样式、Blob 和 ref 残留 |
| CE-ACC-023 | 独立 page 首屏使用 v2 | 不下载 ZIP，不加载 Babel/Analyzer |
| CE-ACC-024 | 引导式后续节点首次引用 v2 | 增量加载成功，不重载整页 |

### 21.4 生命周期与版本

| 编号 | 场景 | 预期 |
|---|---|---|
| CE-ACC-025 | 构建失败 | 草稿保留，已发布版本不受影响 |
| CE-ACC-026 | 提交审核后修改源包 | 原审核失效，必须重新构建提交 |
| CE-ACC-027 | 审核与发布 | 使用同一 buildId 和 artifactHash |
| CE-ACC-028 | 发布新版本 | 存量锁定实例继续使用旧版本 |
| CE-ACC-029 | 页面显式升级 | 展示差异，迁移配置并重新保存 |
| CE-ACC-030 | 回滚 | 精确加载旧 version + artifactHash |
| CE-ACC-031 | v1 转 v2 | 生成新版本和迁移报告，不覆盖 v1 |

---

## 22. 推广门槛

自定义元素 v2 进入正式推广前必须同时满足：

1. `elementProtocolVersion` 已在保存、查询、历史、复制、审核和发布链路生效；
2. 服务端构建、扫描、不可变产物和签名链路可用；
3. 主 `src` 与独立 `page/materials` 通过同一契约测试；
4. v2 元素实例版本锁定已生效；
5. v1/v2 混合回归通过；
6. 权限目录、可信上传角色和审核职责已配置；
7. 典型简单元素、复杂多模块元素、接口型元素和错误元素均完成试点；
8. 构建、加载、错误和 SDK 指标能够查询；
9. 生产 v2 路径不编译原始源码；
10. 安全团队书面确认首期可信主窗口执行边界。

---

## 23. 开发前必须冻结的决策

以下事项在第一期协议评审结束前必须冻结，后续不得由各端自行假设：

1. `ngap.json` JSON Schema 和字段命名；
2. `elementProtocolVersion` 后端字段及所有接口透传规则；
3. 元素版本、packageHash、manifestHash 和 artifactHash 的关系；
4. 首期可信上传角色和审核责任人；
5. 首期 SDK 版本、权限字典和能力目录来源；
6. 首期外部依赖及宿主精确版本；
7. ZIP、源码、样式和资源容量限制；
8. 构建任务状态、阶段、幂等键和保留策略；
9. 运行清单、对象存储不可变 URL、CORS、CSP 和签名方式；
10. 页面实例版本锁定字段及历史、复制、分享链路；
11. v1 新建停止时机和 v1 运行兼容年限；
12. 主 `src`、`materials`、`page` 的共享代码落点和发布方式。

---

## 24. 需求冻结结论

- 正式协议统一使用受约束 ZIP + `ngap.json`；
- `elementProtocolVersion = 2` 是生产上线硬门槛；
- 浏览器可以做预检和开发辅助，但不能产生可审核或可发布的正式产物；
- 正式预览、审核、发布和生产运行统一使用服务端冻结产物；
- v2 通过扁平 Props + `context` SDK 接入平台，不暴露内部 Store 和请求实现；
- SDK 权限不是主窗口安全沙箱，首期必须限制可信内部上传者并配合审核、扫描和签名；
- 新 v2 元素实例从第一天锁定版本与产物，发布新版本不静默改变存量页面；
- v1 与 v2 长期兼容，迁移通过辅助工具和人工确认逐步完成；
- 主 `src` 与独立 `page/materials` 必须共享核心语义，任何一期都不能只完成其中一个入口后视为正式交付。
