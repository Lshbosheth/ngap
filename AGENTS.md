# Workspace instructions

- On Windows, explicitly invoke PowerShell 7 through `pwsh` for PowerShell work. Use Windows PowerShell 5.1 only when an older script specifically requires it.
- Before working on this project in a new conversation, read `CODEX_CONTEXT.md` completely.
- For guided-process architecture or implementation work, also read `GUIDED_PROCESS_REDESIGN.md` completely.
- `REFACTOR_NOTES.md` records acquired API samples and remaining data gaps.
- Preserve compatibility with both the main `src` runtime and the independent `page` runtime.
