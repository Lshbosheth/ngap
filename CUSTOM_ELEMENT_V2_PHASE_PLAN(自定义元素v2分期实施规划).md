# 自定义元素 v2 分期实施规划

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 自定义元素 v2 分期实施规划 |
| 文档标识 | `CUSTOM_ELEMENT_V2_PHASE_PLAN` |
| 需求基线 | `CUSTOM_ELEMENT_V2_REQUIREMENTS(自定义元素v2需求规格).md` |
| 目标设计参考 | `CUSTOM_ELEMENT_REDESIGN(完整设计-理想方案).md` |
| 规划对象 | 产品、前端、后端、构建平台、测试、安全、运维和组件试点团队 |
| 规划状态 | 排期基线，需结合实际人员和后端能力评审后转为项目日历 |
| 分期数量 | 六期 |

本文只定义实施顺序、每期范围、工作量、依赖、交付物和退出门槛。字段、接口、文件和测试的详细实现由对应的 `CUSTOM_ELEMENT_V2_PHASE{N}_TECHNICAL_PLAN` 文档说明。

---

## 2. 排期原则

### 2.1 顺序原则

1. 先冻结协议和稳定身份，再开发上传页面；
2. 先建立服务端构建和冻结产物，再打通审核与生产发布；
3. 主 `src` 与独立 `page/materials` 分期实施，但正式推广前必须完成统一；
4. 页面实例版本锁定不能晚于 v2 正式推广；
5. v1 迁移在 v2 闭环稳定后实施，不能阻塞 v2 基础建设；
6. 浏览器预检或开发预览不能替代服务端正式构建；
7. 安全、测试、运维不是最后补做的独立阶段，每期都有对应门槛，第六期负责整体验收和推广收口。

### 2.2 质量原则

- 每期都必须产出可独立评审、可测试、可回滚的交付物；
- 每期开始前冻结输入契约，完成后通过退出门槛才进入依赖它的下一期；
- 任何正式运行能力都同时考虑 v1/v2、编辑器/独立运行页和资源生命周期；
- 需求编号作为排期、开发任务和测试用例的追踪主键；
- 不以“页面看起来能预览”为完成标准，必须验证数据回读、错误恢复和资源清理；
- 已发布元素版本和运行产物不可原地覆盖。

### 2.3 估算口径

- 1 人日按 8 小时有效开发/测试时间估算；
- 1 人月按 22 人日估算；
- 工作量包含设计细化、开发、自测、联调、缺陷修复和必要文档；
- 日历周期按基准团队并行投入估算，不等于所有角色人日直接相加；
- 未知后端接口、对象存储、CSP、生产构建环境和现有审核系统改造会影响排期；
- 若正式构建服务需从零建设基础设施，第二期和第四期应取估算上限；
- 当前仓库缺失二进制资源不作为自定义元素 v2 主线前置条件，但会影响全量视觉回归环境。

### 2.4 基准团队

| 角色 | 建议投入 |
|---|---:|
| 前端平台开发 | 2 人全程 |
| 后端/构建平台开发 | 2 人，第二至第五期重点投入 |
| 测试 | 1 人全程，第三至第六期可增至 2 人 |
| 产品/需求 | 0.5 人全程 |
| 架构师 | 0.3～0.5 人，第一、二、四期重点投入 |
| 安全 | 0.3～0.5 人，第一、二、四、六期重点投入 |
| 运维/对象存储/CDN | 0.2～0.5 人，第二、四、六期投入 |
| 试点组件开发者 | 第三至第六期按需投入 1～2 人 |

如果只有 1 名前端和 1 名后端，建议按工作量线性延长，不通过砍掉生产构建、协议字段或双运行时验收来压缩周期。

---

## 3. 总体路线

```text
第一期：协议、数据契约与静态分析基础
  ↓
第二期：服务端构建与可信预览
  ↓
第三期：元素管理与主编辑器接入
  ↓
第四期：独立运行时与生产安全发布
  ↓
第五期：版本升级与 v1 迁移
  ↓
第六期：整体验收与推广准备
```

六期不是六个互相独立的版本。第一至第四期构成 v2 从源包到正式运行的最小生产闭环；第五期补齐长期版本治理和存量迁移；第六期完成正式推广门槛。

---

## 4. 第一期：协议、数据契约与静态分析基础

### 4.1 目标

冻结 v2 的稳定语言和身份体系，完成不执行用户源码的 ZIP 预检、`ngap.json` 校验、静态分析、平台组件清单和 SDK 公共契约，为服务端构建、上传页面和双运行时提供唯一规范。

### 4.2 主要范围

#### 产品与协议

- 冻结统一术语和命名；
- 冻结 ZIP 标准目录、包根识别和文件策略；
- 冻结 `ngap.json` JSON Schema；
- 冻结 `elementProtocolVersion = 2`；
- 冻结 `elementId`、`elementVersion`、`packageHash`、`manifestHash`、`artifactHash` 的关系；
- 冻结平台组件清单和运行清单的职责边界；
- 冻结诊断等级、错误码段和动作阻塞规则。

#### 前端/共享核心

- 组件源包读取和安全预检；
- 包根目录规范化；
- 路径、文件类型、容量和敏感文件校验；
- `ngap.json` 读取、规范化和校验；
- 包内模块图；
- 默认导出分析；
- 基础 Props、默认值、事件和简单方法候选分析；
- 平台组件清单合并与校验；
- 平台组件清单转换为旧 Schema；
- 诊断结构和稳定错误码；
- `@ngap/component-sdk` 类型、权限字典、错误类型和本地 mock 骨架；
- 主 `src` 与独立运行时 host adapter 契约测试骨架。

#### 后端契约

- 确认元素主数据、源包、构建任务和元素版本的数据对象；
- 确认 `elementProtocolVersion` 在保存、查询、审核、历史、复制和发布链路的透传；
- 冻结构建任务 API 契约草案；
- 冻结 SDK 能力目录来源和权限审核字段；
- 确认对象存储、不可变 URL、CORS 和 CSP 基线。

### 4.3 需求覆盖

主要覆盖：`CE-PKG-*`、`CE-ANA-*`、`CE-SDK-001～010`、`CE-BLD-001`、`CE-VER` 身份规则、`CE-CMP` 协议识别基础、`CE-NFR-007`。

### 4.4 本期不包含

- 不执行用户组件；
- 不提供可发布构建产物；
- 不接元素管理完整向导；
- 不修改正式 `NgapRender` 渲染分流；
- 不开放生产 v2 菜单；
- 不实现页面版本升级；
- 不停止新建 v1。

### 4.5 交付物

- 协议评审纪要和冻结版本；
- `ngap.json` Schema 与合法/非法样例；
- 平台组件清单类型和规范化规则；
- ZIP package reader 和 analyzer；
- manifest-to-legacy-schema 转换器；
- SDK 公共类型、错误类型、首期权限目录草案和 mock；
- 诊断错误码目录；
- 后端数据及接口契约评审稿；
- 单元测试 fixture 集；
- `CUSTOM_ELEMENT_V2_PHASE1_TECHNICAL_PLAN`。

### 4.6 退出门槛

1. 最小合法包、完整合法包、非法路径包、超限包、缺清单包和复杂类型包均通过约定诊断；
2. 分析过程没有执行用户源码；
3. `ngap.json`、平台组件清单和运行清单不再混用；
4. `elementProtocolVersion` 被后端书面确认是生产硬字段；
5. 基础 Props 可生成当前 ConfigPanel 可读取的 Schema；
6. 推导失败会列出待补充字段和修复模板；
7. SDK 首期能力、权限命名和安全边界经架构与安全评审；
8. 主、独立宿主 adapter 契约测试骨架可运行；
9. 核心单元测试通过，不能靠浏览器手工演示代替。

### 4.7 工作量与周期

| 角色 | 人日 |
|---|---:|
| 前端平台 | 28～38 |
| 后端 | 12～18 |
| 测试 | 8～12 |
| 产品/架构 | 8～12 |
| 安全/运维 | 5～8 |
| 合计 | 61～88 |

基准日历周期：**4～6 周**。

---

## 5. 第二期：服务端构建与可信预览

### 5.1 目标

建立从不可变源包到可加载 ESM/CSS/Assets 的服务端异步构建闭环，生成平台组件清单和运行清单，并以 session 隔离的统一 registry 完成可信预览。

### 5.2 主要范围

#### 源包与构建服务

- 源包上传、服务端 hash 和不可变存储；
- 隔离工作区重新解包；
- 服务端重复执行包、路径、容量和 MIME 校验；
- 模块解析和依赖白名单；
- TS/TSX/JS/JSX 构建；
- Less/CSS 编译、作用域和资源 URL 重写；
- PNG/JPG/JPEG/GIF/WebP/SVG 资源处理；
- 源码和构建产物静态扫描；
- 异步构建状态、阶段、进度、取消、超时和幂等；
- 生成 packageHash、manifestHash 和 artifactHash；
- 生成 preview `runtime-manifest.json`；
- 预览产物保留和垃圾回收。

#### 预览运行核心

- 统一 Custom Element Registry；
- preview session 与 artifactKey；
- ESM、CSS 和 assets 加载；
- style 引用计数和释放；
- registry 并发去重、失败状态、重试和清理；
- v2 Props adapter；
- 基础 SDK mock host；
- 元素级 Error Boundary；
- 真实 `NgapRender` 路径的预览宿主；
- 属性调整、事件日志和方法验证。

#### 用户体验

- 浏览器预检反馈；
- 构建进度和排队提示；
- 离开页面后恢复 buildId；
- 失败诊断的文件、行列、错误码和建议；
- 不要求源包未变化时重复上传；
- 预览关闭后清理全部临时资源。

### 5.3 需求覆盖

主要覆盖：`CE-BLD-001～010`、`CE-UX` 预检/构建/预览部分、`CE-RT-001～007` 基础、`CE-SEC-002～006`、`CE-NFR-003～005/009`。

### 5.4 本期不包含

- 不接完整元素保存、审核和发布；
- 不把 v2 元素正式加入应用编辑器菜单；
- 不完成独立 `page/materials` 生产加载；
- preview 产物不等于可发布产物；
- 不实施页面实例版本升级；
- 不提供 v1 转换。

### 5.5 交付物

- 源包上传和构建 API；
- 构建任务状态机、幂等键和任务清理；
- 服务端 builder、scanner、artifact publisher；
- preview runtime manifest；
- Registry、style registry、Props adapter、Error Boundary；
- v2 Preview Host；
- 构建/预览监控初版；
- 服务端和前端集成测试；
- `CUSTOM_ELEMENT_V2_PHASE2_TECHNICAL_PLAN`。

### 5.6 退出门槛

1. 标准多模块 ZIP 能由服务端构建并在真实 adapter 中预览；
2. 构建全阶段状态可查询，失败可定位，取消和超时能清理工作区；
3. 同幂等输入复用产物，策略或 builder 变化生成新 artifactHash；
4. 非白名单依赖、危险源码、危险资源和 CSS 被阻止；
5. 多预览会话互不污染；
6. 关闭预览后无 window 全局、Blob、style、ref 和订阅残留；
7. 单个组件渲染异常被元素级错误边界捕获；
8. preview runtime manifest、ESM、CSS 和资源 URL 均可回溯到 buildId；
9. 预览不再使用 `window.MyComponent`、`window.MyComponentJsData` 或 `typeZDY`。

### 5.7 工作量与周期

| 角色 | 人日 |
|---|---:|
| 前端平台 | 24～34 |
| 后端/构建平台 | 38～52 |
| 测试 | 14～20 |
| 架构/安全 | 8～14 |
| 运维/对象存储 | 8～12 |
| 合计 | 92～132 |

基准日历周期：**6～8 周**。

---

## 6. 第三期：元素管理与主编辑器接入

### 6.1 目标

把 v2 的源包、分析、构建和预览能力接入现有元素管理生命周期，并在主 `src` 的应用编辑器、业务组件编辑器、属性面板、事件面板、方法调用和 `NgapRender` 中正式使用。

### 6.2 主要范围

#### 元素管理

- 五步式 v2 新建/编辑向导；
- 基础信息、ZIP 文件树、分析结果和配置补充；
- Props、事件、方法、依赖和权限差异编辑；
- 构建任务恢复；
- 正式预览；
- 保存草稿、重新打开和继续编辑；
- 提交审核、驳回回显和再次提交；
- 列表协议版本标识；
- 过渡期 v1 新建入口隔离；
- 编辑已发布元素时创建新草稿版本。

#### 主编辑器运行链路

- `src/packages/index.tsx` 接统一 registry facade；
- 元素菜单保存 protocolVersion、version、artifactHash；
- Schema resolver；
- ConfigPanel 去除临时全局预览特判；
- DragMenuItem/editor 按版本解析 Schema；
- `NgapRender` v1/v2 分流、扁平 Props、事件和 ref；
- 主 `src` SDK host adapter；
- 应用编辑器和业务组件编辑器接入；
- 页面保存、回读、复制、粘贴、撤销和重做保留 v2 字段。

#### 审核接口初步接入

- 保存 buildId、packageHash、manifestHash、artifactHash；
- 提交审核冻结输入；
- 审核预览读取冻结 preview/publish candidate 产物；
- 权限差异和依赖差异展示。

### 6.3 需求覆盖

主要覆盖：`CE-UX-*`、`CE-LFC-001～006` 的管理端部分、`CE-RT` 主 `src` 部分、`CE-SDK` 主 host、`CE-NFR-006` 主入口、`CE-ACC-017/018/020/022`。

### 6.4 本期不包含

- 独立 `page/materials` 尚未作为正式生产 v2 入口；
- 未完成签名校验前不得全量发布生产；
- 页面升级和批量版本治理留到第五期；
- 不停止 v1 新建；
- 不提供外部上传或容器元素。

### 6.5 交付物

- `CustomElementV2Editor` 五步向导；
- 分析、诊断、构建、权限和预览组件；
- 主 `src` Registry Host 和 SDK Host；
- v2 Schema Resolver 和菜单接入；
- 主 `NgapRender` v1/v2 分流；
- 元素保存/审核字段接入；
- 主编辑器集成自动化测试；
- 一组内部试点元素；
- `CUSTOM_ELEMENT_V2_PHASE3_TECHNICAL_PLAN`。

### 6.6 退出门槛

1. v2 草稿可以保存、退出、重新打开并继续；
2. 服务端构建成功后可在向导内真实预览；
3. 提交审核冻结所有 hash、权限和依赖；
4. 已发布候选元素可拖入应用和业务组件编辑器；
5. 属性编辑器类型、默认值、事件和方法完整工作；
6. 页面保存和刷新不丢 protocolVersion、elementVersion 和 artifactHash；
7. 复制、粘贴、删除、撤销和重做不丢 v2 数据；
8. v1 与 v2 在主编辑器同页正常；
9. v1 原有功能回归通过；
10. 此期只允许内部试点，不得宣称独立运行页已完成。

### 6.7 工作量与周期

| 角色 | 人日 |
|---|---:|
| 前端平台 | 40～52 |
| 后端 | 16～24 |
| 测试 | 18～26 |
| 产品/交互 | 6～10 |
| 安全审核 | 4～7 |
| 合计 | 84～119 |

基准日历周期：**6～8 周**。

---

## 7. 第四期：独立运行时与生产安全发布

### 7.1 目标

完成独立 `page/materials` 运行链路、正式 publish build、不可变产物、审核发布一致性、签名校验和生产监控，使 v2 具备受控生产试点条件。

### 7.2 主要范围

#### 独立运行时

- `materials` 使用共享 registry 和 runtime host；
- `page` 批量查询运行信息；
- 按 `elementId + version + artifactHash` 去重；
- v2 加载 runtime manifest、ESM、CSS 和 assets；
- v1 走共享 legacy loader；
- 组装式页面和引导式流程页面；
- 流程后续节点增量加载；
- 单元素失败隔离；
- 页面销毁清理；
- 生产 v2 路径不加载 Babel、JSZip 或 analyzer。

#### 生产构建与发布

- preview build 与 publish build 目的隔离；
- publish build 固化 builder、dependency policy、SDK policy 和 scanner 版本；
- 不可变运行清单、ESM、CSS 和 assets；
- 内容寻址 URL；
- manifest 和 artifact hash；
- 平台签名与运行时校验；
- 审核预览和发布严格复用同一冻结产物；
- 发布、下线和回滚运行信息；
- 签名失败、产物缺失和 SDK 不兼容的安全占位。

#### SDK 正式宿主

- 主 `src` 和独立 runtime host adapter 能力对齐；
- variables、configured API、declared API、events、UI、files、navigation、user、storage、logger；
- 受控 integration capability；
- 权限裁剪、调用审计、超时、取消、限流、结果大小和错误归一化；
- 订阅强制释放；
- host adapter contract suite。

#### 运维和安全

- 产物域名、MIME、CORS、CSP；
- 构建队列容量、隔离和配额；
- 产物保留、回收和回滚；
- 加载、渲染、SDK 和签名指标；
- 可信上传和审核权限配置。

### 7.3 需求覆盖

主要覆盖：`CE-RT-008～013`、`CE-BLD` 生产部分、`CE-SDK` 正式能力、`CE-SEC-*`、`CE-LFC` 发布一致性、`CE-NFR-001～010`、`CE-ACC-011～016/019～024/027`。

### 7.4 本期不包含

- 不开放外部团队上传；
- 不实施 sandbox iframe；
- 不进行大规模 v1 转换；
- 不开放容器元素；
- 不自动升级页面元素版本。

### 7.5 交付物

- 独立 `page/materials` v2 runtime host；
- 批量运行信息接口；
- 正式 publish build 和不可变运行清单；
- 签名与运行时校验；
- 完整 SDK 双宿主实现及契约测试；
- v1/v2 双运行时兼容 loader；
- 组装式和引导式端到端测试；
- 生产指标、告警和应急回退手册；
- `CUSTOM_ELEMENT_V2_PHASE4_TECHNICAL_PLAN`。

### 7.6 退出门槛

1. 同一冻结产物在主预览和独立运行页表现一致；
2. 组装式和引导式页面均能运行 v1/v2 混合元素；
3. 独立运行页按需加载且纯 v2 路径不加载 Babel/ZIP/analyzer；
4. 审核、发布和运行的 artifactHash 完全一致；
5. 产物 URL 不可变，签名或 hash 不一致时拒绝执行；
6. 一个元素失败不影响其他元素和页面主流程；
7. SDK 两宿主通过同一契约测试；
8. 权限、超时、取消、限流、大小限制和安全错误模型生效；
9. 构建和运行监控可定位 elementId/version/artifactHash；
10. 安全、运维和架构共同签署内部生产试点结论。

### 7.7 工作量与周期

| 角色 | 人日 |
|---|---:|
| 前端平台 | 32～44 |
| 后端/构建平台 | 28～40 |
| 测试 | 22～32 |
| 安全/架构 | 12～18 |
| 运维 | 8～14 |
| 合计 | 102～148 |

基准日历周期：**6～9 周**。

---

## 8. 第五期：版本升级与 v1 迁移

### 8.1 目标

建立元素版本使用关系、页面实例锁定、显式升级、声明式配置迁移、精确回滚和 v1 转 v2 辅助能力，使 v2 具备长期演进和存量治理能力。

### 8.2 主要范围

#### 页面版本治理

- 新 v2 实例锁定 protocolVersion、elementVersion 和 artifactHash；
- 保存、历史、复制、模板、分享和发布链路保留字段；
- 元素版本列表和更新日志；
- 页面打开时检测新版本；
- 当前/目标版本公开契约差异；
- 单实例和批量使用关系查询；
- 显式升级确认；
- 升级预览；
- 声明式 renameProps、removeProps 和 defaults 迁移；
- 复杂迁移人工确认；
- 精确版本回滚。

#### 构建器版本治理

- 构建器版本支持策略；
- 旧产物继续运行；
- 可选重新构建；
- builder major 迁移评估、失败清单和截止策略；
- 不允许重建后覆盖同版本原产物。

#### v1 迁移

- v1 兼容性扫描；
- 自动、人工、不可迁移三级报告；
- 标准 Schema 转 `ngap.json`；
- TSX/Less 组装为 v2 源包草稿；
- 内部 Store、request、window 和函数型 Schema 风险提示；
- 创建新 v2 版本并走完整构建审核；
- v1 使用量统计；
- v1 新建停止条件评审。

### 8.3 需求覆盖

主要覆盖：`CE-VER-*`、`CE-CMP-*`、`CE-ACC-025/028～031`、构建器升级策略和使用关系要求。

### 8.4 本期不包含

- 不强制一次性迁移全部 v1；
- 不删除 v1 运行兼容；
- 不允许任意 JavaScript 迁移函数；
- 不开放公共市场；
- 不实施外部上传和 iframe。

### 8.5 交付物

- 元素版本和使用关系服务；
- 页面实例锁定字段全链路；
- 编辑器版本提示和升级向导；
- 配置差异与声明式迁移器；
- 精确回滚；
- v1 兼容性检查和 ZIP 草稿生成器；
- v1 使用统计和收口建议；
- 构建器版本治理规则；
- `CUSTOM_ELEMENT_V2_PHASE5_TECHNICAL_PLAN`。

### 8.6 退出门槛

1. 发布新版本不影响任何已锁定页面；
2. 缺少版本的旧数据被明确标识而非静默视为已锁定；
3. 页面升级前能看到 Props、事件、方法、依赖和权限差异；
4. 声明式迁移可预览、确认、保存和回滚；
5. 复杂迁移不会自动执行不透明脚本；
6. 精确旧 version + artifactHash 可回滚运行；
7. 典型 v1 元素可生成 v2 草稿并通过完整审核；
8. 无法自动迁移的元素得到明确原因和人工操作建议；
9. v1 运行兼容回归通过；
10. 是否停止新建 v1 形成产品和运维评审结论。

### 8.7 工作量与周期

| 角色 | 人日 |
|---|---:|
| 前端平台 | 28～40 |
| 后端 | 22～32 |
| 测试 | 18～26 |
| 产品/数据治理 | 6～10 |
| 试点迁移团队 | 6～12 |
| 合计 | 80～120 |

基准日历周期：**5～8 周**。

---

## 9. 第六期：整体验收与推广准备

### 9.1 目标

用真实组件和真实页面完成端到端验收、容量与稳定性验证、生产演练、开发者支持材料和运营治理，达到正式推广门槛。

### 9.2 主要范围

#### 试点矩阵

- 简单展示元素；
- 表单交互元素；
- 多模块、样式和图片元素；
- configured API 元素；
- declared API 元素；
- 文件上传元素；
- 事件和方法元素；
- 同一页面多实例；
- 同一页面多版本；
- v1/v2 混合；
- 组装式和引导式页面；
- 故意构建失败、加载失败和渲染失败元素。

#### 整体测试

- 协议、analyzer、manifest、SDK、registry 自动化回归；
- 后端构建全状态和故障恢复；
- 元素生命周期全链路；
- 双运行时一致性；
- 版本升级、回滚和 v1 迁移；
- 性能、容量、内存和资源清理；
- 安全攻击样例和签名篡改；
- 构建队列和对象存储故障演练；
- 灰度、回退和下线演练。

#### 开发者体验

- 官方组件模板 ZIP；
- `@ngap/component-sdk` 发布说明；
- 能力和权限目录；
- `ngap.json` 参考手册；
- 常见错误码与修复手册；
- 本地 mock 和示例；
- v1 迁移指南；
- 上传、审核和发布操作手册。

#### 推广治理

- 上传角色和审核角色上线；
- 服务级指标和告警阈值；
- 构建器和 SDK 版本支持策略；
- 源包、失败产物、已发布产物保留策略；
- v1 新建策略；
- 试点复盘和正式推广范围。

### 9.3 需求覆盖

覆盖需求文档全部 `CE-ACC-*`、推广门槛、非功能要求和运行治理要求。

### 9.4 本期不包含

- 不以推广验收为名新增容器元素、iframe 或组件市场；
- 不强制迁移所有 v1；
- 不临时放宽扫描、签名和双运行时要求来通过验收。

### 9.5 交付物

- 全量测试报告和需求追踪矩阵；
- 性能、容量、安全和故障演练报告；
- 试点组件与试点页面；
- SDK、模板、示例和开发者文档；
- 运维监控、告警和应急手册；
- v1/v2 运营数据看板；
- 推广评审结论和遗留事项清单；
- `CUSTOM_ELEMENT_V2_PHASE6_TECHNICAL_PLAN`。

### 9.6 退出门槛

1. 需求文档中的所有推广门槛均有证据；
2. 所有 P0/P1 缺陷关闭，P2 有责任人、影响和计划；
3. 双运行时、v1/v2、版本升级和签名篡改回归通过；
4. 生产构建队列、对象存储和运行加载有监控与告警；
5. 回退演练可恢复到已知稳定元素版本和平台版本；
6. 开发者能使用官方模板独立完成一个元素的开发、上传和审核；
7. 安全、架构、测试、运维和产品共同签署推广结论；
8. 未达到强隔离前，上传角色仍限制为可信内部人员。

### 9.7 工作量与周期

| 角色 | 人日 |
|---|---:|
| 前端平台 | 12～20 |
| 后端/构建平台 | 10～18 |
| 测试 | 28～40 |
| 安全/架构 | 10～16 |
| 运维 | 8～14 |
| 产品/文档/试点 | 10～18 |
| 合计 | 78～126 |

基准日历周期：**4～7 周**。

---

## 10. 汇总排期

### 10.1 各期汇总

| 期次 | 主题 | 工作量（人日） | 基准周期 | 主要退出结果 |
|---|---|---:|---:|---|
| 第一期 | 协议、数据契约与静态分析基础 | 61～88 | 4～6 周 | 协议和 analyzer 可作为唯一基础 |
| 第二期 | 服务端构建与可信预览 | 92～132 | 6～8 周 | ZIP 可产生可信预览产物 |
| 第三期 | 元素管理与主编辑器接入 | 84～119 | 6～8 周 | 主编辑器内部试点闭环 |
| 第四期 | 独立运行时与生产安全发布 | 102～148 | 6～9 周 | 双运行时生产试点闭环 |
| 第五期 | 版本升级与 v1 迁移 | 80～120 | 5～8 周 | 版本治理和迁移能力 |
| 第六期 | 整体验收与推广准备 | 78～126 | 4～7 周 | 达到正式推广门槛 |
| 合计 |  | **497～733** | 顺序相加 31～46 周 | 完整 v2 长期能力 |

总工作量约 **23～33 人月**。这是包含前端、后端、测试、安全、运维、文档和试点的完整团队工作量，不等于单一开发角色工作量。

### 10.2 建议日历

在基准团队稳定投入、接口和基础设施按时提供的前提下，可以通过受控并行把日历压缩到约 **26～34 周**：

```text
第 1～6 周     第一期
第 5～12 周    第二期（第 5～6 周只并行非冻结部分）
第 10～18 周   第三期（依赖第二期最小 preview build）
第 16～25 周   第四期（可提前准备 runtime host 和契约测试）
第 22～30 周   第五期（先做使用关系和页面字段核查）
第 27～34 周   第六期（自动化和文档可提前准备）
```

这是建议窗口，不是承诺日期。若 `elementProtocolVersion`、构建服务、对象存储或签名方案未确认，相关阶段不得以模拟结果替代退出门槛。

### 10.3 最小生产闭环

若业务只关心“第一个可控 v2 元素何时能进入生产试点”，最小生产闭环是第一至第四期，预计：

- 工作量：339～487 人日；
- 顺序日历：22～31 周；
- 受控并行日历：约 18～25 周；
- 前提：2 名前端、2 名后端/构建、1～2 名测试以及安全和运维兼职支持。

不能把第一至第三期的主编辑器试点视为完整生产上线，因为独立运行页、签名、双宿主 SDK 和生产加载仍在第四期。

---

## 11. 依赖与并行关系

### 11.1 硬依赖

| 后置工作 | 硬依赖 |
|---|---|
| 第二期构建服务 | 第一期 `ngap.json`、manifest、diagnostic、hash 和依赖策略 |
| 第三期正式向导预览 | 第二期 preview build 与 registry |
| 第四期生产发布 | 第二期构建产物 + 第三期审核冻结信息 |
| 第五期版本升级 | 第三期页面字段 + 第四期不可变版本运行能力 |
| 第六期推广 | 第一至第五期退出门槛全部满足 |

### 11.2 可提前并行

- 第一期后半段可准备构建服务基础设施和对象存储路径；
- 第二期可并行开发 registry 与服务端 builder，但必须使用同一 runtime manifest；
- 第二期后半段可搭建第三期五步向导壳和状态管理；
- 第三期可提前为 `materials/page` 建立 host adapter 契约测试；
- 第四期可提前建设第五期的元素使用关系查询；
- 各期自动化测试 fixture、错误码文档和监控埋点可持续建设；
- 第三期起可持续制作试点组件，但未达到对应门槛前不得扩大使用范围。

### 11.3 不允许的伪并行

- 前后端分别定义一套 `ngap.json` 或 runtime manifest；
- 主 `src` 与 `materials` 复制实现后声称并行完成；
- 上传向导在构建 API 未冻结前把 mock DTO 当正式接口；
- 签名方案未定时先发布可变 URL；
- 页面版本字段未贯通历史、复制和分享链路时先开放自动升级；
- 安全审核未完成时扩大上传角色。

---

## 12. 里程碑

| 里程碑 | 达成条件 |
|---|---|
| M1 协议冻结 | 第一期退出门槛全部通过 |
| M2 可信预览 | 服务端构建标准 ZIP，真实 adapter 可预览且资源可清理 |
| M3 主编辑器试点 | v2 可保存、审核候选、拖入主编辑器并回读 |
| M4 生产试点 | 双运行时、签名、SDK 和审核发布一致性通过 |
| M5 长期治理 | 页面版本升级、回滚和 v1 迁移可用 |
| M6 正式推广 | 第六期联合验收和生产演练通过 |

---

## 13. 风险缓冲与变更控制

### 13.1 建议缓冲

- 第二期服务端构建和扫描增加 20% 技术风险缓冲；
- 第四期独立运行时、CSP、签名和 SDK 宿主增加 20% 联调缓冲；
- 第五期页面历史、复制、分享和发布链路若后端现状不明，增加 15% 数据迁移缓冲；
- 第六期至少保留 2 周缺陷收敛窗口，不安排新范围。

### 13.2 触发重排期的事项

以下变化需要重新估算，而不是直接塞入当前期：

- 首期增加容器元素；
- 首期改为 sandbox iframe；
- 开放任意 npm 依赖或自定义依赖上传；
- 允许外部团队或租户上传；
- 增加在线多文件 IDE、Git 仓库同步或组件市场；
- 改变现有审核、发布和元素状态体系；
- 要求所有 v1 在推广前完成迁移；
- 扩大浏览器兼容范围到项目当前未支持环境。

### 13.3 降级原则

如果服务端构建或签名延期：

- 可以继续开发协议、analyzer、上传向导和内部开发预览；
- 可以在隔离测试环境进行受控演示；
- 不允许把浏览器 Blob 构建物标记为正式发布版本；
- 不允许独立运行页生产时下载 ZIP 并现场编译；
- 正式推广里程碑相应顺延。

---

## 14. 当前执行位置

当前已完成：

- 完整阅读和核对现有自定义元素理想设计、问题清单、务实版和领导版方案；
- 生成 `CUSTOM_ELEMENT_V2_REQUIREMENTS(自定义元素v2需求规格).md`；
- 将正式命名收口为 `ngap.json`、`elementProtocolVersion`、平台 SDK `context`、服务端构建产物和版本锁定；
- 形成六期产品和项目排期基线；
- 按本规划完成六份分期开发方案。

已生成的分期开发方案：

1. `CUSTOM_ELEMENT_V2_PHASE1_TECHNICAL_PLAN(自定义元素v2一期协议契约与静态分析开发方案).md`；
2. `CUSTOM_ELEMENT_V2_PHASE2_TECHNICAL_PLAN(自定义元素v2二期服务端构建与可信预览开发方案).md`；
3. `CUSTOM_ELEMENT_V2_PHASE3_TECHNICAL_PLAN(自定义元素v2三期元素管理与主编辑器开发方案).md`；
4. `CUSTOM_ELEMENT_V2_PHASE4_TECHNICAL_PLAN(自定义元素v2四期独立运行时与生产发布开发方案).md`；
5. `CUSTOM_ELEMENT_V2_PHASE5_TECHNICAL_PLAN(自定义元素v2五期版本升级与v1迁移开发方案).md`；
6. `CUSTOM_ELEMENT_V2_PHASE6_TECHNICAL_PLAN(自定义元素v2六期整体验收与推广准备开发方案).md`。

下一步进入需求、排期和一期方案联合评审，优先冻结需求文档第 23 节列出的协议、数据、权限、依赖、容量和共享代码决策；评审通过前不生成业务代码。
