# 引导式三期正式输出、诊断结果与动态分支开发方案

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 引导式三期正式输出、诊断结果与动态分支开发方案 |
| 文档性质 | 第三期开发实施方案，不代表已经修改业务代码 |
| 对应阶段 | 第三期：正式输出、诊断结果与动态分支 |
| 当前状态 | 待技术评审，尚未开始正式代码开发 |
| 前置阶段 | 第二期组件作用域、稳定变量身份和结构化引用已达到开发完成门槛 |
| 原始代码基线 | `D:\download\repomix-output\repomix-output.xml` 还原出的项目源码 |
| 前端工程 | 当前 `ngap` 工作区，包含主 `src` 与独立 `page/materials` 两套运行入口 |
| 后端工程 | 当前工作区未提供，接口、存储、历史版本和发布校验由后端在实际工程复核 |
| 分期规划 | `GUIDED_PROCESS_PHASE_PLAN(引导式流程分期实施规划).md` |
| 二期方案 | `GUIDED_PROCESS_PHASE2_TECHNICAL_PLAN(引导式二期组件作用域与引用体系开发方案).md` |
| 总体设计 | `GUIDED_PROCESS_REDESIGN(引导式流程展示编排升级设计).md` |
| 决策记录 | `GUIDED_PROCESS_PENDING_ITEMS(引导式待完善事项).md` |

第三期不是再增加一套全局变量，也不是把当前 `conditionList` 换一个名字。本期要在二期组件作用域之上建立一条稳定链路：

```text
组件内部表单 / 接口数据 / 组件变量
            ↓
组件内部事件与脚本完成业务计算
            ↓
发布正式输出快照
            ↓
┌───────────────────┬────────────────────┐
│ 分支使用稳定 value │ 智能导航使用 label / 诊断明细 │
└───────────────────┴────────────────────┘
            ↓
命中唯一后继分支并继续加载
```

正式输出属于组件实例对流程运行器公开的业务结果，不等于“把组件内全部变量复制到全局”。

---

## 2. 本期目标和交付结果

### 2.1 产品结果

第三期完成后，配置人员应能做到：

- 为通用业务组件声明稳定的正式输出项；
- 在某个流程节点中，把组件变量、表单值或接口结果映射为正式输出；
- 让人工节点在选择或输入变化后，通过事件脚本更新组件变量并重新计算正式输出；
- 让自动节点在接口和初始化动作完成后发布正式输出，并静默选择分支；
- 分别配置用于分支判断的 `value` 和用于智能导航展示的 `label`；
- 一个节点在导航区域展示多条诊断信息，而分支仍只使用一个稳定结果码；
- 为自动节点显式映射 `success` / `failure` 业务诊断状态；
- 配置多个条件出口，并保证一次只能命中一个非默认分支；
- 配置至多一个默认分支；
- 当人工输入尚不完整或校验未通过时停留在当前节点，不提前加载后续节点；
- 在主预览和独立运行页得到一致的输出、诊断、分支和连续自动推进结果。

### 2.2 技术结果

本期至少交付：

- 引导式配置协议 `guidedSchemaVersion = 3`；
- 稳定 `outputId` 的组件正式输出定义；
- 节点实例级输出映射、结果绑定和诊断绑定；
- 统一的 `NodeFormalResult` 运行时快照；
- 人工、自动两种交互模式和明确的完成触发策略；
- 以正式输出为输入的统一分支求值器；
- 唯一命中、默认分支和无匹配的配置及运行校验；
- 智能导航多条诊断展示协议；
- 主 `src` 和独立 `page` 共享的输出及分支核心；
- 旧 MT / AT / VA 配置的兼容读取与受控迁移入口；
- 为第四期路径回滚预留的结果修订号和“分支需要协调”事件。

---

## 3. 已冻结的产品规则

以下规则是第三期开发基线，不在开发过程中重新发散。

### 3.1 正式输出的定位

1. 组件内部可以有任意数量的表单值、接口数据和组件变量。
2. 组件内部负责业务计算；平台不理解“金额和套餐如何组合”。
3. 只有被声明并映射为正式输出的值，才能作为标准分支和诊断数据源。
4. 正式输出默认保存在当前节点运行作用域，不自动写入流程全局变量。
5. 其他节点如需读取，可通过二期的 `nodeId + outputId` 结构化引用读取。
6. 确实需要跨节点共享和修改的数据，仍显式写入流程全局变量。

### 3.2 value 与 label

- `value` 是稳定的机器结果，例如 `PLAN_8`、`PLAN_8_49`、`PLAN_49_PLUS`；
- `label` 是面向用户的可读结果，例如“建议办理 8 元套餐”；
- 分支默认使用 `value`，不使用可变文案；
- 智能导航可以展示 `label`，也可以展示多条额外诊断明细；
- 修改 label 而 value 不变时，只刷新展示，不重新选择分支；
- value 变化时，才认为节点业务结论发生变化。

### 3.3 自动节点

- 自动节点的接口或初始化动作完成后自动生成正式输出；
- 自动节点若有多个条件出口，运行器静默判断并继续执行；
- 自动节点后面仍是自动节点时继续连续加载，不要求组件额外发出“下一步”按钮动作；
- 自动节点没有条件分支且只有唯一出口时，不强制配置决策 value；
- 自动节点的业务诊断状态由组件或接口显式输出 `success` / `failure`；
- 平台不根据“列表是否为空”自行猜测业务成功或失败；
- 接口异常、脚本异常属于技术错误，和业务 `failure` 分开处理，并停止继续推进。

### 3.4 人工节点

- 人工节点可在下拉、多选、输入、点选等事件中运行脚本并给组件变量赋值；
- 组件变量变化后重新生成正式输出；
- 人工节点不一律要求再点击“确认”或“完成”；
- 简单选择可以在 change 后直接产生有效结果并推进；
- 多字段场景可在全部必填条件满足且输出有效后推进；
- 确实需要业务确认或接口校验的场景，可以配置显式完成信号；
- 人工诊断按点选或输入结果展示，不强制使用红绿状态；
- 输入不完整或校验不通过时，不发布有效业务结论，也不继续加载。

### 3.5 分支与结束

- 一个节点同一次求值最多只能命中一个非默认分支；
- 一个节点最多只有一个默认分支；
- 非默认分支均未命中时，才使用默认分支；
- 未配置默认分支且无匹配时，停在当前节点并显示配置错误；
- 所有流程无论经过哪条路径，最终都必须连接到唯一结束节点；
- 有 Footer 时，活动 Content 路径只能先到 Footer，再由 Footer 的唯一后继到结束节点。

---

## 4. 本期范围边界

### 4.1 本期包含

- 正式输出定义、映射、校验、发布和读取；
- 人工/自动节点交互模式；
- change 事件驱动的组件变量计算与结果重算；
- 自动动作完成后的结果发布和连续推进；
- value/label 与多条诊断信息；
- 自动节点 success/failure 业务状态；
- 基于正式输出的分支编辑和运行求值；
- 分支唯一命中、默认分支、无匹配和多匹配处理；
- 输出依赖删除、改名、类型修改时的影响分析；
- 主预览、独立运行页的一致实现；
- 为第四期输出结果变化后的路径协调提供事件和数据。

### 4.2 本期不包含

- 已加载旧路径的完整卸载、取消请求、数据回收和新路径重放；
- 任意回退到历史环节；
- 浏览器刷新后的运行态恢复；
- 跨设备继续运行；
- 业务运行日志平台化；
- 通用表达式沙箱和全项目脚本安全治理；
- 画布上下/左右方向切换及 150 节点性能优化；
- 移动端和可访问性专项；
- Header/Footer 结构重做。

### 4.3 与第四期的明确边界

第三期负责判断“现在正确的结果和目标分支是什么”，第四期负责处理“旧分支已经加载后如何安全切换”。

第三期中如果上游 value 在后续节点已经进入后发生变化：

1. 当前节点仍重新计算并保存最新正式输出；
2. 当前诊断展示可立即更新；
3. 运行器计算新的目标分支；
4. 运行器发出 `BRANCH_RECONCILIATION_REQUIRED`；
5. 第三期不直接把新分支追加到旧分支后，也不使用当前简单数组截断冒充回滚；
6. 第四期实现后，由路径协调器释放旧路径并加载新路径。

因此第三期单独试运行时，流程应满足以下任一条件：

- 后续加载后锁定前序输入；
- 业务页面本身不允许返回修改；
- 试点场景不会在后续进入后修改前序结果。

---

## 5. 当前实现问题

### 5.1 当前分支直接依赖控件细节

现有人工分支主要通过 `atomId + filedKey + relation + value` 读取控件值。它会造成：

- 分支知道组件内部有哪些下拉、多选和输入框；
- 通用组件内部改版会破坏流程分支；
- 多字段组合规则被迫重复配置在流程层；
- 只对少数控件类型有专门支持；
- 组件无法先把复杂规则收敛成稳定业务结果码。

### 5.2 MT、AT、VA 是三套求值路径

当前运行时存在人工控件分支、接口分支和变量表达式分支等独立逻辑。不同入口对空值、类型、默认分支和异常的处理不完全一致，难以保证主预览与独立运行页一致。

第三期不再让分支来源类型决定求值器。不同来源先统一形成正式输出，之后只由一个分支引擎处理。

### 5.3 当前分支结果同时承担展示和路由

当前 `operationRes`、分支名称和状态容易同时承担：

- 分支条件结果；
- 智能导航展示文字；
- 红绿状态；
- 流程推进标记。

这使“展示内容很多，但分支只要一个结果码”的场景难以表达。本期将路由结果、诊断展示和技术执行状态拆开。

### 5.4 当前后续加载缺少正式完成边界

如果控件一变化就直接加载后续节点，多字段场景可能在第二个字段尚未更新时提前命中；如果一律要求按钮确认，又会让简单下拉多一次无意义操作。

本期用可配置完成策略解决，而不是固定为 change 或固定为确认按钮。

---

## 6. 总体技术结构

```text
组件模板
  └─ outputDefinitions（声明对外输出槽位）

流程节点实例
  ├─ outputMappings（把本节点数据映射到输出槽位）
  ├─ resultBinding（指定 decision value / label）
  ├─ diagnosisBinding（指定状态与展示项）
  ├─ completionPolicy（何时认为本节点产生有效结果）
  └─ transitions（正式输出到后继分支）

二期 NodeRuntimeScopeV2
  ├─ variables
  ├─ formData
  ├─ apiData
  └─ outputs  ← 第三期新增正式输出区域

共享运行核心
  ├─ OutputProjector
  ├─ FormalResultValidator
  ├─ CompletionCoordinator
  ├─ TransitionEvaluator
  └─ DiagnosisProjector

主 src / 独立 page
  └─ 只负责挂载、展示和事件接入，不各自复制业务算法
```

---

## 7. 正式输出契约

### 7.1 输出定义

建议组件模板声明稳定输出槽位：

```ts
type GuidedOutputDataType =
  | 'string'
  | 'number'
  | 'boolean'
  | 'object'
  | 'array';

interface GuidedOutputDefinitionV3 {
  outputId: string;
  key: string;
  title: string;
  dataType: GuidedOutputDataType;
  requiredForResult?: boolean;
  description?: string;
}
```

规则：

- `outputId` 创建后稳定，不因标题或 key 改名而变化；
- `key` 在当前组件模板内唯一，供组件开发人员阅读；
- 配置引用保存 `outputId`，界面显示 `title`；
- `requiredForResult` 只表示生成有效正式结果时是否必需；
- 删除或修改输出定义前，复用二期引用索引查询影响流程；
- 对象和数组可用于诊断详情或其他节点读取，但标准 decision value 必须是标量。

### 7.2 节点实例输出映射

同一个通用组件在不同环节可调用不同接口、显示不同标题并产生不同结果。输出槽位由模板声明，具体来源由节点实例配置：

```ts
interface GuidedOutputMappingV3 {
  outputId: string;
  source: GuidedDataReferenceV2;
  emptyPolicy?: 'unresolved' | 'allow-null';
}
```

标准来源沿用二期结构化引用：

- 当前节点组件变量；
- 当前节点表单值；
- 当前节点接口数据；
- 显式流程全局变量；
- 必要时由组件事件脚本先计算并写入组件变量，再映射该变量。

第三期不要求平台表达式重新实现业务计算。金额区间、多选组合和复杂判断优先写在组件事件脚本中，最终只输出稳定结果码。

第三期同时在二期可扩展引用联合类型中注册正式输出引用：

```ts
type GuidedNodeOutputReferenceV3 = {
  kind: 'node-output';
  nodeId: string;
  outputId: string;
  path?: Array<string | number>;
};
```

输出映射的节点级来源默认必须属于当前 nodeId；流程全局变量可显式引用。其他节点只通过 `node-output` 读取已发布输出，不允许绕过正式输出去依赖另一节点的内部表单、API 数据或私有组件变量。

### 7.3 正式结果快照

```ts
type FormalResultValidity = 'unresolved' | 'valid' | 'invalid';
type BusinessDiagnosisStatus = 'success' | 'failure';

interface GuidedDecisionResultV3 {
  value: string | number | boolean;
  label?: string;
}

interface GuidedDiagnosisItemV3 {
  itemId: string;
  label: string;
  value?: unknown;
}

interface GuidedDiagnosisResultV3 {
  status?: BusinessDiagnosisStatus;
  items: GuidedDiagnosisItemV3[];
}

interface NodeFormalResultV3 {
  nodeId: string;
  revision: number;
  validity: FormalResultValidity;
  values: Record<string, unknown>; // key 为 outputId
  decision?: GuidedDecisionResultV3;
  diagnosis?: GuidedDiagnosisResultV3;
  generatedAt: number;
}
```

`NodeFormalResultV3` 是运行时快照，不回写流程定义。它和表单草稿的区别如下：

| 数据 | 含义 | 可作为标准分支输入 |
| --- | --- | --- |
| formData / variables | 用户正在输入或组件正在计算的数据 | 否 |
| outputs.values | 已按输出定义投影的数据 | 仅有效结果时可用 |
| decision.value | 当前节点稳定业务结论 | 是 |
| diagnosis | 当前节点导航展示结论 | 否，只用于展示和统计 |
| technicalError | 接口、脚本、平台执行错误 | 否，且停止推进 |

### 7.4 结果绑定

```ts
interface GuidedResultBindingV3 {
  decisionValueOutputId?: string;
  decisionLabelOutputId?: string;
}
```

- 有条件分支的节点必须配置 `decisionValueOutputId`；
- 对应值必须是 string、number 或 boolean；
- label 可不配置，未配置时导航不自动拿 value 充当复杂业务文案；
- 只有唯一无条件出口的节点可以不配置 decision。

### 7.5 原子发布和去重

同一次 change 事件可能先修改表单值，再执行脚本，最后修改组件变量。运行器必须在整条事件动作完成后统一投影一次输出，不能在每条赋值中间提前分支。

建议规则：

1. 事件进入一次运行事务；
2. 所有同步赋值和已声明需等待的异步动作完成；
3. `OutputProjector` 一次性读取最新节点作用域；
4. 校验所有 required 输出；
5. 生成新的原子结果快照；
6. 与上一版结果比较；
7. 完全相同则不重复推进；
8. label/诊断变化但 decision value 不变，只刷新导航；
9. decision value 变化，触发分支重新求值。

---

## 8. 组件内部计算与响应式更新

### 8.1 本期采用的能力模型

本期不要求实现“声明计算变量后自动追踪任意代码依赖”。采用当前平台可落地的方式：

```text
控件 change
  → 更新控件绑定值
  → 执行事件动作 / 脚本
  → 读取本节点最新组件变量
  → 更新正式输出
  → 校验完成条件
  → 选择分支
```

组件开发人员需要把所有影响结果的控件事件接入同一个计算动作，或复用同一个组件方法，避免每个 change 复制不同规则。

### 8.2 推荐的组件实现约定

以“意向降档金额 + 意向降档套餐”为例，组件内部声明：

```text
组件变量：
  decisionValue
  decisionLabel
  diagnosisAmount
  diagnosisPackage
  resultReady
```

金额和套餐的 change 都调用同一个 `recalculateDecision` 动作。该动作完成：

- 检查两个字段是否已经填写；
- 检查数据是否合法；
- 计算 `PLAN_8` / `PLAN_8_49` / `PLAN_49_PLUS`；
- 生成用户可读 label；
- 更新诊断明细；
- 最后设置 `resultReady`。

节点正式输出只映射上述组件变量。分支不直接读取金额输入框或套餐下拉框。

### 8.3 不完整和无效数据

- 必填输入缺失：结果为 `unresolved`；
- 输入格式错误或业务校验失败：结果为 `invalid`；
- 两者都不得进入后续分支；
- 由组件展示字段校验提示；
- 若上一版已经有有效结果，当前结果失效时必须撤销当前有效 decision；
- 已进入后续路径后的撤销由第四期处理，第三期只发协调事件。

---

## 9. 节点交互模式与完成触发

### 9.1 交互模式

```ts
type GuidedInteractionModeV3 = 'manual' | 'automatic';
```

交互模式是节点运行属性，不再由 MT / AT / VA 分支类型间接推断。

- manual：等待用户交互产生有效结果；
- automatic：初始化或接口动作完成后自动产生结果并继续。

Header、Footer 沿用一期区域语义，不参与 Content 的 manual/automatic 分支模式。

### 9.2 完成策略

```ts
type GuidedCompletionPolicyV3 =
  | { mode: 'output-valid' }
  | { mode: 'component-signal'; signalKey: string }
  | { mode: 'automatic-actions-complete' };
```

#### output-valid

适用于简单下拉、点选和多字段自动计算。正式结果变为 valid 时即认为当前人工节点完成，不需要额外确认按钮。

#### component-signal

适用于需要“校验”“确认办理”等明确业务动作的人工节点。组件发出信号后，运行器再读取和校验最新输出。信号本身不携带下一节点 ID。

#### automatic-actions-complete

适用于自动节点。等待声明为初始化/接口动作的 Promise 完成，再统一发布输出并继续。

### 9.3 自动链连续推进

自动节点完成后：

1. 校验技术执行是否成功；
2. 发布正式结果；
3. 生成业务诊断；
4. 求值分支；
5. 加载唯一后继；
6. 如果后继仍为自动节点，继续执行同一循环；
7. 遇到人工节点、Footer、结束节点或错误时停止自动循环。

必须加入同步循环保护，例如单次调度最大自动步数和重复节点检测，防止错误拓扑锁死页面。该保护只处理异常，不改变合法流程含义。

---

## 10. 业务诊断协议

### 10.1 诊断绑定

```ts
interface GuidedDiagnosisBindingV3 {
  statusOutputId?: string;
  items: Array<{
    itemId: string;
    label: string;
    valueOutputId?: string;
    emptyPolicy?: 'hide' | 'placeholder';
    placeholder?: string;
  }>;
}
```

配置人员可以：

- 把一个正式输出绑定为业务状态；
- 为智能导航配置一条或多条诊断项；
- 每条诊断项使用固定标题和动态输出值；
- 不显示空项或显示占位内容。

### 10.2 自动节点状态

自动节点启用状态展示时，`statusOutputId` 必须映射到明确的 `success` 或 `failure`：

- success：导航卡片绿色；
- failure：导航卡片红色；
- 缺失或其他值：配置/运行错误，不能猜测；
- 技术接口异常不映射为 failure，而是进入技术错误展示并停止流程。

### 10.3 人工节点结果

人工节点默认只展示选择结果或诊断明细，不强制红绿。若个别业务确实存在明确成功/失败语义，可显式配置状态输出，但平台不自动把普通选择理解为异常。

### 10.4 导航展示数据来源

智能导航只读取最新 valid 正式结果中的 diagnosis 快照，不读取 DOM、React 内部 state、当前分支名称或原始接口对象。

当前异常数量只统计显式 `failure` 业务状态；人工普通选择不计入异常。主 `src` 和独立 `page` 必须使用同一个统计函数，修复现有两端口径漂移。

---

## 11. 动态分支协议

### 11.1 分支结构

```ts
interface GuidedTransitionV3 {
  transitionId: string;
  title: string;
  sourceNodeId: string;
  targetNodeId: string;
  condition?: GuidedOutputConditionV3;
  isDefault: boolean;
  order: number;
}

interface GuidedOutputConditionV3 {
  outputId: string;
  operator: 'eq' | 'in' | 'neq' | 'gt' | 'gte' | 'lt' | 'lte';
  expectedValue: unknown;
}
```

首选配置方式是让组件输出结果码，分支只使用 `eq` 或 `in`。金额范围、多选组合等复杂规则由组件内部计算成业务码，不在每条流程线上重复实现。

### 11.2 标准与高级配置

编辑器提供两层能力：

- 标准模式：选择当前节点 decision value，并为每个出口填写一个或多个精确结果值；
- 高级兼容模式：允许使用受支持的标量比较运算，但必须通过互斥性校验。

新流程默认使用标准模式。流程层不直接组合组件内部多选项。

### 11.3 唯一命中算法

每次分支求值：

1. 只读取当前 valid 正式结果；
2. 求值全部非默认 transition；
3. 命中一个，选择该 transition；
4. 命中零个，使用唯一默认 transition；
5. 命中零个且无默认，返回 `NO_MATCHED_TRANSITION`；
6. 命中两个及以上，返回 `MULTIPLE_MATCHED_TRANSITIONS`；
7. `order` 只决定界面显示和求值日志顺序，不用于掩盖多分支重叠。

### 11.4 发布校验

保存允许暂存不完整配置，发布必须校验：

- 每个 transition 有稳定 ID、来源和目标；
- 条件引用的 outputId 存在且属于当前节点；
- 决策输出为标量类型；
- 标准模式不同 transition 的匹配值不得重复；
- 最多一个默认分支；
- 默认分支不再配置普通条件；
- 支持的数值区间不得有明显重叠；
- 高级条件若平台无法证明互斥，则不得按第三期新协议发布，需改成组件内部结果码；
- 所有活动路径最终可达唯一结束节点；
- 有 Footer 时满足一期拓扑约束。

### 11.5 拓扑身份

第三期为分支增加稳定 `transitionId`。目标节点关联该 ID，不以数组下标作为唯一身份。为兼容现有数据，可暂时同时保存 `branchIndex`，但新编辑和运行以 transitionId 为主。

删除、重排分支时，编辑器必须基于 transitionId 保持连线关系，并在删除前显示受影响的后继节点。

---

## 12. 运行时状态与事件

### 12.1 二期节点作用域扩展

```ts
interface NodeRuntimeScopeV3 extends NodeRuntimeScopeV2 {
  outputs: Record<string, unknown>;
  formalResult?: NodeFormalResultV3;
  outputRevision: number;
  completionState: 'waiting' | 'ready' | 'completed' | 'error';
}
```

这些字段均按 nodeId 隔离。同一个通用组件出现十次，会有十份独立输出和修订号。

### 12.2 建议运行事件

```text
NODE_INPUT_CHANGED
NODE_ACTIONS_SETTLED
NODE_OUTPUT_PROJECTED
NODE_RESULT_VALID
NODE_RESULT_INVALID
NODE_DIAGNOSIS_CHANGED
NODE_DECISION_CHANGED
NODE_COMPLETED
TRANSITION_SELECTED
BRANCH_RECONCILIATION_REQUIRED
NODE_TECHNICAL_ERROR
```

事件用于运行器内部协作和第四期衔接，不等于建设通用业务日志平台。

### 12.3 修订号和过期结果

- 每次正式结果实际变化时 `outputRevision + 1`；
- 异步动作开始时记录期望修订上下文；
- 较早异步结果晚到时不得覆盖较新的组件结果；
- 第三期至少实现节点级 latest-wins；
- 完整 AbortController、执行 ID 和旧路径请求取消在第四期加强。

---

## 13. 编辑器改造

### 13.1 组件模板编辑器

新增“正式输出”区域：

- 新增、编辑、删除输出定义；
- 维护 title、key、dataType、是否为结果必需项；
- 自动生成 outputId；
- 删除或改类型前查询引用影响；
- 已发布模板的 outputId 不允许重建式替换。

### 13.2 流程节点配置

Content 节点新增配置页签或分区：

1. 交互模式：人工 / 自动；
2. 完成策略；
3. 输出映射；
4. 分支 value 与展示 label 绑定；
5. 诊断状态与诊断项；
6. 条件出口和默认出口。

配置界面显示组件/变量/输出名称，持久化保存稳定 ID。

### 13.3 分支编辑体验

- 每个出口有可读名称；
- 标准模式直接选择允许的结果 value；
- 多个 value 可以汇入同一出口；
- 重复 value 当场提示；
- 默认分支有明确开关；
- 显示出口连接到的节点；
- 删除出口前展示会断开的节点；
- 提供输入样例试算，显示命中出口或错误。

### 13.4 保存、删除和发布校验

在二期引用注册表中新增 `node-output` 引用类型。以下操作必须校验：

- 删除节点；
- 删除输出定义；
- 修改输出类型；
- 修改 decision/diagnosis 绑定；
- 删除或改为默认分支；
- 修改事件脚本所写入的组件变量；
- 保存草稿；
- 发布版本。

草稿可保存未完成映射，但必须给出问题清单；发布不得存在断链、类型错误、无目标分支或无法证明互斥的新分支配置。

---

## 14. 后端存储与接口要求

### 14.1 组件模板数据

组件模板保存和查询需要支持：

- `outputDefinitions`；
- outputId 的稳定回读；
- 输出定义版本；
- 删除和类型修改前的引用影响查询。

后端不得在保存时重建 outputId，也不得只保存 title/key。

### 14.2 流程定义数据

`sceneData` 或对应流程协议新增：

```text
guidedSchemaVersion: 3
node.interactionMode
node.completionPolicy
node.outputMappings
node.resultBinding
node.diagnosisBinding
node.transitions
node.parentTransitionId（兼容期可并存 branchIndex）
```

后端需要完整保存、查询、复制、版本历史和发布这些字段，不能在 DTO 转换中丢失。

### 14.3 后端校验职责

后端发布校验至少覆盖：

- outputId、nodeId、transitionId 引用存在；
- 类型和绑定合法；
- 默认分支数量；
- 标准结果值重复；
- 拓扑可达性；
- Footer/结束节点约束；
- 二期引用索引完整更新。

前端即时校验用于体验，后端发布校验是最终防线。

### 14.4 不新增平台业务结果接口

每个自动节点调用什么业务接口、接口如何保证返回，由配置开发人员负责。平台只要求业务组件把接口返回中的明确状态字段映射为正式输出；接口不返回、脚本报错时直接进入技术错误，不为第三期另建统一业务结果服务。

---

## 15. 主 src 与独立 page 双入口

### 15.1 必须共享的部分

以下能力应放入共享模块：

- 输出定义和结果类型；
- 输出投影与类型校验；
- 结果去重和 revision；
- diagnosis 投影；
- transition 求值；
- 默认/多匹配/无匹配错误；
- completion 协调；
- 自动链循环保护；
- 导航异常数量计算。

### 15.2 入口各自职责

主 `src` 和独立 `page` 只分别完成：

- 将现有组件事件接入共享运行核心；
- 将各自 Store 适配为 NodeRuntimeScopeV3；
- 渲染同一 diagnosis 视图模型；
- 调用同一 transition 结果加载下一节点；
- 展示一致的技术错误。

禁止一端继续使用 MT/AT/VA，另一端使用正式输出，否则试点和生产会再次出现行为差异。

---

## 16. 兼容与迁移策略

### 16.1 Schema 升级

- 二期流程读取后默认升级为 `guidedSchemaVersion = 3` 的内存模型；
- 没有正式输出的普通唯一出口节点可保持空配置；
- 保存为 v3 前生成稳定 outputId 和 transitionId；
- 后端继续保留原始历史版本，升级失败可回退查看，不覆盖原定义。

### 16.2 MT / AT / VA 处理

旧配置只做兼容读取，不把三套旧模型长期带入新编辑器：

- 简单 VA：若可唯一识别变量来源，可生成输出定义、映射和新条件；
- 简单 AT：可把明确接口字段映射为输出，状态字段仍需人工确认 success/failure 语义；
- MT 多控件条件：不自动推断复杂业务规则，迁移工具给出待人工改造成组件计算结果码的清单；
- `operationRes/status`：只作为迁移参考，不直接当作新正式输出事实；
- 无法无损转换时保留 legacy 标记，只允许查看和兼容运行，不允许静默发布成 v3。

### 16.3 第一期、第二期试点流程

由于目前没有正式投产的引导式旧流程，优先保证第一期亲手配置的试点流程升级成本低：

- Header/Footer 配置不变；
- 二期变量 ID 和引用不重建；
- Content 节点逐个补交互模式、输出映射和分支结果码；
- 连线少的流程可人工确认 transitionId；
- 升级报告列出所有需补配节点，不猜测业务含义。

---

## 17. 贯穿验收示例：意向降档

### 17.1 场景

人工节点包含：

- 意向降档金额；
- 意向降档套餐；
- 备注；
- 不再依赖下面另一个“是否到营业厅咨询”节点替上方做组合判断。

### 17.2 组件内部规则

示例规则按最终业务口径实现，以下只验证平台能力：

| 输入情况 | decision value | decision label | 流程行为 |
| --- | --- | --- | --- |
| 金额、套餐均为 8 元档 | `PLAN_8` | 建议办理 8 元方案 | 进入 8 元分支 |
| 结果落在 8 元以上、49 元以下 | `PLAN_8_49` | 建议办理 8～49 元方案 | 进入中间分支 |
| 结果为 49 元及以上 | `PLAN_49_PLUS` | 建议办理 49 元及以上方案 | 进入高档分支 |
| 任一必填项未填写 | 无 | 展示待填写信息 | 停留当前节点 |
| 输入或业务校验失败 | 无有效结果 | 展示校验提示 | 停留当前节点 |

### 17.3 输出定义示例

```text
decisionValue      string   required
decisionLabel      string   required
selectedAmount     number   optional
selectedPackage    string   optional
```

诊断区域可同时显示：

```text
最终建议：建议办理 8～49 元方案
意向金额：28 元
意向套餐：28 元套餐
```

分支只读取 `decisionValue = PLAN_8_49`，不解析三行展示文字。

### 17.4 change 行为

金额和套餐 change 都执行相同的重算动作：

1. 用户先选 8 元；
2. 组件计算 `PLAN_8`；
3. 正式结果有效，命中第一分支；
4. 若尚未进入后续节点，可按第一分支继续；
5. 用户改成 28 元后，组件计算 `PLAN_8_49`；
6. decision value 变化；
7. 若后续已经加载，第三期发出 `BRANCH_RECONCILIATION_REQUIRED`；
8. 第四期路径协调器上线后，释放第一分支并重新加载第二分支。

这项验收必须证明第三期没有把“重算正确结果”和“清理旧运行路径”混成一个不完整动作。

---

## 18. 开发任务拆分与预计时间

估算口径：一名前端、一名后端，均为新进入项目且不熟悉现有代码；前后端可并行；人日包含代码阅读、联调、测试修复和文档回填，不包含等待外部环境、需求重新讨论和业务组件逐个改造时间。

| 任务 | 主要内容 | 前端人日 | 后端人日 |
| --- | --- | ---: | ---: |
| P3-T1 现状核查与契约定稿 | 核对 MT/AT/VA、两套运行入口、Store、分支保存结构；冻结 v3 字段和迁移矩阵 | 2～3 | 2 |
| P3-T2 输出定义与 Schema V3 | 输出定义编辑、稳定 outputId、协议类型、保存回读、引用索引扩展 | 3～4 | 2～3 |
| P3-T3 节点映射与结果配置 | 输出映射、value/label、diagnosis、交互模式、完成策略编辑器 | 4～5 | 1～2 |
| P3-T4 运行时输出与完成协调 | 原子投影、校验、revision、事件事务、人工/自动完成、连续自动推进 | 4～6 | 1～2 |
| P3-T5 动态分支引擎与编辑 | transitionId、唯一命中、默认分支、试算、保存/发布校验 | 3～4 | 2～3 |
| P3-T6 智能导航诊断 | 多项诊断、红绿状态、异常统计、主 src 与 page 一致适配 | 2～3 | 1～2 |
| P3-T7 兼容迁移与双运行时 | v2→v3、MT/AT/VA 兼容、主预览与独立页联调 | 2～3 | 1 |
| P3-T8 测试、修复与文档 | 单元/集成/E2E、示例流程、问题修复、协议及交付文档 | 2 | 0 |
| **合计** |  | **22～30** | **10～15** |

### 18.1 日历周期

建议日历周期为 **6～8 周**：

- 第 1 周：T1，协议和现状核查；
- 第 2 周：T2，输出定义、存储和引用索引；
- 第 3～4 周：T3、T4，编辑器和运行时主链；
- 第 4～5 周：T5，动态分支与发布校验；
- 第 5～6 周：T6、T7，导航、双入口和迁移；
- 第 6～8 周：T8、联调、试点流程和缓冲。

若后端实际 DTO、历史版本或发布链路比 Repomix 中可见部分更复杂，应使用 8 周口径。若组件事件动作当前不能可靠等待 Promise，T4 还需增加专项改造时间。

### 18.2 不计入本期的业务配置工作

以下工作由试点业务配置开发另行估算：

- 为具体通用组件补充计算脚本；
- 把每个业务接口状态字段映射为 success/failure；
- 为每个业务环节撰写诊断文案；
- 把旧业务判断逐个转换成稳定结果码；
- 业务接口和业务日志改造。

---

## 19. 测试方案

### 19.1 单元测试

- 输出定义和映射类型校验；
- required 输出的 unresolved/valid/invalid；
- 事件事务完成后只投影一次；
- 完全相同结果不增加 revision；
- label 变化不触发重新路由；
- value 变化触发分支求值；
- success/failure 严格值校验；
- 唯一命中、零命中、多命中和默认分支；
- 重复标准结果值校验；
- latest-wins 防止旧异步结果覆盖；
- 自动链循环保护。

### 19.2 集成测试

- Select/Radio change → 组件变量 → 正式输出 → 下一节点；
- 下拉 + 多字段输入 → 统一重算 → 输出 valid 后推进；
- 组件显式完成信号 → 校验 → 分支；
- 自动接口 → 状态输出 → 导航红绿 → 自动下一节点；
- 自动节点连续自动节点，直到人工节点停止；
- 同一通用组件使用两次，输出不串值；
- 其他节点通过 nodeId+outputId 读取正式输出；
- 删除输出、节点或 transition 时给出完整影响；
- 保存、刷新配置页、重新发布后 ID 不变；
- 主预览与独立页输出和分支一致。

### 19.3 第四期边界测试

- 后续未进入前 value 变化，可使用最新分支；
- 后续已进入后 value 变化，不错误追加新分支；
- 正确产生 `BRANCH_RECONCILIATION_REQUIRED`；
- 当前诊断可更新；
- 未上线第四期时按试点限制锁定或阻止继续；
- 不通过简单截断 `allRenderElements` 声称完成旧路径清理。

### 19.4 回归测试

- 第一期 Header 初始化和 Footer 固定挂载不受影响；
- Header 全局数据仍能被 Content 结构化读取；
- Footer 按钮显隐仍能读取 Content 数据；
- 无分支的普通 Content 节点仍可运行；
- 唯一结束节点及 Footer 拓扑校验仍有效；
- 二期节点作用域、订阅和引用删除校验不回退。

---

## 20. 验收标准

### 20.1 正式输出

- [ ] 组件模板可声明稳定 outputId 的正式输出；
- [ ] 节点实例可映射自身变量、表单和接口数据；
- [ ] 输出未自动写入流程全局变量；
- [ ] 同一模板多个实例的输出完全隔离；
- [ ] 其他节点可通过 nodeId+outputId 显式读取；
- [ ] 删除和改类型有影响分析。

### 20.2 计算和完成

- [ ] 相关 change 事件可更新用于计算的组件变量；
- [ ] 整条事件动作完成后原子发布结果；
- [ ] 简单人工选择不强制确认按钮；
- [ ] 多字段未完整时不推进；
- [ ] 显式确认场景可使用 component-signal；
- [ ] 自动节点动作完成后自动推进；
- [ ] 连续自动节点可以静默连续运行。

### 20.3 诊断和分支

- [ ] 分支 value 与展示 label 可分别绑定；
- [ ] 导航可展示多条诊断明细；
- [ ] 自动业务状态仅接受显式 success/failure；
- [ ] 技术错误和业务 failure 分开；
- [ ] 一次只命中一个非默认分支；
- [ ] 至多一个默认分支；
- [ ] 无匹配和多匹配均停止并给出明确错误；
- [ ] 所有路径最终可达唯一结束节点。

### 20.4 双入口和兼容

- [ ] 主 src 与独立 page 使用同一核心算法；
- [ ] 两端导航状态和异常数量一致；
- [ ] 第一期、第二期试点流程可升级到 v3；
- [ ] 旧 MT/AT/VA 无法安全转换时不会被静默猜测；
- [ ] 输出结果变化可为第四期产生明确协调事件。

---

## 21. 开发完成门槛

第三期只有同时满足以下条件才算完成：

1. 输出定义、映射、value/label、诊断和 transition 协议完成评审；
2. 前后端能稳定保存、回读、复制和发布 `guidedSchemaVersion = 3`；
3. 人工 change、人工显式完成、自动动作完成三种链路均通过；
4. 自动节点连续推进通过；
5. 唯一命中、默认分支、无匹配和多匹配全部有测试；
6. 主预览与独立运行页行为一致；
7. 意向降档示例完整跑通；
8. 二期引用校验已经覆盖 node-output；
9. 第四期边界测试证明第三期不会错误拼接新旧路径；
10. 第一、二期回归测试通过；
11. 协议、迁移清单和业务组件接入说明已回填；
12. 产品、前端、后端和试点配置开发共同验收。

第三期完成后，第四期才能以稳定的 `nodeId + outputId + revision + transitionId` 为输入，实现旧后续路径释放和新分支重载。
