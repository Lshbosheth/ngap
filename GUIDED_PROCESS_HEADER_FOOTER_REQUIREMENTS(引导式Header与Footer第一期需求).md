# 引导式 Header / Footer 第一期需求文档（冻结版）

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 需求名称 | 引导式顶部信息区与底部操作区 |
| 优先级 | P0，当前第一优先级 |
| 目标版本 | 引导式改造第一期 MVP |
| 文档状态 | 产品规则已冻结，可以进入技术核查与开发拆分 |
| 适用范围 | 引导式流程编辑、保存、预览和独立运行页 |
| 适用运行时 | 主 `src` 预览与独立 `page` 运行页 |

第一期包含：

- Header、Content、Footer 三类展示区域；
- Header/Footer 的唯一性、首尾拓扑和保存校验；
- 页面初始化时 Header 前置初始化、Footer 固定挂载；
- Header 公共数据供后续 Content 使用；
- Footer 内部元素根据流程进行中的数据响应式显隐；
- Footer 按钮执行自身业务操作；
- 稳定 `nodeId` 和 `guidedSchemaVersion` 落盘；
- 主 `src` 与独立 `page` 的一致实现。

第一期不包含：

- 完整的组件节点作用域和正式输出体系；
- 新的复杂条件语言；
- `control` 的完整改造；
- 大流程容量优化、自动布局和左右/上下方向切换；
- 重型旧流程迁移系统；
- P2 项目。

上述排除项不影响 Header/Footer 使用当前已有变量、绑定、显隐和事件能力完成简单试点流程。

---

## 2. 背景与目标

当前引导式运行页主要把已执行业务节点持续追加到同一个正文区域，不能稳定表达：

- 整个场景最先展示和初始化的顶部核心信息；
- 正常诊断、选择和办理环节；
- 始终位于页面底部的场景操作区；
- 底部按钮如何根据 Content 中的选择或结果动态显隐并执行操作。

现有 `BottomBanner` 依靠物料自身定位实现底部展示，容易覆盖正文或与其他节点通栏重叠，也无法表达它与流程首尾结构的关系。

第一期目标是先建立稳定的 Header / Content / Footer 页面骨架，使配置人员可以搭建一个简单、正常可用的引导式流程，并为后续组件变量作用域、动态输出和分支回滚改造保留稳定的数据身份。

---

## 3. 核心产品模型

Header、Content、Footer 都以业务节点形式保存在流程图中。它们共享节点配置和连线关系，但运行时挂载位置和生命周期不同。

| 区域 | 产品名称 | 流程位置 | 运行时行为 | 智能导航 | 数量 |
| --- | --- | --- | --- | ---: | ---: |
| `header` | 顶部核心信息 | 开始节点后的首个业务节点 | 页面启动时先初始化 | 否 | 最多 1 个 |
| `content` | 正文环节 | Header 与 Footer 之间 | 按活动路径逐步加载 | 是 | 多个 |
| `footer` | 底部操作区 | 结束节点前的最后一个业务节点 | 页面启动时固定挂载 | 否 | 最多 1 个 |

普通业务节点不需要显式选择区域。未配置特殊用途或区域字段缺失时，一律归一化为 `content`。

标准拓扑：

```text
有 Header 和 Footer：
开始 → Header → Content... → Footer → 结束

只有 Header：
开始 → Header → Content... → 结束

只有 Footer：
开始 → Content... → Footer → 结束

都没有：
开始 → Content... → 结束
```

这里的连线用于表达完整业务拓扑和执行顺序，不代表 Footer 必须等活动路径走到它时才出现在页面上。

---

## 4. 页面结构

```text
┌────────────────────────────────────┐
│ Header：顶部核心信息（可选）       │
├────────────────────────────────────┤
│ 智能导航（按现有开关决定是否显示） │
├────────────────────────────────────┤
│ Content：当前有效路径中的正文环节  │
│                                    │
│             独立滚动               │
│                                    │
├────────────────────────────────────┤
│ Footer：场景操作区（可选）         │
└────────────────────────────────────┘
```

- Header 位于 Content 之前；
- Content 是主要滚动区域；
- Footer 是页面壳中的独立底部区域，不混入 Content 卡片列表；
- Footer 不能依靠业务物料绝对定位覆盖正文；
- 未配置 Header/Footer 时不生成对应空区域；
- Footer 已配置但其内部元素当前全部隐藏时，不展示无内容的空白底栏。

---

## 5. 页面启动与流程推进

### 5.1 初始化顺序

进入页面或执行整体重启时，运行时按以下顺序处理：

```text
读取并校验流程定义
→ 定位唯一 Header 和 Footer
→ 创建节点运行域并挂载 Header、Footer 固定区域
→ 执行 Header 接口和组件初始化
→ Header 初始化成功
→ 启动 Content/Control 活动路径
→ 自动环节连续执行，人工环节等待交互
```

要求：

- Content 不能早于 Header 必需的接口和初始化逻辑开始；
- Header 可以把公共数据写入显式全局变量，后续 Content 可以读取；
- Footer 在页面初始化阶段固定挂载，不等待活动路径走到 Footer；
- Footer 可在初始阶段整体为空或按默认规则隐藏内部元素，后续随 Content 数据更新；
- 没有 Header 时直接启动 Content/Control 活动路径；
- 没有 Footer 时不挂载底部区域。

### 5.2 到达 Footer 和结束节点

- Header/Footer 不配置人工或自动类型，也不作为人工等待点；
- Header 初始化成功后沿唯一无条件出线进入后续路径；
- 当 Content/Control 活动路径最终到达已经挂载的 Footer 时，不重新加载 Footer，也不等待按钮操作；
- 运行时直接沿 Footer 的唯一无条件出线进入结束节点；
- Footer 按钮不承担“完成 Footer”或“推进流程”的职责；
- 流程进入结束节点后，只要页面没有关闭、刷新或整体重启，Header/Footer 继续保留。

### 5.3 自动与人工 Content

- 自动 Content 加载完成后自动评估分支并继续运行；
- 后续仍是自动环节时连续加载；
- 人工 Content 在用户点选、选择、输入或触发已配置事件后，根据组件产生的结果继续；
- Header/Footer 的存在不改变 Content 原有的人工/自动推进规则。

---

## 6. Header 业务规则

### 6.1 数量和拓扑

- 一个流程可以没有 Header，最多只能有一个；
- 存在 Header 时，开始节点必须且只能直接连接 Header；
- Header 不能位于中间分支，也不能拥有其他业务父节点；
- Header 只能有一条无条件出线；
- Header 后面可以进入 Content、Control、Footer 或结束节点，但不得再次进入 Header；
- Header 不进入智能导航，也不计入导航的成功、失败或环节数量统计。

### 6.2 唯一性替换

当用户把第二个节点设置为 Header 时，编辑器必须提示替换：

```text
当前已有顶部节点“诊断核心信息”。
是否将顶部区域替换为“客户基础资料”？
```

确认后：

- 原 Header 恢复为 Content；
- 新节点设置为 Header；
- 保留两个节点各自的业务组件和配置；
- 立即重新执行首节点拓扑校验，并提示需要调整的连线；
- 不得静默删除节点、组件或连线。

取消后不修改任何配置。

### 6.3 数据和生命周期

- Header 组件及其接口完成初始化后，Content 才能开始；
- Header 可以读取页面输入和已定义的全局变量；
- Header 可以通过现有赋值能力写入显式全局变量；
- 后续 Content 可以读取 Header 已成功写入的公共数据；
- 普通分支变化不重新执行或卸载 Header；
- 浏览器刷新、手机号变更后确认重启、或其他明确的整体重启会重新初始化 Header；
- 页面卸载或整体重启时释放 Header 的组件、订阅和未完成异步任务。

---

## 7. Content 业务规则

- 普通业务节点默认属于 Content；
- Content 可以有多个，按流程连线和分支规则执行；
- 已加载且当前仍有效的 Content 进入智能导航；
- Content 的人工/自动配置决定是否等待用户交互；
- Content 中的选择、表单值、组件变量、节点结果或显式全局变量，可以作为 Footer 内部元素显隐和按钮操作的输入；
- Footer 不允许直接读取 Content DOM 或组件内部未公开的 React state；
- 前序选择变化导致当前有效路径变化时，Footer 必须基于最新有效数据重新计算，已失效路径的数据不得继续生效；
- 完整的节点作用域、正式输出和自动回滚引擎属于后续阶段；第一期允许使用当前平台已有的变量、赋值、事件和显隐能力搭建简单试点流程。

---

## 8. Footer 业务规则

### 8.1 数量和拓扑

- 一个流程可以没有 Footer，最多只能有一个；
- 存在 Footer 时，它必须是结束节点前最后一个业务节点；
- Footer 的唯一后续节点必须是结束节点；
- Footer 只能有一条出线，且不能配置条件；
- Footer 后面禁止连接 Content、Control、Header、其他 Footer 或分支节点；
- 不同互斥路径可以汇合到同一个 Footer；
- 配置 Footer 后，所有正常结束的业务路径都必须先汇合到 Footer，再连接结束节点；
- Footer 不进入智能导航，也不计入导航统计。

### 8.2 固定操作区

- Footer 在页面启动阶段挂载一次，并在本次页面运行期间保持；
- Footer 可以包含一个或多个按钮及必要的辅助提示；
- Footer 不按普通 Content 卡片样式展示，不加入 Content 滚动列表；
- Footer 高度变化时，Content 可用高度自动调整；
- Footer 内部没有可见内容时隐藏空白底栏；
- 活动路径到达 Footer 时不得重复创建组件、重复调用初始化接口或重置按钮状态。

### 8.3 响应式显隐

Footer 整体及其内部元素可以根据流程进行中的数据控制显隐或禁用。例如：

```text
Content 选择“需要办理”
→ Footer 显示“确认办理”按钮

Content 选择“只需答复”
→ Footer 隐藏“确认办理”，显示“直接答复”按钮
```

规则：

- 第一阶段复用平台已有的变量、数据绑定和元素显隐能力；
- Content 相关值变化时立即重新计算 Footer 状态；
- 分支变化后只允许当前有效路径的数据继续影响 Footer；
- 依赖值尚未产生时，按配置的默认隐藏或默认显示处理，不能因此阻塞 Content 流程；
- 后续节点作用域上线后，Footer 引用升级为稳定的 `nodeId + variableId/outputId` 结构化引用；
- Footer 不得无约束遍历其他节点的私有状态。

### 8.4 按钮操作

Footer 按钮执行各自配置的业务操作，包括：

- 调用业务接口；
- 设置或更新允许写入的变量；
- 触发组件事件；
- 打开弹窗或提示；
- 执行提交、确认、办理、直接答复等业务动作。

操作边界：

- 每个按钮可以分别配置显隐、禁用和点击动作；
- 按钮只执行自身业务动作，不选择 `nextNodeId`，不推进流程；
- 不存在“是否完成 Footer”配置，也不存在 Footer 完成事件；
- 同一按钮执行期间应防止重复点击和重复提交；
- 操作失败直接展示业务配置或接口返回的错误，不由引导式自动重试；
- 按钮操作失败不改变已经确定的流程路径，也不重新执行 Header/Footer；
- 按钮成功后的提示、刷新数据、关闭页面等后续行为，由该按钮自己的业务动作配置负责。

### 8.5 生命周期

- 页面初始化时挂载 Footer；
- Content 运行过程中，Footer 只更新依赖数据和内部元素状态；
- 分支回滚或重新选择只使 Footer 重新计算，不卸载 Footer；
- 流程进入结束节点后 Footer 继续保留；
- 切换到其他页签再切回，不重新挂载、不清空状态；
- 浏览器刷新从头开始，重新挂载 Footer；
- 手机号变化时弹框提示，用户确认后废弃旧运行并从头加载；
- 页面卸载或整体重启时清理 Footer 组件、订阅、临时状态和未完成异步任务。

---

## 9. 编辑器需求

### 9.1 配置入口

在流程业务节点编辑入口增加“展示区域”：

```text
展示区域
  ○ 正文环节（默认）
  ○ 顶部核心信息
  ○ 底部操作区
```

- 展示区域属于当前流程中的节点实例，不修改通用组件模板；
- 普通节点默认 Content，不要求配置人员逐节点设置；
- Header/Footer 不展示人工/自动配置项；
- Footer 配置面板沿用其内部组件已有的元素显隐、禁用和事件动作配置，不增加“完成 Footer”。

### 9.2 画布展示

节点卡片增加区域标记：

```text
[顶部] 诊断核心信息
[环节] 套餐使用情况
[底部] 办理操作
```

- Header 使用明确的顶部标记；
- Content 沿用普通环节样式；
- Footer 使用明确的底部标记；
- 区域标记不能改变节点 ID、组件配置、分支数据或执行关系。

### 9.3 配置与连线约束

- 设置 Header/Footer 时立即检查唯一性；
- 设置第二个 Header/Footer 时必须经过替换确认；
- 存在 Header 时，开始节点连接其他业务节点必须阻止或明确报错；
- Header 只能创建一条无条件出线；
- Footer 创建出线时只能连接结束节点；
- Footer 出线不能配置条件；
- 多条互斥路径允许连接同一个 Footer；
- 节点切回 Content 时保留其组件配置；
- 删除 Header/Footer 或改变区域后立即重新校验，不静默删除其他业务节点或连线；
- 复制 Header/Footer 节点时，复制结果默认降级为 Content，避免唯一性冲突。

### 9.4 保存与发布校验

保存时至少校验：

- Header/Footer 各自最多一个；
- Header 是开始节点后的首个业务节点；
- Header 只有一条无条件出线；
- Footer 是结束节点前最后一个业务节点；
- Footer 只有一条指向结束节点的无条件出线；
- 配置 Footer 后，正常结束路径不得绕过 Footer；
- Header/Footer 节点可达且不存在错误回路；
- 区域字段、稳定 `nodeId` 和流程版本字段可以正确保存并回读。

错误必须定位到具体节点或连线，提供可理解的修复提示。草稿保存是否允许保留校验错误沿用应用现有规则，但存在阻断错误时不得发布为可运行流程。

建议文案：

- “一个流程最多只能配置一个顶部核心信息节点”；
- “顶部核心信息必须是开始节点后的第一个业务节点”；
- “顶部核心信息只能配置一条无条件出线”；
- “一个流程最多只能配置一个底部操作区节点”；
- “底部操作区必须是结束节点前的最后一个业务节点”；
- “底部操作区之后只能连接结束节点”；
- “正常结束路径必须先进入底部操作区”。

---

## 10. 运行时需求

### 10.1 区域化运行状态

运行时不能只用一个扁平元素数组表达整页，至少需要维护：

- 唯一 Header 的节点实例及初始化状态；
- 当前已经加载且仍有效的 Content 列表；
- 唯一 Footer 的节点实例及当前依赖状态；
- 当前活动路径和流程是否已经到达结束节点。

Header/Footer 虽然固定展示，仍必须保留自己的节点身份、组件实例和运行数据，不能脱离流程定义变成页面级硬编码。

### 10.2 页面布局

- Header 位于智能导航和正文之前；
- Content 是唯一主滚动区域；
- Footer 位于页面壳底部，不覆盖正文；
- Footer 高度变化时 Content 可用高度自动调整；
- Header/Footer 未配置或没有可见内容时不显示空容器；
- Header 与导航的固定/滚动方式沿用引导式页面布局配置；
- Footer 使用 Grid/Flex 等正常页面布局，不允许业务物料通过绝对定位占据页面底部。

### 10.3 `BottomBanner` 兼容

- Footer 的正式来源是节点实例的 `presentation.region = footer`；
- 不能长期通过是否包含 `BottomBanner` 推断 Footer；
- `BottomBanner` 位于 Footer 节点时，只负责内部内容布局，外部定位由页面壳负责；
- 普通组装式页面继续保留现有 `BottomBanner` 行为；
- 当前没有生产中的旧引导式流程，因此第一期不建设批量迁移；如遇旧模拟数据，只按 Content 兼容或由配置人员重新指定 Footer。

### 10.4 异常处理

- Header 初始化或必需接口技术异常时，在 Header 区域展示明确错误并停止 Content/Control 启动；
- 不为 Header 自动重试，也不自动选择失败分支；
- Footer 初始加载异常时只在 Footer 区域展示错误，不能导致正文白屏或覆盖正文；
- Footer 按钮动作异常时解除该按钮的执行锁并展示错误；
- 技术异常不计入智能导航的业务成功/失败诊断；
- 接口必须返回是配置人员责任，引导式不为缺失返回设计补偿流程。

### 10.5 刷新、手机号变化和页签切换

- 浏览器刷新：按新页面运行，从 Header 开始重新初始化；
- 手机号变化：弹框提示用户，确认后废弃旧运行结果并从头渲染；
- 切换其他页签再返回：没有重新进入事件，当前 Header、Content、Footer 和活动路径保持不变。

---

## 11. 数据要求

节点实例保存展示区域：

```ts
presentation.region = 'header' | 'content' | 'footer'
```

第一期同时必须满足：

- 每个流程节点保存稳定、不可因标题或组件名称变化而重建的 `nodeId`；
- 流程定义保存 `guidedSchemaVersion`；
- 区域字段属于流程节点实例，不能写回通用业务组件模板；
- 字段缺失或非法时按 Content 兼容；
- Footer 仍保存在流程节点和连线数据中，并保留正常 `nodeId/componentId`；
- Footer 按钮显隐、禁用和业务动作保存在 Footer 组件实例配置中；
- 不保存 Footer 完成动作，不保存由 Footer 按钮指定的目标节点 ID；
- 保存接口返回后再次读取，区域、节点身份、流程版本和组件配置不能丢失；
- 主 `src` 和独立 `page` 使用相同的默认值、区域归一化和运行语义。

本需求冻结字段语义，不预先冻结后端接口 JSON 的具体嵌套层级。

---

## 12. 兼容与迁移

- 不配置 Header/Footer 的引导式流程保持现有 Content 运行方式；
- 普通组装式页面不受影响；
- 区域字段缺失的业务节点按 Content 处理；
- 删除 Header/Footer 后，对应页面区域自然消失；
- 节点新增、删除、复制、移动、连线、撤销重做和保存必须保留正确区域信息；
- Header/Footer 第一阶段创建的试点流程必须带稳定 `nodeId + guidedSchemaVersion`；
- 后续节点作用域上线时，对少量试点流程做轻量升级，保留拓扑、Header/Footer 和组件配置；
- 能确定来源的变量及引用自动升级，歧义项由配置人员确认，不静默按名称猜测；
- 主 `src` 与独立 `page` 必须同步交付，具体共享代码位置在实施阶段核查。

---

## 13. 验收标准

### 13.1 编辑器验收

- 新建普通节点默认是 Content；
- 任意业务节点可以设置为 Header 或 Footer；
- 同一流程不能发布两个 Header 或两个 Footer；
- 存在 Header 时，开始节点只能直接连接 Header；
- Header 只能有一条无条件出线；
- Footer 之后只能通过一条无条件出线连接结束节点；
- 正常结束路径不能绕过已配置的 Footer；
- 多条互斥路径可以汇合到同一个 Footer；
- Header/Footer 卡片标记正确；
- Footer 配置中不存在“完成 Footer”或“按钮推进流程”选项；
- 保存并重新打开后，区域、`nodeId`、`guidedSchemaVersion`、连线和组件配置正确恢复；
- 复制 Header/Footer 后的新节点默认是 Content。

### 13.2 运行时验收

- 无 Header/Footer 的流程表现不变；
- 进入页面后先完成 Header 必需初始化，再启动 Content；
- Content 能读取 Header 成功写入的显式全局数据；
- Footer 在页面初始化阶段挂载，不等待活动路径走到末尾；
- Header/Footer 不进入智能导航及其统计；
- 修改 Content 选择后，Footer 整体或指定元素立即按最新有效数据更新；
- 活动路径到达 Footer 时不重新挂载，也不等待按钮，自动进入结束节点；
- 流程到达结束节点后 Header/Footer 继续显示；
- Footer 不覆盖正文，Footer 高度变化后 Content 仍可滚动到最后；
- Footer 不同按钮分别执行自己的业务动作，均不改变流程连线或推进状态；
- Footer 按钮连续点击不会重复提交；
- Header 初始化失败时 Content 不启动；
- Footer 或按钮失败不导致整页白屏；
- 浏览器刷新和手机号确认变更会从头加载，普通页签切换保持原状态；
- 主 `src` 预览和独立 `page` 使用同一份定义时表现一致。

### 13.3 回归验收

- 普通组装式页面中的 `BottomBanner` 布局不变；
- 区域字段缺失的数据仍可打开、编辑、预览和保存；
- 流程节点新增、删除、复制、移动、连线、撤销和重做没有回归；
- Footer 内已有按钮、接口、变量赋值、事件和显隐能力正常工作；
- Header/Footer 改造不改变 Content 原有人工/自动推进语义。

---

## 14. 第一期测试场景

1. `开始 → Header → 自动 Content → 人工 Content → Footer → 结束`；
2. `开始 → Header → Content → 结束`；
3. `开始 → Content → Footer → 结束`；
4. `开始 → Content → 结束`，验证无特殊区域兼容；
5. Header 接口完成并写入全局数据后，Content 才加载并正确读取；
6. 页面刚进入时 Footer 已挂载，但按钮按未赋值默认规则隐藏；
7. Content 点选后 Footer 显示对应按钮，再修改选择后按钮立即切换；
8. 两条互斥分支汇合到 Footer，验证到达 Footer 后直接进入结束节点且不重复挂载；
9. 到达结束节点后继续操作 Footer 按钮，验证按钮只执行业务动作；
10. Footer 按钮接口失败和快速重复点击，验证错误反馈与动作锁；
11. 设置第二个 Header/Footer，验证替换、取消和连线校验；
12. 尝试从开始节点绕过 Header，验证发布被阻断；
13. 尝试从 Footer 连接普通节点或配置条件出线，验证发布被阻断；
14. Header 初始化失败，验证 Content 不加载且 Footer 不覆盖错误区域；
15. 浏览器刷新、手机号确认变更、页签切出再切回，分别验证三种生命周期；
16. Footer 内容高度变化，验证正文可用高度和滚动；
17. 保存并重开流程，验证稳定 `nodeId`、`guidedSchemaVersion` 和区域信息；
18. 主 `src` 预览和独立 `page` 对同一份流程数据进行对比测试；
19. 普通组装式页面回归 `BottomBanner`；
20. 未配置区域字段的模拟旧数据按 Content 运行。

---

## 15. 开发前技术核查项

以下事项不再是产品待确认项，只需在实施前根据代码现状确定落点：

1. 主 `src` 编辑、预览和独立 `page` 运行入口各自的数据读取与渲染链路；
2. 两套运行入口可共享的区域归一化、拓扑校验和生命周期代码位置；
3. 当前流程保存接口中 `presentation.region`、稳定 `nodeId` 和 `guidedSchemaVersion` 的具体落盘位置；
4. Header 初始化完成的可靠判断时机，以及接口异常向区域错误态传递的方式；
5. Footer 固定容器与现有页面滚动、导航固定和 `BottomBanner` 样式的冲突点；
6. Footer 当前可以复用的数据绑定、显隐、按钮事件和防重复提交能力；
7. 草稿保存、发布校验和画布即时连线限制分别对应的现有入口；
8. 同一份流程定义在主 `src` 和独立 `page` 中的回归基线。

上述核查只决定实现方式，不改变本需求已冻结的产品行为。

---

## 16. 冻结结论

本需求已无产品待确认项。第一期统一按以下原则实施：

1. Header/Footer 是流程节点，也是页面固定区域；
2. Header 先初始化，成功后才启动 Content；
3. Footer 页面启动时固定挂载，并随 Content 数据更新内部状态；
4. Footer 按钮只做业务操作，不完成 Footer、不选择下一节点、不推进流程；
5. 活动路径到达 Footer 后自动通过唯一出线进入结束节点；
6. 流程结束后 Header/Footer 保留到页面卸载或整体重启；
7. 普通节点默认 Content，Header/Footer 各最多一个并遵守首尾拓扑；
8. 第一阶段必须保存稳定 `nodeId` 和 `guidedSchemaVersion`；
9. 主 `src` 与独立 `page` 行为一致；
10. 画布容量、方向切换、完整节点作用域及 P2 均不混入本需求范围。
