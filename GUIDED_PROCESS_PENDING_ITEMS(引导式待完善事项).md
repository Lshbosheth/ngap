# 引导式流程待完善事项（GUIDED_PROCESS_PENDING_ITEMS）

> 用途：单独记录引导式流程改造中尚未冻结、需要需求/产品/前端/后端共同确认的事项。  
> 使用方式：评审时逐项更新“当前状态”和“最终结论”，结论冻结后再同步回《引导式流程展示编排升级设计（GUIDED_PROCESS_REDESIGN(引导式流程展示编排升级设计).md）》。
> 当前阶段：方案讨论，尚未开始本轮大规模代码重构。  
> 更新时间：2026-08-11

---

## 1. 已经确认的前提

以下内容已经在本轮讨论中形成明确方向，后续评审默认以此为前提，不再反复扩大范围：

1. 新引导式是前端融合式 React 引导流程，不再由后端逐环节决定下一页面；
2. 节点在产品模型上融合 React DOM、配置、数据契约、动作和分支；
3. 流程遍历、状态、回滚、取消和连线选择仍由平台统一引导式引擎负责，不能散落在各 React 组件中；
4. 后端主要提供业务接口、可信业务规则、权限、配置持久化，以及可选的运行快照和日志；
5. 环节之间的连线就是流程推进关系；
6. 设计图允许条件分支和不同分支重新进入同一后续节点；
7. 一次运行只有一条活动路径，不考虑 B、C 同时执行后等待汇聚的并行流程；
8. `A-B-D / A-C-D` 是两条互斥路径，当前模型应继续支持；
9. 简单判断可以直接配置在出线上，复杂判断可以使用无 DOM 的服务/判断节点；
10. 普通环节默认归属 `content`，无需逐节点配置；只有 `header/footer/control` 作为特殊用途显式设置；
11. 一个引导式流程最多一个 `header`、一个 `footer`，可以有多个 `content` 和 `control`；
12. `header`、`footer` 渲染但不进入智能导航，`control` 不渲染但照常执行接口、条件判断和分支推进；
13. 存在 `header` 时，开始节点必须直接连接 `header`；存在 `footer` 时，它在拓扑上位于结束节点之前且唯一后续只能是结束节点。运行时进入页面后先初始化并固定渲染 header、footer，再加载实际 content 路径，不等待活动路径走到 footer 才显示；
14. 所有已进入活动路径且有内容的 `content` 自动进入智能导航，不提供节点级导航开关；
15. “人工/自动”只针对 `content`：它既是环节交互与推进方式，也作为智能导航卡片文案。自动环节执行完成后静默判断分支并连续加载；人工环节进入后等待用户交互；
16. 每个节点实例拥有自己的数据域，内部数据默认私有；
17. 节点必须产生正式、结构化输出，分支和下游节点主要读取输出，不直接依赖组件内部 DOM、state 或原子 ID；
18. 流程共享变量必须显式创建，不能再把每个组件的变量默认注册到全局；
19. 主 `src` 与独立 `page/materials` 必须共享相同运行语义。

---

## 2. 状态和优先级说明

状态：

| 状态 | 含义 |
| --- | --- |
| 待产品确认 | 需要需求/产品决定用户可见行为 |
| 待技术确认 | 需要前端、后端或架构验证可行性和成本 |
| 待接口样例 | 缺少真实接口或真实流程数据，无法最终冻结 |
| 已有推荐 | 已给出建议默认值，但还没有正式评审通过 |
| 已冻结 | 已形成明确结论，可以进入实现 |

优先级：

| 优先级 | 含义 |
| --- | --- |
| P0 | 不确认就不能安全开始运行时主干改造 |
| P1 | 主干开发期间必须确认，否则影响上线、迁移或运维 |
| P2 | 当前阶段整体暂不考虑，不作为设计、开发、验收或正式推广前置条件；仅保留备忘 |

---

## 3. P0：开发主干前必须确认

### P0-01 人工/自动 content 环节的推进时机

**问题**

当前运行时按旧 `AT/MT` 逻辑递归或暂停，但新模型必须明确自动环节连续加载、人工环节等待交互的统一规则，并将“推进触发”与“组件输出字段”分开。

**需要确认**

- 自动环节在什么时机认为本次接口或组件逻辑已经完成；
- 自动环节存在多条条件分支时是否需要暂停；
- 人工环节由哪些点选、输入或确认动作恢复推进；
- 没有分支判断字段的自动环节如何进入唯一后续节点。

**最终结论**

- 自动 content 进入后自动调用接口或执行组件逻辑；数据取得或自动逻辑完成后立即评估已有分支规则并进入唯一命中的下一节点；
- 自动环节即使存在多条条件分支也不暂停。命中的下一节点仍为自动环节时继续静默加载，直到人工环节、结束节点或执行异常；
- 只有一条无条件出线的自动环节不需要为了推进而额外产生判断输出，自动逻辑完成后直接继续；
- 自动环节的组件输出只在分支判断、智能导航展示或下游取数确有需要时提供，它不是“继续加载”的触发器；
- 人工 content 进入后暂停等待交互。组件自行定义何时形成有效分支结果：单一点选可在选择变化后继续；多字段组件既可在必填字段完整且校验通过后自动计算并继续，也可按业务需要通过确认/完成动作继续；
- 人工环节继续后，如有分支则读取组件输出判断；只有一条出线时直接进入后续节点；
- 组件只能产生业务结果，不能输出 `nextNodeId`，下一节点仍由画布连线和分支规则决定。

**当前状态：已冻结。**

### P0-02 智能导航中的环节诊断结果

**问题**

本轮产品不设计或展示复杂的节点生命周期状态。需要定义的是应用组件向智能导航提供的业务诊断结果，而不是运行引擎内部状态。

**需要确认**

- 自动环节如何向导航提供诊断结果文案；
- 自动环节如何提供业务意义上的成功或失败；
- 人工环节如何把点选结果或多个展示结果同步到导航；
- 技术请求异常如何避免被误当成业务诊断失败。

**最终结论**

- 自动环节由应用组件开发人员控制诊断结果；接口会单独返回业务状态字段，暂时只有 `success/failure` 两种，导航分别显示绿色或红色；
- 多数自动环节正文以列表展示，当前可以把“有数据成功、无数据失败”作为理解方式，但平台最终以接口显式状态字段为准，不自行依赖列表长度推断；
- 自动环节可同时向导航提供诊断结果文案，完整列表继续在环节正文展示；
- 人工环节根据组件点选或交互结果展示诊断内容，不强制套用自动环节的红绿成功/失败；
- 请求超时、程序异常等技术错误与业务 `failure` 分开处理；
- 运行引擎内部如何去重、取消和恢复属于技术实现，不扩展为本轮产品节点状态体系。

**当前状态：产品展示规则已冻结；接口状态字段名和值域待真实接口确认。**

### P0-03 组件输出及其用途

**问题**

当前分支主要读取内部 `atomId` 对应的值。需要把“组件向流程提供哪些环节结果”统一称为组件输出，避免把它误解为另一套全局参数或额外配置。

**需要确认**

- 应用组件开发人员如何定义组件能够提供的输出字段；
- 分支判断、智能导航和下游节点分别选择哪些字段；
- 一个字段能否同时用于分支和导航，导航是否允许展示多个字段；
- 哪些输出确实需要被下游读取或显式发布为流程共享参数；
- 字段名称、类型、必填和敏感标记等技术说明如何保存。

**推荐结论**

- 组件输出首先服务于分支判断，也可以被智能导航和下游节点按需使用；
- 组件输出默认属于当前 `nodeId`，不是全局参数；发布到流程共享域必须显式配置；
- 分支判断字段与导航展示字段可以不同，导航也可以展示多个组件输出；
- 简单人工选择暂定使用 `value` 作为稳定分支值、`label` 作为导航文案；复杂组件可以分别提供判断字段和诊断展示字段；
- 自动环节可提供业务诊断状态、诊断摘要和必要数据；完整接口列表除非分支或下游需要，否则不必全部暴露为组件输出；
- “输出契约”只是组件输出字段的名称、含义和类型说明，不是第二套输出机制；产品侧统一使用“组件输出”一词；
- 组件可以输出业务结果代码，但不能输出 `nextNodeId`。

**当前状态：产品概念与使用规则已冻结，字段级技术协议待确认。**

### P0-04 多选组合和条件语言

**问题**

现有数组判断属于逐项比较，难以清晰表达“必须同时包含 A、B”“只能是 A、B”“至少选择两个”等集合条件。

**需要确认**

- 第一版必须支持哪些操作符；
- 同一分支内是否只支持一层 AND/OR，还是支持条件组嵌套；
- 多选值是否统一归一化为无序集合；
- 空值、未填写和字段不存在是否区分；
- 数字、日期、字符串的隐式转换是否允许。

**原建议操作符（降为兼容或后续增强）**

```text
equals / notEquals
containsAny / containsAll / equalsSet / notContains
sizeEquals / sizeGreaterThan
isEmpty / isNotEmpty
greaterThan / lessThan / greaterOrEqual / lessOrEqual
```

结构化集合操作符不再作为所有新组件第一版的 P0 必选能力，可用于旧组件兼容或后续配置增强；普通新流程不开放任意 JavaScript 分支表达式。

**2026-08-12 讨论进展**

- 已确认把复杂选择和计算集中在应用组件内部：组件开发人员声明一个专门的“分支结果变量”，通过组件事件和代码把内部点选、输入等内容归一为稳定业务代码，流程只根据这个结果变量选择连线；
- 仅声明变量或给变量填写默认表达式，不会自动形成响应式计算。当前代码需要显式配置 `onChange → 运行脚本/表达式 → 变量赋值`，变量变化后再触发 `VA` 分支重算；
- 组件变量作用域已经确定：改造后默认写入当前组件实例对应的 `nodes[nodeId].variables`，同名组件变量互不覆盖；
- 当前组件可以直接读取自己的内部变量；需要取得其他组件的全部变量时，通过明确的组件实例 ID 获取只读快照。产品界面可以称“组件 ID”，引导式底层必须保存 `nodeId`，不能使用可能重复的模板 `componentId` 区分实例；
- 组件可以读取流程中显式定义的全局变量；修改全局变量必须使用明确的全局变量赋值动作或 Runtime API，组件变量不会自动注册成全局变量；
- 当前 `MT/VA` 把交互方式与分支数据来源耦合在一起。新模型需要允许“人工交互 + 组件变量分支”：人工环节等待用户操作，组件计算并赋值后，变量变化触发分支继续；
- 当前主 `src` 与独立 `page` 对 VA 无匹配结果的保护不完全一致，正式依赖组件分支结果变量前必须统一“空值/未完成不推进”的行为；
- 复杂计算由组件产生稳定分支结果，平台第一期的复杂多选集合操作符降级为兼容或增强能力，不再作为所有新组件的必选配置方式；
- 分支结果变量必须使用稳定业务代码，不能保存 `nextNodeId`；导航展示可读取其他组件变量，不要求与分支结果变量相同；
- 多字段尚未完整或校验未通过时不能产生有效分支结果，流程保持在当前人工环节；
- 结果变量的更新和推进时机由应用组件定义：即时决策场景在所需字段完整且校验通过后自动计算并赋值；需要显式提交的业务场景由确认/完成动作计算并赋值；
- “意向降档金额＋意向降档套餐”属于即时决策场景：两个字段完整有效后自动计算稳定业务值；任一字段变化都重新计算，结果变化时按 P0-07 回滚并重载后续分支，不需要额外确认按钮；
- 该场景示例输出三种稳定业务值：两个字段都是8元时输出 `PLAN_8`；非全8元且组件计算出的业务判定金额大于8、小于49时输出 `PLAN_8_49`；业务判定金额大于等于49时输出 `PLAN_49_PLUS`。具体用哪个字段或组合值划档由组件内部脚本负责，流程只匹配结果值；
- 例如已经以 `PLAN_8` 加载第一条后续路径，用户再把相关选择改成28元，组件重新输出 `PLAN_8_49`，运行时立即释放原分支的后续节点域和页面内容，再加载第二条分支；字段未填完整、被清空或校验失败时不产生有效结果并停留在当前环节；
- 流程分支第一版只需要对组件提供的稳定业务值执行相等/枚举匹配并选择唯一出线；复杂组合关系由组件内部事件和脚本负责计算。

**当前状态：已冻结。组件结果时机由组件定义，复杂条件集中在组件内部计算；集合条件语言转为兼容或后续增强，不再作为 P0 门槛。**

### P0-05 多条出线的唯一命中规则

**问题**

如果两条条件同时为真，或者全部不为真，运行时必须有确定行为。

**需要确认**

- 是否允许配置优先级；
- 是否要求每个有条件分支的节点配置默认出线；
- 多条同优先级出线同时命中时，阻止保存还是运行时报错；
- 无命中时显示配置错误、业务阻塞还是走失败节点；
- `success/failed/completed` 等触发类型如何与条件组合。

**最终结论**

- 正常推进只选择一条出线；
- 编辑器尽可能静态检查冲突；
- 运行时多条同优先级命中时停止并记录配置错误，不静默任选；
- 非结束节点必须有默认出线或显式“无命中即阻塞”策略；
- 自动环节即使有多条条件出线也静默判断并连续执行，不因多分支暂停；
- 组件只提供稳定业务结果，画布规则负责把结果映射到唯一后续连线。

**当前状态：已冻结。**

### P0-06 流程图结构和保存校验

**问题**

画布目前会阻止明显回连，但仅依赖交互期校验不足以保证导入数据、历史数据和后端返回数据都合法。

**需要确认**

- 第一版是否明确只支持有向无环图；
- 是否只允许一个开始节点；
- 是否允许多个结束节点；
- 是否允许不可达节点；
- 普通节点是否可以无出线；
- D 可以有多个父节点时，产品文案要明确这是互斥路径汇合，不是并行等待。

**最终结论**

- 第一版采用单活动路径 DAG；
- 只允许一个开始节点和一个结束节点；
- 无论中间命中哪条互斥分支，所有有效业务路径最终都必须连接到同一个结束节点；
- 禁止环路、孤立节点和不可达节点；
- 非结束节点必须有合法后续策略；
- 保存、发布和运行加载时都执行完整拓扑校验；
- D 可以有多个父节点，但表示互斥路径重新汇合，不表示并行等待。

**当前状态：已冻结。**

### P0-07 分支回滚的数据范围

**问题**

当前切换前序人工选择时主要截断后续渲染元素，页面级变量、接口结果和表单数据可能继续残留。

**需要确认**

- 旧路径的草稿、输出、接口结果和组件本地状态是否全部删除；
- 返回修改时当前节点草稿是否保留；
- 已产生副作用的接口是否需要补偿；
- 旧异步请求返回后如何拒绝写入；
- 用户从 footer 返回前序 content 时，footer 的运行态如何清理。

**最终结论**

- 分叉点以前的有效节点域保留；
- 旧分支节点域从后向前整体释放；
- 使用 `runId/executionId` 拒绝旧请求结果；
- 旧路径的草稿、组件私有状态、接口结果、组件输出、DOM、订阅和仍在进行的异步任务一并失效或释放；
- 分支回滚只释放旧分支的 content/control 节点域；固定挂载的 header/footer 不因活动分支变化退出或卸载，footer 内部元素显隐按回滚后的最新数据重新计算；
- 已产生后端副作用的数据不能只靠前端删除，必须按接口语义决定补偿或保留记录，平台不做无依据的通用回滚。

**当前状态：已冻结。**

### P0-08 浏览器刷新、手机号变更与页签切换

**问题**

需要明确浏览器刷新、手机号变更和宿主页签切换是否影响当前流程运行态。

**2026-08-12 已确认结论**

- 第一版不支持浏览器刷新后断点恢复；浏览器刷新一律丢弃当前前端运行态，从开始节点重新执行并重新渲染整个引导流程；
- 手机号属于本次业务办理的关键上下文。平台监听到手机号变更事件后，必须弹框提示用户页面将按新手机号重新加载，然后终止旧运行并从开始节点重新执行；
- 手机号已经变化后不能继续沿用旧手机号对应的节点数据和活动路径。弹框用于明确告知重启影响，不允许旧流程继续运行；
- 两类重启都必须创建新的 `runId/executionId`，清理旧节点域、表单草稿、组件变量、节点输出、接口结果、导航诊断、DOM、订阅和可取消的异步任务，并拒绝旧异步请求回写；
- 重启后重新读取当前最新手机号及其他运行上下文，再从唯一开始节点按正常规则执行，自动环节重新静默加载，直到人工环节、结束节点或异常；
- “从头开始”只重置前端引导流程运行态，不表示自动撤销已经发生的后端业务结果；刷新后接口会重新按节点配置执行，配置开发人员必须保证相关接口允许该流程行为；
- 本期不因浏览器刷新引入流程运行快照接口。未来若新增断点续办，需作为独立能力设计，不能把运行值写回流程定义。
- 第一版没有“返回上一步”或通过智能导航重新进入前序环节的独立功能；用户直接修改页面上仍可操作的前序环节，按 P0-07 执行后续路径回滚，不属于本项；
- 用户切换到其他宿主页签后再切回当前页签，不视为重新进入，不触发刷新、重新执行或状态清理；当前 DOM 和流程运行态保持不变；
- 页签切换本身没有额外触发时机，也不增加专门的生命周期配置。

**当前状态：已冻结，不再有待确认项。**

### P0-09 节点接口动作和 control 的失败策略

**2026-08-12 已确认结论**

- 引导式流程第一版不设计接口自动重试、手动重试、失败出线、通用补偿或幂等策略；
- 接口配置、返回完整性和业务可用性由应用组件配置开发人员负责，配置人员必须保证流程使用的接口能够结束调用并按约定返回；
- 请求异常、超时、没有返回有效结果或返回结构不符合组件约定时，当前节点直接进入技术错误状态，页面展示错误并停止流程继续加载；
- 错误不能只写控制台日志，也不能静默跳过当前环节；
- 接口正常返回的业务 `success/failure` 属于组件业务诊断结果，不属于技术错误，仍可用于智能导航红绿展示和已配置的业务分支；
- 平台只负责统一捕获、展示和停止推进，不替配置人员猜测默认值、自动切换其他分支或重复调用接口。

**当前状态：已冻结，不再作为待讨论项。**

### P0-10 主预览与独立运行页的统一方式

**问题**

主 `src` 与 `page/src/page/index.tsx` 当前各自维护流程推进逻辑，已经存在取消控制、变量合并、control 渲染和导航类型文案差异。

这两组文件当前可以先简单理解为：

- 主 `src`：NGAP 管理应用中的流程编辑、预览及其运行入口；
- 独立 `page`：项目单独构建的一套流程运行页面，具体生产使用位置和发布链路需要在实施阶段继续核实。

**需要确认**

- 共享运行引擎放在哪个公共包；
- 主项目与独立 page 如何注入不同请求、用户、路由和监控适配器；
- 是否允许过渡期双写节点域和旧 page scope；
- 如何建立同一流程定义在两个运行时的契约测试。

**推荐结论**

- 抽取唯一 `GuidedRuntime`、binding resolver、condition evaluator 和 rollback manager；
- 两个运行时只保留宿主适配差异；
- 以同一组 JSON 流程用例执行 contract tests；
- 禁止在两份页面组件中继续复制新增流程规则。

**2026-08-12 处理结论**

- 本项属于代码结构和发布入口的技术实现问题，不要求需求侧现在选择共享包放在哪里；
- 暂不纳入当前产品讨论，也不作为需求冻结或开始开发简单流程的前置条件；
- 开发实施时先核实独立 `page` 的实际生产用途，再保证真实使用的预览和发布入口行为一致；
- 在用途核实前，不贸然删除其中任意一套文件。

**当前状态：暂缓，转为实施阶段技术核查项。**

### P0-11 header/footer 的生命周期

**2026-08-12 已确认结论**

- header、footer 虽然作为节点保存在流程图中，但运行时属于页面固定区域，不按普通 content 的命中时机显示；
- 每次进入页面或整体重启时，运行时先找到 header、footer；header 作为 content 的前置初始化环节执行，footer 作为固定操作区挂载，然后才启动实际 content/control 活动路径；
- header 的初始化接口和组件逻辑必须执行完成后，才允许加载 content。header 可以把接口结果写入显式全局变量，后续 content 通过全局变量读取这些公共数据；
- footer 区域在本次页面运行期间固定挂载，一般不整体卸载。随着流程进行，footer 内部的某个按钮或其他元素可根据 content 选项、组件变量、节点输出或全局变量动态显隐；
- header、footer 不配置人工/自动方式，不作为人工等待点，也不需要“完成”动作；
- footer 按钮只执行各自配置的业务事件或接口动作，不承担推进流程、完成 footer 或选择 `nextNodeId` 的职责；
- 流程拓扑约束继续保留：存在 header 时开始节点必须且只能直接连接 header；footer 在拓扑上是结束前最后一个业务节点，可以接收互斥路径汇合，且唯一后续只能是结束节点；
- 当实际 content/control 路径走到 footer 时，运行时不等待按钮，直接沿 footer 的唯一出线进入结束节点；footer 已经在页面初始化时渲染，不重复挂载；
- 流程进入结束节点后，只要当前页面未关闭或整体重启，header、footer 仍保持页面固定区域；
- 前序 content 修改造成分支回滚时，只回滚受影响的 content/control 路径；header、footer 不因分支切换被卸载，但其依赖数据和显隐状态应随最新 content 结果重新计算；
- header 初始化接口若发生技术异常，统一按 P0-09 直接报错并停止后续 content 加载；footer 自身业务动作发生异常时，也按 P0-09 展示错误，不替配置人员自动重试。

**当前状态：已冻结，不再有待确认项。**

---

## 4. P1：主干开发期间必须确认

### P1-01 组件变量、全局变量与引用完整性校验

**2026-08-12 已确认方向**

- 组件作用域已经解决同名变量互相覆盖问题，本项不额外建设一套输出契约主版本体系；
- 真正需要保护的是引用完整性：组件节点或组件变量被删除、修改后，分支、导航、footer、下游输入或全局变量可能失去数据来源；
- 平台必须在删除、结构性修改、保存和发布时校验，不能等运行时才发现；
- 引用必须保存为结构化技术 ID，不能依赖显示名称或在表达式字符串中搜索变量名。

**结构化引用**

```text
组件变量来源：nodeId + variableId
组件输出来源：nodeId + outputKey/outputId
全局变量目标：globalVariableId
读取关系：sourceRef → consumerRef
赋值关系：writerActionId → targetRef
```

- 节点、组件变量和全局变量都使用不可变技术 ID；显示名称改名不导致引用断开；
- 分支条件、导航字段、footer 显隐、输入绑定、输出映射和变量赋值动作都保存结构化引用；
- 新脚本不能只在文本中自由读取任意变量。变量选择器选中的读写项同时保存到脚本动作的 `reads/writes` 依赖元数据；自由书写的动态属性访问无法追踪时，保存校验应报错或要求改为显式声明。

**校验时机**

1. 删除节点、删除变量、改变变量类型或改变输出来源前，执行即时反向引用检查；
2. 保存草稿时重建全流程依赖图，展示完整错误和警告；草稿允许保留未完成配置，但必须明确标记为不可发布；
3. 提交审核、发布或上线前执行强校验，存在阻断错误时不允许继续；
4. 加载历史流程时再次校验；若旧数据已经存在失效引用，显示配置错误，不使用空值静默运行。

**删除和修改规则**

- 无引用时允许直接删除；
- 存在引用时默认阻止删除，并展示引用位置、用途和所在节点；
- 配置人员必须先重新绑定来源、删除对应依赖，或明确使用“删除节点及相关配置”并查看将被清理的完整清单；
- 平台不按同名变量自动换绑；
- 改显示名称不影响引用；删除变量、改变数据类型或改变业务含义需要重新校验消费者。

**全局变量来源保证**

全局变量不仅要检查“有没有赋值动作”，还要检查“使用前是否保证已经赋值”：

- 有有效默认值：可以作为保证来源；
- header 初始化成功后写入：对后续 content 是保证来源；
- 在某个 content/control 中无条件赋值：只保证其后的同一路径；
- 普通可选按钮、未参与环节完成的 change、条件分支或部分路径赋值：属于条件来源，不能保证所有路径；
- 如果 change/确认事件链中的赋值是当前人工环节形成有效结果并继续流程的必经动作，且赋值失败时节点不能完成，则可以归一化为“节点完成前保证赋值”；
- 对每个必需读取点，流程图从开始到该点的每条可达路径上都必须存在先于它执行的有效赋值，或者全局变量有默认值；
- footer 响应式显隐可以声明为“允许未赋值”：未赋值时按隐藏或配置的默认状态处理；作为接口必填参数、分支条件或节点必需输入时则必须保证赋值。

**阻断错误**

- 引用的节点、组件变量、输出或全局变量不存在；
- 来源与使用方类型不兼容；
- 必需读取点存在至少一条路径在使用前没有赋值；
- 依赖形成无法求值的循环；
- 脚本读写了未声明、无法追踪的组件变量或全局变量；
- 删除节点后，全局变量虽仍存在，但其必需消费者已经没有任何保证赋值来源。

**提示或警告**

- 全局变量只有条件赋值来源，但当前消费者允许空值；
- 全局变量存在多个写入来源，需要展示各自所在路径和触发时机；
- 变量或输出已经没有任何消费者；
- footer 元素在流程早期读取尚未赋值的数据，但已经配置未赋值时的显隐默认值。

**示例**

```text
全局变量 customerLevel
  ← 客户判断节点(node-customer-check).level

使用位置：
  → footer“办理”按钮显隐
  → 套餐推荐节点的必需输入
```

删除“客户判断节点”时，平台应显示以上影响并阻止直接删除。只有重新绑定 `customerLevel`、给它提供满足路径要求的其他赋值来源，或同步删除所有依赖后，才允许完成修改并发布。

**当前状态：引用保护和校验方式已冻结；具体依赖图数据结构与脚本静态分析实现放到技术设计。**

### P1-02 同一组件模板多次使用

**2026-08-12 已确认结论**

- 平台需要支持通用组件模板复用。例如多个环节都只是表格展示时，应复用同一个表格组件，不为每个环节复制一份组件；
- `componentId` 标识通用组件模板，`nodeId` 标识该模板在当前流程中的环节实例，`nodeExecutionId` 标识该节点的一次运行；
- 相同 `componentId` 在同一流程中可使用多次，每次生成不同 `nodeId`，所有运行数据按 `nodeId` 隔离；
- 通用组件负责共同的表格布局、加载方式、空态和交互能力；每个节点实例分别配置自己的后端接口、接口入参、环节标题、导航标题、结果字段映射、诊断展示和分支结果；
- 接口不应写死成组件模板只能调用一个固定接口。组件模板声明可配置的接口能力或接口插槽，流程节点实例选择实际接口并配置参数绑定；
- 每个实例拥有独立的组件变量、表单数据、API 结果、输出、诊断状态、加载状态和错误状态；同名内部变量不会互相覆盖；
- 一个实例重新请求、修改结果、分支回滚、刷新或删除时，不影响其他使用相同组件模板的节点实例；
- 节点标题和环节结果属于节点实例配置或运行结果，不应修改组件模板本身；
- 复制流程节点时生成新 `nodeId`，可以复制原节点的接口和展示配置作为初始值，但后续两者独立修改；
- 删除通用组件模板或修改其变量/配置能力时，按 P1-01 对所有节点实例执行引用影响校验。

**示例**

```text
通用组件模板：CommonTable（componentId = table-common）

节点A：在途工单
  nodeId = node-orders
  接口 = queryPendingOrders
  环节标题 = 在途工单
  诊断结果 = 有在途工单 / 无在途工单

节点B：可办理套餐
  nodeId = node-packages
  接口 = queryAvailablePackages
  环节标题 = 可办理套餐
  诊断结果 = 查询到可办理套餐 / 暂无可办理套餐
```

两个节点使用同一个表格模板，但接口数据、组件变量、输出、导航结果和分支状态完全独立。

**当前状态：已冻结。**

### P1-03 多页签、重复点击和重复提交

**2026-08-12 已确认结论**

- 当前业务不会出现两个页签同时操作同一流程实例，不设计多页签冲突、后端乐观锁或流程快照版本；
- 人工环节第一次有效点击后，当前事件链进入处理中状态；完成前对同一动作的重复点击直接忽略，相关按钮或选择控件可显示 loading/暂时禁用；
- 当前结果已经生效后再次选择相同值，不重复调用接口、不重复计算分支，也不重复追加后续节点；
- 改成不同有效结果不属于重复点击，按 P0-07 生成新的节点执行批次、使旧结果失效，并回滚重载后续路径；
- 每次有效操作使用新的 `nodeExecutionId/actionExecutionId`。旧异步请求晚返回时，执行 ID 不再有效则拒绝写入；
- footer 按钮也使用同一实例内的前端动作锁，避免连续点击重复执行业务动作；footer 仍不推进流程；
- 平台只负责前端重复触发保护，接口本身的业务重复调用规则仍由配置开发人员保证。

**当前状态：已冻结。**

### P1-04 业务记录所需的事件触发点（平台不管日志）

**2026-08-12 已确认结论**

- 平台完全不负责业务日志：不定义记录模型、记录内容、接口格式、存储、查询、轨迹或脱敏规则；
- 环节记录、人工点选记录、按钮操作记录等均属于各业务模块自己的业务动作，由组件和配置开发人员选择自己的接口、参数和调用时机；
- 人工点选记录继续配置在具体单选、下拉、卡片等元素的 `change/click` 事件中；按钮操作记录配置在按钮 `click` 事件中；人工确认后的记录配置在该确认事件的动作链中；
- 平台目前缺少的是“环节加载完成”这个可配置触发点，因此第一版只补充节点实例级 `onNodeLoaded` 事件；
- `onNodeLoaded` 只是通用事件，不带“写日志”语义。配置人员可以用它调记录接口，也可以执行其他正常业务动作；
- 同一通用组件模板用于多个节点时，`onNodeLoaded` 属于节点实例配置，不同环节可以调用完全不同的业务接口；
- 所有节点和元素事件上下文都提供只读的“当前有效 content 路径” `context.process.activeContentPath`，业务可以取得当前已经加载且最终仍成立的全部 content 环节；
- 该列表就是当前流程的有效轨迹/当前最终态，只反映现在页面上成立的活动路径，不包含任何已经回滚失效的旧分支，也不表示曾经走过的历史轨迹；
- 事件中接口动作的执行和失败处理沿用平台现有事件动作链及 P0-09，不增加日志专用的阻断、重试或失败策略。

**事件时机定义**

```text
onNodeLoaded
  节点所需接口和初始化逻辑已完成，数据已写入节点域，组件已具备展示条件；
  自动节点在判断分支并进入下一节点之前触发；
  人工节点在进入等待用户操作之前触发；
  header 在允许content开始之前触发；footer在固定区域首次挂载和初始化后触发；
  control在本节点初始化和配置动作完成、进入分支判断之前触发。
```

事件上下文至少允许读取当前 `nodeId`、组件变量、API结果、表单数据、节点输出候选值和显式全局变量，以便业务配置自己的接口动作；读取和写入仍受 P1-01 的结构化引用校验约束。

当前有效路径建议返回只读摘要，不暴露其他组件的 DOM、React state 或私有变量：

```ts
interface ActiveContentPathItem {
  sequence: number;
  nodeId: string;
  componentId: string;
  title: string;
  navigatorTitle?: string;
  interactionMode: 'manual' | 'automatic';
  output: Readonly<Record<string, unknown>>;
  diagnosis?: {
    value?: unknown;
    label?: string;
    status?: 'success' | 'failure';
  };
}
```

- `onNodeLoaded` 触发时，列表已经包含刚加载完成的当前 content；
- header、footer、control 不进入该列表；
- 分支切换时先原子化移除旧后续路径，再加入新分支；后续 `onNodeLoaded/change/click/confirm` 取得的都是切换后的当前有效最终态；
- 人工点选轨迹可以在对应 `change/click` 事件中同时提交事件值和 `activeContentPath`；环节加载轨迹可以在 `onNodeLoaded` 中提交当前节点和完整路径快照；
- 平台只提供当前有效最终态快照，不保存历史记录，也不定义业务接口参数。

**当前状态：已冻结。平台不管日志，只补充缺失的节点实例级 `onNodeLoaded` 事件；其他业务记录复用组件现有事件。**

### P1-05 表达式安全

**2026-08-12 处理结论**

- 表达式沙箱、`new Function` 替换、受限 DSL、网络和全局对象权限属于整个 NGAP 平台级安全改造，不属于本次引导式改造；
- 本次引导式继续复用平台现有事件、脚本和变量赋值能力，不重写脚本引擎；
- 引导式分支不新增自由 JavaScript 入口，只匹配组件内部计算出的稳定业务值；
- 为支持 P1-01 删除和保存校验，脚本动作选择组件变量或全局变量时仍需保存结构化 `reads/writes`；这属于引导式引用完整性，不代表本期治理脚本执行安全；
- 旧脚本保持兼容，不因本次改造强制迁移。

**当前状态：移出本次范围，不再讨论，也不作为开发或验收门槛。**

### P1-06 数据权限和敏感输出

**2026-08-12 处理结论**

- 敏感字段分类、字段级权限、脱敏、Token治理和自定义组件安全边界属于NGAP平台级数据安全能力，不属于本次引导式改造；
- 本次只实现已经冻结的功能作用域：组件直接读取当前节点数据，并可读取流程中显式定义的全局变量；其他组件数据通过明确组件实例引用取得；
- 不在本次引导式需求中新增敏感标记、字段授权、统一脱敏或安全审计系统。

**当前状态：移出本次范围，不作为开发或验收门槛。**

### P1-07 旧流程迁移

**2026-08-12 修订结论**

- 当前虽然没有既有生产引导式流程，但实施会分阶段进行：第一阶段先上线 header/footer 并搭建一个正常可用的试点流程，节点变量作用域会在后续阶段完善；
- 因此第一阶段创建的试点流程，在节点作用域上线时会成为“过渡版流程”，P1-07不能完全关闭；
- 预计过渡流程数量少、组件变化不大、连线也不多，不建设面向大量未知历史数据的重型通用迁移系统；
- 第一阶段保存结构必须提前保留稳定 `nodeId`，并写入 `guidedSchemaVersion`。不能等节点作用域开发时再猜节点身份；
- 节点作用域版本上线后，对试点流程执行一次轻量升级：保留节点、连线、header/footer/content设置和组件实例配置，把能够确定归属的组件变量迁入对应 `nodes[nodeId].variables`；
- 明确声明的流程全局变量继续保留为全局变量；组件内部变量不再默认并入全局；
- 分支、footer显隐、事件动作和接口参数中能够确定来源的引用，自动转换为结构化引用；来源不明确或同名冲突的引用列出清单，由配置人员人工选择；
- 迁移不能静默猜测，也不能自动按同名换绑。迁移后必须执行P1-01全量引用校验，存在错误时允许保存草稿但不能发布；
- 因试点流程规模小，也允许配置人员直接重建个别组件或连线；平台至少要保证流程拓扑和header/footer配置无需从零重画；
- 现有业务组件和平台事件动作若继续复用当前变量/接口格式，可由运行时做必要适配，这属于组件运行兼容。

**推荐实施顺序**

```text
第一阶段：header/footer + 稳定nodeId + guidedSchemaVersion
→ 搭建首个试点流程
→ 第二阶段：节点变量作用域 + 结构化引用
→ 打开试点流程时执行轻量升级
→ 配置人员处理少量歧义
→ 引用校验通过后重新发布
```

**当前状态：重新打开，按少量试点流程的轻量迁移处理，不建设重型通用迁移系统。**

### P1-08 组件输出和后端可信结论的边界

**2026-08-12 处理结论**

- 哪些业务规则由前端计算、哪些必须由后端接口返回，属于各业务模块及整个项目的系统设计，不是引导式运行时应统一规定的产品能力；
- 引导式只消费组件最终提供的稳定业务结果，根据已配置连线选择路径，不判断该结果应由前端脚本还是后端接口产生；
- 组件开发人员可以把后端返回的 `decisionCode` 写入组件变量或节点输出，也可以按业务需要在组件内部计算结果；其可信性和提交依据由对应业务负责；
- 本次不新增全项目的前后端可信规则、审批权限或资金安全治理。

**当前状态：移出本次范围，不作为引导式开发或验收门槛。**

### P1-09/P1-10 流程画布体验与容量

P1-09与原P1-10合并为同一画布优化专题，但保留两个独立验收维度：

- **容量和性能**：解决节点、连线和组件配置增多后的打开、拖动、缩放、编辑和保存卡顿；
- **布局和可读性**：支持左右、上下两种整图连线方向，改善复杂流程的阅读和排布。

#### A. 大流程编辑器容量与优化路线

旧场景中已经存在 77、110 个环节的真实流程，产品确认单流程最多不超过 150 个业务节点。当前约 20 个节点时已经观察到编辑应用出现卡顿，因此“大流程能够稳定打开和编辑”是旧流程迁移的前置能力，不能只作为上线后的普通体验优化。

容量不能只按业务节点数判断，还必须同时统计：

- 业务节点数、开始/结束节点数、连线数和分支出口数；
- 每个业务节点内部的原子元素数量、全部节点的原子元素总量；
- 节点完整组件配置的序列化体积，以及接口、变量、事件和表达式配置体积；
- 编辑器同时挂载的 DOM/SVG 数量和 Store 订阅数量；
- 撤销重做条数及每条历史记录实际复制的数据量；
- 运行时当前活动路径累计进入的节点数、实际渲染元素数和接口结果体积。

当前代码已观察到的主要风险：

- 编辑器一次加载全部流程节点，并为所有节点批量取得完整业务组件定义；
- 画布节点对象携带完整 `componentData`，节点内部元素虽然不直接画在流程卡片上，仍会进入内存、Store 更新和深拷贝；
- 拖动一个节点时，每次 `mousemove` 都会遍历并刷新全部连线，同时更新 `lineArr`；
- 撤销重做快照会深拷贝 `nodeMap`、分支数据、组件列表和流程数据，节点内部元素越多，复制成本越高；
- 所有画布节点同时挂载，尚无按视口渲染或大图降级策略；
- 引导式运行页会累计当前活动路径上的实际元素，长路径中复杂表格、表单、订阅和接口结果同样可能造成卡顿。

暂定优化路线：

1. 先优化现有画布，用真实 110 节点流程和模拟 150 节点流程验证；第一阶段不预先锁定 React Flow、X6、LogicFlow 等具体开源库；
2. 编辑器拆分“流程拓扑摘要”和“完整业务组件定义”。画布节点只保留 `nodeId/componentId/name/position/presentation/branchSummary` 等轻量数据，节点内部元素、接口、变量和事件在打开编辑或预览时再按需加载；
3. 节点卡片只展示摘要和必要统计，不在流程画布内挂载业务组件的真实原子元素；关闭节点编辑器后释放不再使用的完整组件详情缓存；
4. 建立节点到入线/出线的邻接索引。拖动时只刷新当前节点相关连线，通过 `requestAnimationFrame` 合并高频坐标更新，拖动结束后再提交 React/Store 状态和历史记录；
5. 撤销重做只记录轻量拓扑快照或操作差异，不复制完整 `componentData`、原子元素树和大体积接口结果；历史记录需要设置合理容量；
6. 节点、连线、选中态和编辑弹窗使用细粒度订阅，单节点变化不能使全部节点卡片和无关面板重新渲染；
7. 初始化和保存阶段避免反复全量 DOM 查询、同步布局测量和不必要的整图序列化；必要时再增加按视口渲染；
8. 运行时按实际命中路径懒加载组件资源，字段变化只通知当前节点和显式依赖者；已完成复杂节点是否转为只读摘要、保留完整交互 DOM 或按需恢复，需要结合返回修改要求单独确定；
9. 若完成上述数据瘦身和更新隔离后仍不能通过 150 节点验收，再使用同一份数据对候选开源画布做原型对比，替换的仅是拖拽、缩放、连线、连接点和视口等画布基础层，不改变现有流程协议与引导式运行语义。

压测必须覆盖“节点少但单节点元素多”和“节点多但单节点简单”两种情况，至少记录：首屏可操作时间、拖动/缩放帧耗时、单节点编辑打开时间、连线更新时间、撤销重做耗时、保存序列化耗时、DOM/SVG 数量、JS 堆内存和长任务。元素容量不能凭节点数换算，应先统计真实 110 节点流程的元素分布和配置体积，再据此确定总元素数与单节点峰值的正式验收线。

**当前状态：单流程业务节点上限暂定 150；现有画布尚未证明可承载，先优化并压测，是否替换开源画布由同数据基准结果决定。**

#### B. 画布连线方向

- 流程级支持左右、上下两种连线方向；
- 左右模式为左侧进入、右侧流出，上下模式为上方进入、下方流出；
- 切换只改变连接点和线路径，不改变节点关系、分支顺序、条件和执行语义；
- 第一版不因切换方向自动重排节点，自动布局作为后续独立能力；
- 旧流程缺少配置时继续使用当前左右模式；
- 使用真实复杂流程验证多分支出口、长连线、交叉线、箭头和分支文字。

**当前状态：画布体验专题已形成方案。容量上限按150个业务节点压测；方向第一版支持整图左右/上下切换，二者可同一阶段开发和验收，但互不替代。**

---

## 5. P2：当前阶段整体暂不考虑

**统一结论**

- P2-01～P2-05 当前均不继续讨论，不纳入本轮引导式改造的设计、开发和验收范围，也不作为正式推广的前置条件；
- 以下条目仅作为未来可能重新评估的备忘，不代表已经承诺建设；
- 后续只有产品明确重新提出时，才单独恢复相应事项的评审。

### P2-01 编辑器易用性

- 输出映射能否自动推荐；
- 分支条件是否提供自然语言预览；
- 是否显示“该输出被哪些节点使用”；
- 复杂条件是否提供测试数据试跑；
- 复制节点时输出和引用如何处理。

### P2-02 流程调试

- 是否支持从某节点开始试跑；
- 是否允许注入模拟 input/output；
- 是否显示每次条件判断的左右值；
- 是否能对比两次分支运行结果；
- control 如何在画布上显示执行轨迹。

### P2-03 性能

- P1-09 负责旧流程迁移所需的大流程编辑器容量，本节继续关注引导式运行期和资源治理；
- 长流程是否按节点懒加载组件资源；
- 节点连续执行接口和规则时是否需要让出浏览器渲染线程；
- 大型 output 是否只存引用或分页数据；
- 节点字段变化是否只通知当前节点和显式依赖者；
- 旧路径释放后是否存在组件、订阅和 Blob 资源泄漏。

### P2-04 可访问性和移动端

当前结论：项目为内网使用场景，本轮不考虑可访问性和移动端适配，不提出键盘全流程操作、小屏适配、焦点管理或屏幕阅读器支持要求。

### P2-05 灰度、回滚和版本治理

- 新旧 GuidedRuntime 是否按流程版本灰度；
- 发布失败后如何回退到旧定义和旧组件版本；
- 流程定义、组件版本和输出契约如何形成可复现快照；
- 运行中的老实例是否继续使用创建时版本。

推荐：运行实例锁定流程定义版本和组件版本，新版本只影响新实例，除非产品明确发起迁移。

---

## 6. 当前代码中已经观察到的风险依据

| 位置 | 当前表现 | 对应待完善事项 |
| --- | --- | --- |
| 主 `ProcessPage.tsx` 的 `formData` effect | 关联原子值一变化就截断后续 DOM 并重新判断 | P0-01、P0-07 |
| 主 `ProcessPage.tsx` 的 `branchElementData` | 通过 `atomId` 收集内部值，没有节点正式输出 | P0-03、P0-04 |
| `checkBranch()` | 支持条件列表和 AND/OR，但数组主要逐项比较 | P0-04、P0-05 |
| `addProcessNode()` | 旧版按 AT/MT 递归或暂停，需要统一为自动连续执行、人工等待交互 | P0-01、P0-09 |
| `refreshPage()` | 主要清空渲染元素并重新从开始节点加载 | P0-08 |
| 主运行时 `currentRenderId` | 已尝试阻止旧分支继续渲染，但不是完整节点执行实例模型 | P0-07、P1-03 |
| 独立 `page/src/page/index.tsx` | 与主运行时存在取消、变量、control 和导航文案差异 | P0-10 |
| 页面级 `formData/variableData/apiOutData` | 多节点写入同一页面级容器 | P0-03、P0-07、P1-02 |
| 变量名追加组件 ID | 属于重名规避手段，不是真正节点域 | P0-03、P1-07 |
| 动态表达式 `new Function` | 新配置继续扩展会增加安全和可诊断风险 | P1-05 |
| 主 `src` 与独立 `page` 的 VA 变量监听 | `page` 对 `checkVABranch() === false` 有保护，主预览对应重算链路保护不完整 | P0-04、P0-10 |

---

## 7. 建议评审顺序

不要一次评审全部细节，建议按下面顺序收敛：

1. 冻结单活动路径、节点类型、展示区域和前后端职责；
2. 冻结自动 content 连续执行、人工 content 交互恢复、组件输出和推进模型；
3. 冻结输出 Schema、输出映射和多选条件语言；
4. 冻结连线唯一命中、默认分支和完整拓扑校验；
5. 冻结回退修改、分支回滚、刷新和断点恢复；
6. 按已冻结规则处理接口技术错误：直接报错并停止推进；
7. 主 `src` 与独立 `page/materials` 的用途和共享实现转入开发阶段技术核查，不占用当前产品评审；
8. 再讨论输出契约版本、旧数据迁移、安全、日志和灰度。

每次评审至少形成：

```text
事项编号：
最终结论：
适用范围：
不支持范围：
前端责任：
后端责任：
兼容规则：
验收用例：
确认人和日期：
```

---

## 8. 开始编码前的冻结门槛

满足以下条件后，再进入引导式运行时主干编码：

- P0-01 至 P0-07 形成明确产品结论；
- 确认是否需要第一版断点续办；
- 至少取得一个真实人工多字段/多选分支样例；
- 至少取得一个真实自动接口分支样例；接口技术异常统一按“报错并停止”验收，不要求为其设计失败流程；
- 输出 Schema、输出映射、完成动作和分支条件能够组成完整保存结构；
- 明确本期真实使用的预览和发布入口；共享代码的具体落点不作为产品冻结门槛；
- 旧流程兼容和回滚策略有可执行方案；
- 测试用例覆盖 `A-B-D / A-C-D`、返回修改、多选组合、接口异常直接中止和刷新后从头执行。

---

## 9. 与其他文档的关系

- 项目上下文交接记忆（`CODEX_CONTEXT.md`）：新会话首先阅读，了解当前进度和已冻结结论；
- 引导式流程展示编排升级设计（`GUIDED_PROCESS_REDESIGN(引导式流程展示编排升级设计).md`）：保存已经形成的完整目标方案；
- NGAP 平台与重点优化需求入门指南（`NGAP_REQUIREMENTS_ONBOARDING(NGAP需求人员入门指南).md`）：供新进需求人员理解平台和两项优化；
- NGAP 核心改造记录（`REFACTOR_NOTES(NGAP核心改造记录).md`）：记录真实接口样例、数据缺口和阶段状态；
- 自定义元素完整设计·理想方案（`CUSTOM_ELEMENT_REDESIGN(完整设计-理想方案).md`）：引导式稳定后再实施的第二优先级方案。
