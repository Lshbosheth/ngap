# 自定义元素 v2 设计文档问题清单

> 针对自定义元素完整设计·理想方案（CUSTOM_ELEMENT_REDESIGN）的合理性和逻辑审查

## 一、必须修复的问题（阻塞实施）

### 问题 1：协议识别字段是硬性要求，不是可选

**当前状态：**
- 16.4 节说"若后端只增加一个字段时，优先增加 `elementProtocolVersion`"
- 16.3 节提供了"不改后端字段的过渡方案"

**问题：**
靠解析 `elementConfigDemo` 内容判断协议版本非常不可靠：
- 如果 JSON 格式错误会怎样？
- 如果误把 v1 Schema 识别成 v2 manifest 会导致编译崩溃
- 回滚时可能把 runtime-manifest.json 当 Schema TS 编译

**建议：**
```diff
- 若后端能增加一个字段，优先增加 elementProtocolVersion
+ elementProtocolVersion 字段是硬性要求，必须在后端接口评审时确认
+ 过渡方案仅用于开发验证，不能作为生产方案
```

**影响范围：** 后端接口设计、前端加载逻辑、回滚兼容性

---

### 问题 2：AST 推导失败时的用户体验缺失

**当前状态：**
- 9.2 节列出了支持的 Props 声明方式
- 说"第一阶段不完整支持复杂类型"
- 说"遇到不能解析的类型时，对应属性不自动暴露"

**问题：**
用户上传后看到空白属性面板，不知道：
- 为什么推导失败？
- 哪些属性没识别出来？
- 应该怎么补充配置？

**建议补充：**

#### 9.2 节增加"推导失败的用户提示"小节：

```markdown
### 推导失败提示

当 AST 无法完整推导 Props 时，上传页面必须显示：

1. **明确的诊断信息**：
   - CE2001：未找到可分析的 Props 类型
   - CE2104：title 的默认值需要运行代码，平台未采纳
   
2. **待补充字段清单**：
   ```
   检测到 3 个属性，已识别 1 个，需要手动补充 2 个：
   - title: string ✅ 已识别
   - variant: 复杂联合类型 ⚠️ 需要在 ngap.json.props 中补充
   - onCustomEvent: 泛型函数 ⚠️ 需要在 ngap.json.events 中补充
   ```

3. **操作引导**：
   点击"补充配置"按钮，跳转到 ngap.json 编辑器，自动定位到需要补充的字段

4. **示例配置**：
   自动生成待填写的 ngap.json 模板：
   ```json
   {
     "props": {
       "variant": {
         "label": "展示类型",
         "editor": "Select",
         "options": [
           { "label": "默认", "value": "default" }
         ]
       }
     }
   }
   ```
```

**影响范围：** 上传页面交互设计、分析器错误输出

---

### 问题 3：SDK 权限的真实作用被误导性表述

**当前状态：**
- 8.3 节详细设计了权限声明和审批流程
- 18.2 节说"SDK 权限属于公共 API 治理和审计边界，不是浏览器级安全沙箱"
- 但整个文档按"权限能隔离"的假设设计

**问题：**
容易让后端开发误以为做了权限检查就能防止恶意代码：

```javascript
// SDK 检查权限
if (!hasPermission('variables.read')) {
  throw new Error('SDK_PERMISSION_DENIED');
}

// 但组件可以直接绕过
const token = window.localStorage.getItem('token');
fetch('/api/sensitive', { headers: { token } });
```

**建议修改：**

#### 8.3 节"SDK 权限声明"增加安全提示框：

```markdown
### ⚠️ 首期安全边界说明

**SDK 权限在 `trusted-main-window` 模式下的作用：**

✅ **能做到的：**
- 审计组件使用了哪些平台能力
- 强制组件使用标准 API，不直接依赖内部实现
- 新增权限触发重新审核
- 便于后续能力升级和 API 版本管理

❌ **不能做到的：**
- 阻止组件访问 DOM、Cookie、localStorage
- 阻止组件直接发起网络请求
- 阻止组件访问 window 全局对象
- 阻止组件运行任意 JavaScript 代码

**首期依赖的真正安全措施：**
1. 只有可信内部角色可上传（通过组织架构控制）
2. 人工审核源码（安全团队 Review）
3. 服务端静态扫描（检测明显危险模式）
4. 发布产物签名（防止篡改）

**未来如果开放给外部团队/租户/第三方：**
必须切换到 `sandbox-iframe` 模式，SDK 权限才能作为真正的安全隔离边界。
```

#### 18.2 节"首期可信执行模型"移到前面（第 8 章之后）

把安全模型的限制在设计 SDK 权限之前就说清楚，避免误导。

**影响范围：** 后端权限实现、安全审核流程、未来外部开放策略

---

### 问题 4：构建异步化的用户体验不明确

**当前状态：**
- 16.2 节提供了 `/element/buildElementPackageV2` 异步构建接口
- 说"首次响应只表示任务受理，返回 buildId"
- 说"上传页以 1~2 秒退避轮询"

**问题：**
用户体验完全不清楚：
- 用户上传 ZIP 后等多久能预览？5 秒？30 秒？
- 能不能先用浏览器快速预览，服务端慢慢构建？
- 如果服务端构建队列拥堵了怎么办？
- 构建失败了要重新上传吗？

**建议补充：**

#### 15.4 节"步骤 3：分析与配置"增加构建模式说明：

```markdown
### 构建模式

#### 开发预览（快速反馈）
- 用户上传 ZIP 后，浏览器立即解包和分析
- 在前端使用轻量级构建器（Vite/esbuild-wasm）生成临时产物
- 3~5 秒内可预览组件效果
- 同时异步触发服务端正式构建
- ⚠️ 浏览器预览仅供开发调试，不能直接发布

#### 提交审核（正式构建）
- 必须等待服务端构建完成（通常 10~30 秒）
- 显示构建进度：queued → analyzing → bundling → scanning → finished
- 构建失败显示详细诊断，可在线修改 ngap.json 后重新构建
- 审核人预览的是服务端构建产物，不是浏览器临时产物

#### 已发布运行（不可变产物）
- 只加载服务端构建、签名的不可变产物
- 不再执行任何编译或解包
```

#### 25 节"首期切片"明确构建策略：

```diff
  服务端 ZIP preview build
+ 首期支持双模式：浏览器快速预览 + 服务端异步构建
+ 开发预览可使用浏览器构建，提交审核必须等服务端构建完成
```

**影响范围：** 上传页面交互、构建服务容量规划、用户等待时间预期

---

### 问题 5：版本锁定和升级策略不明确

**当前状态：**
- 16.5 节说"新 v2 实例立即保存 elementVersion + artifactHash"
- 15.7 节说"编辑已发布元素创建新草稿版本"
- 但没说清楚已有实例会不会自动升级

**问题：**
- 用户在页面 A 拖入了"客户卡片 v1.0"
- 组件作者发布了"客户卡片 v2.0"（修复了 bug）
- 页面 A 会自动升级吗？还是继续用 v1.0？
- 如果不自动升级，用户怎么知道有新版本？
- 如果自动升级，配置不兼容怎么办？

**建议补充：**

#### 16.5 节增加"版本升级策略"小节：

```markdown
### 版本升级策略

#### 版本锁定时机

**创建实例时：**
```json
{
  "type": "2608051421540100045",
  "elementVersion": "1.0.0",
  "elementArtifactHash": "abc123..."
}
```
- 拖入画布时锁定当前发布版本
- 页面保存后锁定该版本，不会自动升级

**已保存页面：**
- 缺少 `elementVersion` 的旧数据兼容为 "latest"
- 运行时加载最新已发布版本
- ⚠️ 存在隐式升级风险，建议显示升级提示

#### 升级提示

**编辑器提示：**
打开页面时检测元素版本：
```
客户信息卡片有新版本可用
当前：v1.0.0 (2024-01-01)
最新：v2.0.0 (2024-01-15) - 修复了数据刷新问题

[查看更新日志] [升级到 v2.0.0] [保持当前版本]
```

**批量升级：**
- 提供"元素版本管理"页面
- 列出使用了旧版本元素的所有页面
- 支持批量升级 + 影响范围评估

#### 配置迁移

元素在 manifest 中可声明迁移规则：
```json
{
  "migrations": [
    {
      "from": "1.0.0",
      "to": "2.0.0",
      "renameProps": { "oldTitle": "title" },
      "removeProps": ["deprecated"],
      "defaults": { "variant": "default" }
    }
  ]
}
```

声明式迁移自动执行；复杂迁移需要人工介入。
```

**影响范围：** 元素菜单显示、编辑器提示、页面运行时加载逻辑

---

## 二、建议优化的问题（不阻塞但重要）

### 问题 6：首期支持多模块的必要性存疑

**当前设计：**
完整支持 ZIP 内多个 TSX/TS 模块、相对 import、模块图、循环依赖检测

**质疑：**
- 大多数自定义元素是单文件小组件（200 行以内）
- 复杂组件应该拆成多个独立元素，而不是一个大 ZIP
- 多模块会带来类型推导失败、循环依赖、构建复杂度等问题
- 首期投入过多精力在边缘场景上

**建议：**
```markdown
## 首期范围调整

### v2.0 首期（3~4 人月）
- ✅ 单入口 TSX/TSX
- ✅ 本地类型文件（types.ts）
- ✅ 样式文件（Less/CSS）
- ✅ 静态资源（图片、SVG）
- ❌ 暂不支持多业务模块拆分

### v2.1 扩展（2~3 人月）
- ✅ 支持包内多业务模块
- ✅ 完整模块图和循环依赖检测
- ✅ 更复杂的类型推导

**理由：**
首期聚焦核心价值（ZIP 上传、ngap.json、SDK、服务端构建），
待核心稳定后再扩展边缘能力。
```

**收益：** 降低 30~40% 首期工作量，更快上线核心能力

---

### 问题 7：构建器版本升级策略缺失

**当前状态：**
11.5 节说"构建器版本、插件版本和 externals 必须锁定"

**问题：**
- 构建器从 2.0 升级到 2.1 后，旧组件怎么办？
- artifactHash 会变吗？
- 已发布的组件会失效吗？
- 需要重新构建所有组件吗？

**建议补充：**

```markdown
### 构建器版本管理

#### 版本隔离
每个构建器版本维护独立的产物：
```
/custom-elements/{elementId}/{version}/{builderVersion}/{artifactHash}/
```

#### 升级策略

**构建器 minor 升级（2.0 → 2.1）：**
- 新上传组件使用新构建器
- 旧组件继续使用旧构建器产物
- 可选择"重新构建"触发升级

**构建器 major 升级（2.x → 3.0）：**
- 设置迁移截止日期（如 6 个月）
- 系统自动重新构建所有活跃组件
- 迁移失败的组件标记为"需要更新"

#### 回退策略
每个元素版本记录 `builderVersion`，回退时：
1. 仍优先使用原构建器产物
2. 如果产物已清理，用当前构建器重新构建并标注
```

**影响范围：** 产物存储策略、长期运维成本

---

### 问题 8：v1 迁移工具的局限性说明不足

**当前状态：**
17.3 节提供"v1 转 v2 辅助器"，列出了转换步骤

**问题：**
过于乐观，没有说清楚哪些情况无法自动转换

**建议补充：**

```markdown
### 自动转换的局限性

#### ✅ 可以自动转换的场景
- 标准 Schema 声明（attrs、config、events、methods）
- 函数组件 + Props interface
- Less 样式文件
- 字面量默认值

#### ⚠️ 需要人工处理的场景
- Schema 中的 `customRequest` 函数 → 改用 context.files.upload
- Schema 中的 `condition` 函数 → 改用声明式表达式
- 组件内访问 `window.appContext` → 改用 context
- 组件内直接调用 `request()` → 改用 context.api.call
- 组件内使用项目内部 utils → 需要复制到 ZIP 包内或使用 SDK

#### ❌ 无法自动转换的场景
- 运行时生成的 Schema（工厂函数）
- 动态 import 的子组件
- 依赖非白名单的 npm 包
- 访问 Zustand store 的组件

#### 转换前检查
提供"迁移兼容性检查"工具：
1. 扫描源码中的风险模式
2. 生成检查报告（可迁移/需修改/无法迁移）
3. 给出具体修改建议
```

**影响范围：** 迁移工具实现、用户预期管理

---

## 三、缺失的章节建议

### 建议增加：成本与风险评估

```markdown
## X. 实施成本与风险

### 工作量估算

#### 前端开发（4~5 人月）
- ZIP reader + analyzer + manifest：1.5 人月
- SDK types + mock + host adapter：1 人月
- Registry + Props adapter + NgapRender：1 人月
- 元素管理上传向导：1 人月
- 独立运行时同步 + 测试：0.5~1 人月

#### 后端开发（3~4 人月）
- ZIP 上传、存储、安全检查：1 人月
- 异步构建服务（解包、分析、打包、扫描）：1.5~2 人月
- 版本管理、审核流程、产物发布：1 人月
- 接口扩展（protocol version、manifest 等）：0.5 人月

#### 总计：7~9 人月

### 关键风险

| 风险 | 影响 | 概率 | 缓解措施 |
|---|---|---|---|
| 后端字段无法扩展 | 协议识别不稳定 | 中 | 提前确认，作为硬性要求 |
| 服务端构建延期 | 无法提交审核 | 中 | 首期允许浏览器预览，异步开发服务端构建 |
| AST 推导成功率低 | 用户体验差 | 高 | 提供清晰诊断 + 手动补充路径 |
| 构建队列拥堵 | 用户等待时间长 | 中 | 构建缓存复用 + 容量规划 |
| SDK 权限被误解 | 安全风险 | 高 | 明确文档说明 + 限制上传角色 |

### 降级方案

如果服务端构建无法在首期完成：
1. 浏览器构建产物可临时发布，标记为"开发预览版"
2. 限制组件复杂度（单文件、无多模块）
3. 加强人工审核（必须 Review 源码）
4. 服务端构建完成后批量重新构建
```

---

## 四、文档结构优化建议

### 当前问题
- 4706 行，100+ KB，信息密度过高
- 安全边界说明在第 18 章，读到前面已经误解了
- 实施方案和技术细节混在一起

### 建议拆分

```
1. CUSTOM_ELEMENT_OVERVIEW.md（10~15 KB）
   - 改造目标和价值
   - 核心架构图
   - 用户体验对比
   - 实施路线图
   - 成本与风险

2. CUSTOM_ELEMENT_PROTOCOL.md（20~30 KB）
   - ZIP 目录规范
   - ngap.json 协议
   - manifest 规范
   - SDK 协议
   - 安全边界（提到前面！）

3. CUSTOM_ELEMENT_IMPLEMENTATION.md（30~40 KB）
   - 详细技术实现
   - 文件修改清单
   - 测试方案
   - 数据库字段

4. CUSTOM_ELEMENT_MIGRATION.md（10~15 KB）
   - v1 兼容策略
   - 迁移工具设计
   - 迁移步骤
```

---

## 五、修复优先级

### P0（必须在实施前修复）
1. ✅ 明确 `elementProtocolVersion` 是硬性要求
2. ✅ 补充 AST 推导失败的用户体验
3. ✅ 澄清 SDK 权限的真实作用（移到前面）
4. ✅ 明确构建异步化的用户体验
5. ✅ 明确版本锁定和升级策略

### P1（首期实施时处理）
6. ⚠️ 评估首期是否支持多模块（建议砍掉）
7. ⚠️ 补充构建器版本升级策略
8. ⚠️ 补充 v1 迁移工具局限性

### P2（后续迭代优化）
9. 📝 补充成本与风险评估章节
10. 📝 拆分文档为多个可读性更好的子文档

---

## 总体评价

**设计质量：⭐⭐⭐⭐ (85/100)**

- ✅ 架构设计合理，技术选型正确
- ✅ 细节覆盖全面，考虑了大部分场景
- ✅ 分阶段实施可行
- ⚠️ 部分逻辑矛盾需要澄清
- ⚠️ 安全模型表述有误导性
- ⚠️ 用户体验和成本考虑不足

**实施可行性：⭐⭐⭐⭐ (80/100)**

- ✅ 技术方案可落地
- ✅ 兼容性设计完整
- ⚠️ 首期范围偏大，建议收窄
- ⚠️ 依赖后端配合较多

**落地建议：先修复 P0 问题，评估 P1 问题，P2 可延后处理。**
