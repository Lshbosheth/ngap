# 引导式六期整体验收与推广准备开发方案

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 引导式六期整体验收与推广准备开发方案 |
| 文档性质 | 第六期交付实施方案，不代表已经修改业务代码 |
| 对应阶段 | 第六期：整体验收与推广准备 |
| 当前状态 | 待技术评审，前五期完成后实施 |
| 前置阶段 | 第一至第五期均达到各自开发完成门槛 |
| 前端工程 | 主 `src` 预览、应用编辑器与独立 `page/materials` 运行入口 |
| 后端工程 | 实际流程定义、历史版本、发布、业务接口联调环境 |
| 分期规划 | `GUIDED_PROCESS_PHASE_PLAN(引导式流程分期实施规划).md` |
| 前期方案 | 第一期至第五期详细开发方案 |
| 总体设计 | `GUIDED_PROCESS_REDESIGN(引导式流程展示编排升级设计).md` |
| 决策记录 | `GUIDED_PROCESS_PENDING_ITEMS(引导式待完善事项).md` |

第六期不再增加新的大范围产品能力。它把前五期组合成一套可以真实交付、配置、发布、运行、定位错误和重复验收的完整方案。

---

## 2. 本期目标

- 前五期能力按真实组合方式完成端到端验收；
- 同一流程在主预览和独立运行页关键结果一致；
- 第一批试点流程完成升级、发布和回归；
- 简单、复杂分支、大节点和重组件四类场景均有验收记录；
- 刷新、手机号变化、页签切换和异步竞态均有结论；
- 配置错误在发布前可发现，运行错误有明确提示；
- 配置人员能依靠文档独立搭建简单流程；
- 150 节点容量结论可复现；
- 正式使用范围、已知限制和后续事项明确；
- 形成签字/确认所需的交付清单和缺陷清单。

---

## 3. 本期范围边界

### 3.1 包含

- 全链路组合测试；
- 双入口一致性测试；
- 试点流程 v1→v5（或最终版本）升级；
- 后端保存、回读、复制、历史、发布联调；
- 技术错误、配置错误和引用失效验证；
- 容量和内存回归；
- 配置手册、组件接入手册、错误手册和验收记录；
- 第一批使用场景和责任人确认；
- 缺陷修复与回归；
- 已知限制和延期事项收口。

### 3.2 不包含

- 新的流程调试器；
- 自然语言生成流程；
- 移动端和无障碍；
- 浏览器刷新后断点恢复；
- 平台统一业务日志；
- 多页签并发控制；
- 自动布局；
- 未经第五期决策的画布库迁移；
- 自定义元素 v2 正式改造；
- 待完善事项 P2-01～P2-05。

验收发现阻断缺陷可以修复；如果修复需要新增上述能力，应退回产品评审并单独立项，不能悄悄扩入第六期。

---

## 4. 交付基线和冻结管理

### 4.1 基线组成

验收开始前固定：

- 流程定义 Schema 最终版本；
- 前端构建版本；
- 独立 page 构建版本；
- 后端接口和数据库版本；
- 通用组件模板版本；
- 试点流程定义版本；
- 四类性能数据集版本；
- 浏览器、机器和联调环境信息。

### 4.2 变更规则

- 验收期只接受缺陷修复和文档澄清；
- 协议字段变化必须评估所有历史和双入口影响；
- 每次修复记录受影响测试集；
- 修复合入后执行定向回归和最低完整冒烟；
- 基线变更在验收记录中留痕。

这不是建设 P2-05 灰度/version governance，只是当前交付版本的测试管理。

---

## 5. 验收角色与职责

| 角色 | 主要职责 |
| --- | --- |
| 产品/需求 | 确认冻结规则、场景结果、导航内容、已知限制和正式范围 |
| 前端开发 | 编辑器、主预览、独立页修复；自动化和性能回归 |
| 后端开发 | 保存发布链路、接口联调、测试数据和服务端缺陷修复 |
| 组件/配置开发 | 通用组件输出、事件脚本、接口映射和试点流程配置 |
| 业务验收人员 | 按真实业务步骤验证结果、提示和按钮动作 |
| 项目负责人 | 确认阻断缺陷关闭和首批使用场景 |

当前估算没有独立测试人员，因此前后端承担技术测试；业务规则仍需产品和业务验收人员确认。

---

## 6. 验收场景集

### 6.1 S1 简单流程

```text
开始 → Header → 自动查询 → 人工选择 → Footer → 结束
```

验证：

- Header 初始化数据写全局变量；
- Footer 页面启动时固定挂载；
- 自动节点加载和诊断；
- 人工 change 直接完成；
- Footer 按钮显隐与业务动作；
- 唯一结束节点。

### 6.2 S2 连续自动流程

```text
开始 → Header → 自动A → 自动B → 自动条件分支 → 人工C → Footer → 结束
```

验证 onNodeLoaded 顺序、自动静默推进、技术错误中止和 activeContentPath。

### 6.3 S3 复杂人工分支

使用“意向降档金额 + 套餐”示例：

- 未填完整不推进；
- 8 元进入 PLAN_8；
- 改 28 元释放旧路径并进入 PLAN_8_49；
- 改 49 以上再次换路；
- label/诊断明细更新；
- value 相同不重复加载。

### 6.4 S4 分支汇合

```text
A → B → D
  ↘ C ↗
```

先走 B，再改选走 C。验证旧 B、旧 D 执行实例均释放，新 C 和新 D 重新执行，最终只有一条活动路径。

### 6.5 S5 通用组件复用

同一个 CommonTable 在多个节点分别配置不同接口、标题、输出和诊断。验证节点作用域、接口结果和导航完全隔离。

### 6.6 S6 大流程和重组件

- 真实约 110 节点；
- 模拟 150 节点；
- 节点少但单节点元素多；
- 节点多但元素少；
- 左右、上下两种方向。

执行第五期固定操作脚本和一次真实预览。

### 6.7 S7 生命周期

- 浏览器刷新从开始重新执行；
- 手机号变化取消时保持；
- 确认时清空旧运行并以新号码从头执行；
- 普通页签切出切回不变化；
- Footer 始终按当前有效值计算。

### 6.8 S8 错误场景

- Header 初始化失败；
- 自动接口无返回或报错；
- 输出 required 缺失；
- diagnosis status 非 success/failure；
- 分支零匹配无默认；
- 多分支同时命中；
- 引用节点/变量/outputId 已删除；
- Footer 拓扑错误；
- 旧异步结果晚返回。

---

## 7. 双入口一致性

### 7.1 对比维度

同一已发布流程分别在主 src 预览和独立 page 运行，逐项比较：

- Header/Footer 挂载和初始化顺序；
- Content 加载顺序；
- 组件变量、全局变量和正式输出；
- diagnosis 内容和异常数量；
- transition 命中；
- activeContentPath；
- 回滚后的剩余节点；
- onNodeLoaded 次数和上下文；
- 技术错误码和用户提示；
- 刷新和手机号变化行为。

### 7.2 自动对比

建议共享核心提供测试快照：

```ts
interface GuidedRuntimeTestSnapshot {
  activeNodeIds: string[];
  activeContentPath: ActiveContentPathItemV4[];
  sharedValues: Record<string, unknown>;
  nodeResults: Record<string, unknown>;
  selectedTransitionIds: string[];
  technicalErrors: Array<{ code: string; nodeId?: string }>;
}
```

测试快照只用于自动化和诊断，不成为生产业务 API，也不保存历史轨迹。

---

## 8. 后端全链路验收

每个最终 Schema 字段验证：

- 新建保存；
- 查询回读；
- 编辑再保存；
- 复制应用/流程；
- 历史版本查看；
- 历史版本恢复定义；
- 发布；
- 独立 page 获取发布数据；
- 未知字段不被 DTO 丢弃；
- 稳定 nodeId/variableId/outputId/transitionId 不被重建；
- layoutDirection 正确回读。

发布校验验证：

- 区域唯一和首尾拓扑；
- 引用存在、类型和路径赋值；
- 正式输出和诊断绑定；
- 分支唯一命中配置；
- 默认分支；
- 唯一结束节点；
- 有 Footer 时的结束约束。

---

## 9. 试点流程升级

### 9.1 升级对象

第一期 Header/Footer 上线后创建的少量过渡流程进入升级清单。每个流程记录：

- 当前 guidedSchemaVersion；
- 节点/连线数；
- Header/Footer 配置；
- 变量和引用；
- 旧 MT/AT/VA；
- 组件模板及版本；
- 需要人工确认的歧义。

### 9.2 升级流程

```text
备份原定义
→ 运行逐版本迁移
→ 生成变更和歧义报告
→ 配置人员人工确认
→ 运行全量引用/拓扑校验
→ 保存草稿
→ 主预览验收
→ 独立 page 验收
→ 发布
→ 留存验收记录
```

不按同名静默猜测，不要求重新从零绘制整个流程。

### 9.3 回退

如果升级流程在正式发布前无法通过，可继续使用原已发布定义或暂缓场景上线。第六期不建设运行中实例灰度，但必须保留升级前定义备份。

---

## 10. 错误体系和文案

### 10.1 错误分类

| 类型 | 示例 | 行为 |
| --- | --- | --- |
| 配置阻断 | 引用失效、多个默认分支、无结束节点 | 阻止发布，定位配置位置 |
| 运行技术错误 | 接口异常、脚本异常、状态字段非法 | 停止推进，展示节点和原因 |
| 业务诊断失败 | 自动节点输出 failure | 红色诊断，仍按业务配置分支 |
| 校验未完成 | 人工必填缺失 | 停留当前节点，显示组件校验 |
| 路径竞态废弃 | 旧执行晚返回 | 不展示为用户错误，拒绝回写 |

### 10.2 文案要求

- 对配置人员显示环节名称、配置项和修复建议；
- 技术 ID 可复制但不作为唯一可读内容；
- 对业务用户不显示堆栈和内部对象；
- 技术错误和业务 failure 不能使用同一红色结论文案；
- 主预览和独立 page 使用同一错误码及核心文案。

---

## 11. 文档交付

### 11.1 配置人员手册

至少包含：

- 创建开始、Header、Content、Footer、结束节点；
- 人工/自动模式；
- 组件变量和全局变量；
- 结构化引用；
- 正式输出 value/label；
- 诊断状态和多条展示；
- 分支、默认分支和唯一命中；
- onNodeLoaded；
- Footer 显隐和按钮动作；
- 左右/上下方向；
- 保存、发布和错误修复；
- 当前不支持事项。

### 11.2 组件开发接入手册

- 组件模板与节点实例身份；
- 组件变量定义；
- change 事件集中重算；
- 输出定义和映射；
- 自动业务 status 字段；
- Promise 等待和资源注册；
- dispose 清理要求；
- 同模板多实例测试；
- 禁止组件直接指定 nextNodeId。

### 11.3 运维/问题定位清单

- 确认流程定义和构建版本；
- 主预览与独立页对比；
- 查看配置校验错误；
- 识别技术错误与业务 failure；
- 采集 Runtime 测试快照（仅测试/诊断环境）；
- 复现性能基准；
- 已知限制和升级联系人。

### 11.4 验收记录

每个场景记录数据版本、步骤、预期、实际、截图/证据、缺陷号、复测结果和确认人。

---

## 12. 缺陷分级和发布门槛

### 12.1 阻断缺陷

以下任一未关闭不得正式推广：

- 数据串值或引用到错误节点；
- 分支走错、同时保留两条活动路径；
- 旧异步结果覆盖新结果；
- Header/Footer 挂载或结束拓扑错误；
- 保存、发布或回读丢字段/重建稳定 ID；
- 主预览和独立 page 关键结果不一致；
- 手机号变化后旧数据残留；
- 150 节点无法完成约定核心操作且无正式范围限制；
- 浏览器崩溃、明显内存持续泄漏；
- 配置人员无法定位发布阻断原因。

### 12.2 可带限制发布

非核心视觉问题或已明确规避方式的问题，可经产品和负责人确认后记录在已知限制中。不得把数据正确性、路径正确性和双入口一致性降为普通限制。

---

## 13. 推广准备

### 13.1 第一批场景选择

优先选择：

- 业务规则已明确；
- 通用组件已经具备；
- 接口状态字段明确；
- 节点规模在已验证范围；
- 配置开发人员可持续配合；
- 可完整执行业务验收。

### 13.2 使用范围声明

正式说明至少写清：

- 单流程业务节点上限暂定 150；
- 支持 Header/Content/Footer/Control 和唯一结束；
- 支持组件作用域、正式输出、诊断、分支和改选回滚；
- 刷新从头、手机号变化确认后从头、页签切回保持；
- 不支持断点恢复、多页签并发、移动端、统一业务日志和自动布局；
- 业务接口正确返回和业务日志仍由配置/业务模块负责。

### 13.3 支持安排

- 明确产品、前端、后端和组件配置联系人；
- 第一批流程发布后安排短期问题收集窗口；
- 只收集缺陷和真实使用反馈，不自动承诺 P2 新能力；
- 后续改进另行排序。

---

## 14. 开发任务与预计时间

| 任务 | 主要内容 | 前端人日 | 后端人日 |
| --- | --- | ---: | ---: |
| P6-T1 验收基线和环境 | 版本、数据、场景、账号、浏览器和联调环境冻结 | 1～2 | 1 |
| P6-T2 全链路组合回归 | S1～S8、前五期组合、错误和生命周期 | 3～5 | 1～2 |
| P6-T3 双入口一致性 | Runtime 快照、逐项对比、差异修复 | 2～3 | 0～1 |
| P6-T4 试点升级与发布 | 迁移报告、人工确认、保存发布和业务验收 | 2～3 | 1～2 |
| P6-T5 容量及普通页回归 | 110/150、重组件、方向、BottomBanner/普通页面 | 1～2 | 1 |
| P6-T6 文档和培训材料 | 配置、组件接入、错误、已知限制和验收记录 | 1～2 | 1 |
| P6-T7 缺陷修复与最终验收 | 阻断问题关闭、全量冒烟、交付清单 | 2～3 | 1～2 |
| **合计** |  | **12～18** | **6～10** |

建议日历周期 **3～5 周**。该周期假设前五期已经各自达到完成门槛；若第六期才首次补前期核心能力，应退回对应阶段重新估算，而不是继续使用验收期工时。

---

## 15. 最终验收清单

### 15.1 产品能力

- [ ] Header 先初始化，Footer 固定挂载；
- [ ] Content 人工/自动语义正确；
- [ ] Control 静默执行且不进入导航；
- [ ] 所有路径最终到唯一结束节点；
- [ ] 组件作用域和全局变量边界清晰；
- [ ] value/label/diagnosis 正确；
- [ ] 分支唯一命中和默认规则正确；
- [ ] 改选后旧路径完整释放；
- [ ] activeContentPath 只含当前最终态；
- [ ] onNodeLoaded 时机正确；
- [ ] 左右/上下不改变业务语义。

### 15.2 数据和技术

- [ ] nodeId/variableId/outputId/transitionId 稳定；
- [ ] 保存、查询、复制、历史和发布不丢字段；
- [ ] 主 src/page 共用核心并结果一致；
- [ ] 旧异步回写被拒绝；
- [ ] 删除节点/变量/输出有影响分析；
- [ ] 发布阻断错误完整；
- [ ] 普通组装式页面不受影响；
- [ ] 浏览器刷新、手机号变化、页签切换符合规则。

### 15.3 性能和交付

- [ ] 110/150 节点结果可复现；
- [ ] 重组件基准通过；
- [ ] 无明显持续内存泄漏；
- [ ] 配置人员可独立完成简单流程；
- [ ] 组件开发人员可按手册接入输出和释放；
- [ ] 已知限制和正式范围已确认；
- [ ] 阻断缺陷全部关闭；
- [ ] 首批场景和责任人明确。

---

## 16. 最终交付物

第六期完成时应交付：

1. 第一至第五期完成门槛汇总表；
2. S1～S8 全链路验收记录；
3. 主预览/独立 page 一致性报告；
4. 试点流程升级和发布报告；
5. 110/150 节点及重组件性能报告；
6. 配置人员手册；
7. 组件开发接入手册；
8. 错误码和问题定位说明；
9. 已知限制与正式使用范围；
10. 缺陷清单和关闭证据；
11. 第一批正式使用场景与支持联系人；
12. 最终验收结论。

---

## 17. 开发完成门槛

1. 前五期完成门槛全部复核通过；
2. 所有阻断缺陷关闭；
3. 双入口关键结果一致；
4. 试点流程升级后无变量串值、旧路径残留或 Footer 状态错误；
5. 150 节点目标有正式可复现结论；
6. 配置人员独立搭建简单流程的演练通过；
7. 正式使用范围和已知限制经产品及项目负责人确认；
8. 交付文档完整；
9. 第一批场景、配置人员和支持联系人明确；
10. 产品、前端、后端、配置开发和业务验收共同确认可以进入正式使用。
